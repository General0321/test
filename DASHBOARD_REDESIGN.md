# XProbe 仪表盘重新设计

## 概述

XProbe 仪表盘经过全面重新设计，采用现代化的卡片式布局，提供更清晰、更直观的系统概览和实时监控。

## 🎨 设计亮点

### 1. 现代化配色方案

采用专业的配色体系，清晰区分不同类型的信息：

| 颜色 | 用途 | RGB |
|------|------|-----|
| 🔵 PRIMARY | 主要信息 | `rgb(41, 128, 185)` |
| 🟢 SUCCESS | 成功/正常状态 | `rgb(39, 174, 96)` |
| 🟠 WARNING | 警告信息 | `rgb(243, 156, 18)` |
| 🔴 DANGER | 危险/漏洞 | `rgb(231, 76, 60)` |
| 🟣 INFO | 辅助信息 | `rgb(142, 68, 173)` |
| 🔷 TEAL | 参数相关 | `rgb(22, 160, 133)` |
| 🔹 INDIGO | 接口相关 | `rgb(52, 73, 94)` |

### 2. 卡片式布局

所有内容区域采用白色卡片设计，具有：
- ✅ 圆角边框（12px）
- ✅ 微妙阴影
- ✅ 左侧彩色指示条
- ✅ 清晰的视觉层次

### 3. 响应式设计

- 自适应窗口大小
- 可滚动的内容区域
- 合理的间距和留白

## 📊 界面布局

### 顶部：标题和状态栏

```
┌────────────────────────────────────────────────────────────┐
│ XProbe 仪表盘  实时监控 & 参数收集    ● 运行中              │
│                                        模式: 参数名+关键词   │
└────────────────────────────────────────────────────────────┘
```

**功能**：
- 大标题展示扩展名称
- 实时系统状态指示器（运行中/已停止）
- 当前参数收集模式显示

### 第一行：核心统计卡片（8个）

```
┌─────────────┬─────────────┬─────────────┬─────────────┐
│ 📊 总请求数  │ ✓ 已扫描    │ ⚠ 发现漏洞   │ 🌐 主域名    │
│    1,234    │    1,234    │      15     │      5      │
└─────────────┴─────────────┴─────────────┴─────────────┘
┌─────────────┬─────────────┬─────────────┬─────────────┐
│ 🔑 参数      │ 📝 关键词    │ 🔗 接口      │ 🚀 Arjun扫描 │
│    456      │    1,280    │     89      │      3      │
└─────────────┴─────────────┴─────────────┴─────────────┘
```

**卡片设计**：
- Emoji 图标快速识别
- 大号数字清晰显示
- 主题色彩区分
- 左侧彩色条强化视觉

### 第二行：双栏详情面板

#### 左侧：参数收集统计

```
┌────────────────────────────────┐
│ 参数收集统计                    │
├────────────────────────────────┤
│ === 参数收集详情 ===           │
│                                │
│ 主域名数量: 5                  │
│ Host 数量: 12                  │
│ 接口数量: 89                   │
│ 参数数量: 456                  │
│ 关键词数量: 1,280              │
│                                │
│ 收集模式: 参数名+关键词         │
│                                │
│ 提示:                          │
│ • 参数会按主域名分组            │
│ • Arjun 扫描时自动使用          │
│ • 支持实时增量更新              │
└────────────────────────────────┘
```

#### 右侧：活动日志

```
┌────────────────────────────────┐
│ 活动日志                 [清空] │
├────────────────────────────────┤
│ [14:23:15] XProbe 仪表盘已加载  │
│ [14:23:16] 参数收集模式: ...    │
│ [14:24:03] 参数发现: ...        │
│ [14:25:12] Arjun 扫描: ...      │
│                                │
└────────────────────────────────┘
```

**特点**：
- 时间戳精确到秒
- 实时滚动显示
- 自动限制行数（500行）
- 一键清空功能

### 第三行：最近发现

```
┌────────────────────────────────────────────────────────────┐
│ 最近发现                                                    │
├──────────┬────────┬──────────────┬──────────────────────────┤
│ 时间     │ 类型   │ 目标         │ 详情                      │
├──────────┼────────┼──────────────┼──────────────────────────┤
│ 14:25:12 │ Arjun  │ api.test.com │ 发现 8 个隐藏参数          │
│ 14:24:03 │ 参数   │ test.com     │ 发现 15 个新参数           │
│ 14:20:45 │漏洞-SQL│ test.com/api │ SQL注入: id参数            │
└──────────┴────────┴──────────────┴──────────────────────────┘
```

**功能**：
- 最多显示 100 条最近记录
- 按时间倒序排列
- 类型区分：参数/Arjun/漏洞
- 快速定位重要发现

## 🔄 实时刷新机制

### 自动刷新
- **频率**：每 5 秒
- **内容**：所有统计数据
- **方式**：异步更新，不阻塞 UI

### 事件驱动更新
```java
// 参数发现时
dashboard.addParameterDiscovery("example.com", 15);

// Arjun 扫描完成时
dashboard.addArjunScan("api.example.com", 8);

// 发现漏洞时
dashboard.addVulnerabilityFound("SQL", "test.com/api", "id参数");
```

## 📱 API 接口

### 设置参数收集器

```java
DashboardTab dashboard = new DashboardTab(api, configManager, 
                                         requestFilter, logModel);

// 关联参数收集器
dashboard.setParameterCollector(parameterCollector);
```

### 添加活动日志

```java
// 基础日志
dashboard.addActivityLog("开始扫描目标...");

// 参数发现
dashboard.addParameterDiscovery("example.com", 15);
// 输出: [14:23:15] 参数发现: example.com - 新增 15 个参数

// Arjun 扫描
dashboard.addArjunScan("api.example.com", 8);
// 输出: [14:25:12] Arjun 扫描: api.example.com - 发现 8 个参数

// 漏洞发现
dashboard.addVulnerabilityFound("SQL", "test.com/api", "id参数存在注入");
// 输出: [14:20:45] 发现漏洞: SQL - test.com/api
```

### 添加发现记录

```java
// 通用发现
dashboard.addFinding("类型", "目标", "详情");

// 会同时：
// 1. 添加到"最近发现"表格
// 2. 添加到活动日志
// 3. 更新统计数据
```

### 更新系统状态

```java
// 设置为运行中
dashboard.updateSystemStatus("运行中", SUCCESS_COLOR);

// 设置为已停止
dashboard.updateSystemStatus("已停止", DANGER_COLOR);

// 设置为扫描中
dashboard.updateSystemStatus("扫描中", WARNING_COLOR);
```

## 🎯 使用场景

### 场景 1：启动时初始化

```java
// 1. 创建仪表盘
DashboardTab dashboard = new DashboardTab(api, configManager, 
                                         requestFilter, logModel);

// 2. 关联参数收集器
dashboard.setParameterCollector(parameterCollector);

// 3. 显示启动日志
dashboard.addActivityLog("XProbe 已启动");
dashboard.updateSystemStatus("运行中", SUCCESS_COLOR);
```

### 场景 2：被动扫描时

```java
// 每次收集到新参数
if (hasNewParameters) {
    dashboard.addParameterDiscovery(mainDomain, newParamsCount);
}

// 统计数据会自动更新（每5秒）
```

### 场景 3：Arjun 扫描时

```java
// 扫描前
dashboard.addActivityLog("开始 Arjun 扫描: " + target);
dashboard.updateSystemStatus("扫描中", WARNING_COLOR);

// 扫描后
dashboard.addArjunScan(target, discoveredParams);
dashboard.updateSystemStatus("运行中", SUCCESS_COLOR);
```

### 场景 4：发现漏洞时

```java
// 添加漏洞记录
dashboard.addVulnerabilityFound(
    "SQL注入",           // 类型
    "test.com/api/user", // 目标
    "id 参数存在 SQL 注入"  // 详情
);

// 会自动：
// 1. 更新"发现漏洞"统计
// 2. 添加到最近发现
// 3. 记录到活动日志
```

## 💡 设计理念

### 1. 信息层次清晰

```
顶部：一眼看到最重要的状态
  ↓
中部：核心统计数字，快速了解整体情况
  ↓
底部：详细信息和历史记录
```

### 2. 视觉引导

- **Emoji 图标**：快速识别功能区域
- **彩色编码**：不同类型信息一目了然
- **大号数字**：关键数据突出显示
- **卡片分组**：相关信息组织在一起

### 3. 用户体验

- **实时更新**：无需手动刷新
- **自动滚动**：最新信息自动显示
- **行数限制**：防止内存占用过大
- **清空功能**：随时重置日志

## 🔍 与旧版对比

| 特性 | 旧版 | 新版 |
|------|------|------|
| 布局方式 | 传统表格 | 现代卡片 |
| 配色 | 基础颜色 | 专业配色方案 |
| 信息密度 | 分散 | 集中清晰 |
| 参数统计 | 简单计数 | 详细分类统计 |
| 实时更新 | 手动刷新 | 自动刷新 |
| x8 相关 | ✅ 显示 | ❌ 移除（已废弃） |
| Arjun 统计 | ❌ 无 | ✅ 完整统计 |
| 关键词统计 | ❌ 无 | ✅ 独立统计 |
| 主域名分组 | ❌ 无 | ✅ 支持 |
| 活动日志 | 分散多处 | 统一展示 |
| 最近发现 | ❌ 无 | ✅ 表格展示 |

## 📈 统计数据说明

### 基础统计

- **总请求数**：Burp 拦截的所有请求
- **已扫描**：XProbe 实际扫描的请求
- **发现漏洞**：检测到的 High/Critical 级别漏洞

### 参数收集统计

- **主域名**：收集到的主域名数量
- **参数**：去重后的参数名称总数
- **关键词**：从参数值提取的关键词总数
- **接口**：发现的唯一接口数量

### Arjun 统计

- **Arjun扫描**：已完成的 Arjun 扫描次数
- 每次扫描的发现会记录在"最近发现"中

## 🎨 自定义配色

如需修改配色，编辑常量：

```java
private static final Color PRIMARY_COLOR = new Color(41, 128, 185);
private static final Color SUCCESS_COLOR = new Color(39, 174, 96);
// ... 其他颜色
```

## ⚙️ 性能优化

### 1. 自动限制

- **活动日志**：保留最近 500 行
- **最近发现**：保留最近 100 条
- **自动刷新**：每 5 秒（可调整）

### 2. 异步更新

所有 UI 更新都通过 `SwingUtilities.invokeLater()` 异步执行，避免阻塞主线程。

### 3. 智能滚动

日志和表格自动滚动到最新内容，无需用户手动操作。

## 🚀 未来计划

- [ ] 添加图表可视化（趋势图）
- [ ] 支持导出报告
- [ ] 添加筛选和搜索功能
- [ ] 支持自定义刷新间隔
- [ ] 添加性能监控面板

## 📝 更新日志

### v1.1.0 (2025-10-01)
- ✅ 全新现代化设计
- ✅ 8个统计卡片
- ✅ 参数收集详情面板
- ✅ 实时活动日志
- ✅ 最近发现表格
- ✅ 自动刷新机制
- ✅ 移除废弃的 x8 相关内容
- ✅ 集成 ParameterCollector 统计

---

**版本**: 1.1.0  
**设计**: 现代化卡片式布局  
**更新日期**: 2025-10-01  
**状态**: ✅ 已完成

