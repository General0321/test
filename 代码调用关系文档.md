# XProbe 完整函数调用关系文档

> 包含所有文件、所有函数、函数功能说明和完整调用关系的详细文档

**生成日期**: 2024  
**版本**: 1.0  
**文件总数**: 87个Java文件  
**函数总数**: 1993+ 个方法

---

## 📋 目录

1. [主入口模块](#主入口模块)
2. [核心处理模块](#核心处理模块)
3. [扫描引擎模块](#扫描引擎模块)
4. [主动探测模块](#主动探测模块)
5. [Arjun参数发现模块](#arjun参数发现模块)
6. [配置管理模块](#配置管理模块)
7. [UI界面模块](#ui界面模块)
8. [工具类模块](#工具类模块)
9. [数据模型模块](#数据模型模块)
10. [日志模块](#日志模块)

---

## 主入口模块

### 1. XProbe.java

**文件路径**: `src/main/java/com/xprobe/scanner/XProbe.java`  
**类名**: `XProbe`  
**实现接口**: `BurpExtension`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `initialize(MontoyaApi api)` | 插件初始化入口，创建所有核心组件 | 被Burp Suite调用 → 调用XProbeConfigManager.initialize() → 创建所有组件 |
| `constructMainTab(...)` | 构建主UI标签页 | 被initialize()调用 → 创建DashboardTab、ScanResultTab、PassiveScanConfigTab、ActiveProbeTab、UnifiedConfigTab |
| `getConfigManager()` | 获取配置管理器 | 被外部调用 |

#### 详细调用链

```
XProbe.initialize()
  ├─> XProbeConfigManager.initialize()
  │   └─> ConfigPersistence.load()
  │
  ├─> LogModel (new)
  ├─> ConfigurationManager (new)
  │   └─> ConfigurationManager.addConfiguration()
  │
  ├─> GlobalFilter (new)
  │   └─> GlobalFilter.updateWhitelist()
  │   └─> GlobalFilter.updateBlacklist()
  │
  ├─> RequestFilter (new)
  │
  ├─> RealtimeScannerRefactored (new)
  │   ├─> ArjunService (new)
  │   ├─> ParameterCollector (new)
  │   └─> ParameterManager (new)
  │
  ├─> OriginalResponseCache (new)
  ├─> ScannerFactory (new)
  │   └─> ScannerFactory.initializeScanners()
  │
  ├─> TaskScheduler (new)
  │   └─> ThreadPoolExecutor (new)
  │
  ├─> RequestHandler (new)
  │   └─> api.http().registerHttpHandler()
  │
  └─> constructMainTab()
      ├─> DashboardTab (new)
      ├─> ScanResultTab (new)
      ├─> PassiveScanConfigTab (new)
      ├─> ActiveProbeTab (new)
      └─> UnifiedConfigTab (new)
```

---

## 核心处理模块

### 2. RequestHandler.java

**文件路径**: `src/main/java/com/xprobe/scanner/core/RequestHandler.java`  
**类名**: `RequestHandler`  
**实现接口**: `HttpHandler`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `handleHttpRequestToBeSent(HttpRequestToBeSent)` | 处理即将发送的HTTP请求 | 被Burp Suite调用 → 调用RealtimeScannerRefactored.processNewRequest() |
| `handleHttpResponseReceived(HttpResponseReceived)` | 处理接收到的HTTP响应 | 被Burp Suite调用 → 调用OriginalResponseCache.put() → RealtimeScannerRefactored.processResponse() → TaskScheduler.scheduleScan() |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `collectScanTasks(HttpRequest, RequestContext)` | 收集所有需要扫描的任务 | 被handleHttpResponseReceived()调用 → 调用ConfigurationManager.getEnabledConfigurations() |
| `shouldScanRequest(HttpRequest)` | 检查请求是否应该扫描 | 被handleHttpResponseReceived()调用 → 调用GlobalFilter.shouldProcessPassive() |

#### 详细调用链

```
RequestHandler.handleHttpRequestToBeSent()
  └─> RealtimeScannerRefactored.processNewRequest()
      └─> ParameterCollector.collectFromRequest()

RequestHandler.handleHttpResponseReceived()
  ├─> OriginalResponseCache.put()
  │   └─> OriginalResponseCache.generateKey()
  │
  ├─> RealtimeScannerRefactored.processResponse()
  │   ├─> ParameterCollector.collectFromResponse()
  │   └─> RealtimeScannerRefactored.checkAndAutoTriggerArjun()
  │
  ├─> XProbeConfigManager.isPassiveScanEnabled()
  ├─> RequestHandler.shouldScanRequest()
  │   └─> GlobalFilter.shouldProcessPassive()
  │
  └─> RequestHandler.collectScanTasks()
      ├─> ConfigurationManager.getEnabledConfigurations()
      └─> TaskScheduler.scheduleScan()
          └─> TaskScheduler.executeScanTask()
```

### 3. TaskScheduler.java

**文件路径**: `src/main/java/com/xprobe/scanner/core/TaskScheduler.java`  
**类名**: `TaskScheduler`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `TaskScheduler(...)` | 构造函数，初始化线程池 | 被XProbe.initialize()调用 → 创建ThreadPoolExecutor |
| `scheduleScan(List<ScanTask>)` | 调度扫描任务 | 被RequestHandler调用 → 调用CompletableFuture.runAsync() → executeScanTask() |
| `setScanTaskListener(ScanTaskListener)` | 设置任务监听器 | 被DashboardTab调用 |
| `getProgressStatistics()` | 获取进度统计 | 被DashboardTab调用 → 调用getAllTaskProgress() |
| `getAllTaskProgress()` | 获取所有任务进度 | 被getProgressStatistics()调用 |
| `pauseAllTasksAndClear()` | 暂停所有任务并清空 | 被UI调用 |
| `shutdown()` | 关闭线程池 | 被XProbe卸载处理器调用 |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `executeScanTask(ScanTask)` | 执行单个扫描任务 | 被scheduleScan()调用 → 调用ScannerFactory.getScanner() → Scanner.canScan() → Scanner.scan() |
| `findOriginalResponse(HttpRequest)` | 查找原始响应 | 被executeScanTask()调用 → 调用OriginalResponseCache.get() |
| `logResult(ScanTask, ScanResult, TaskProgress)` | 记录扫描结果 | 被executeScanTask()调用 → 调用LogModel.addLog() → ScanTaskListener.onTaskProgress() |

#### 内部类

**TaskProgress**
- `TaskProgress(ScanTask, int)` - 构造函数
- `incrementSentRequests()` - 增加已发送请求数
- `setRunning(boolean)` - 设置运行状态
- `setCompleted(boolean)` - 设置完成状态
- `setVulnerabilityCount(int)` - 设置漏洞数量
- `isRunning()` - 是否运行中
- `isCompleted()` - 是否已完成
- `getSentRequests()` - 获取已发送请求数
- `getExpectedRequests()` - 获取预计请求数
- `getTask()` - 获取任务对象
- `getVulnerabilityCount()` - 获取漏洞数量
- `getRuleName()` - 获取规则名称

**TaskProgressStatistics**
- `TaskProgressStatistics(int, int, int, int)` - 构造函数
- `getScanningCount()` - 获取扫描中数量
- `getWaitingCount()` - 获取等待中数量
- `getTotalSent()` - 获取总发送数
- `getTotalExpected()` - 获取总预计数

#### 详细调用链

```
TaskScheduler.scheduleScan()
  └─> CompletableFuture.runAsync()
      └─> TaskScheduler.executeScanTask()
          ├─> ScannerFactory.getScanner()
          ├─> Scanner.canScan()
          ├─> Scanner.calculateExpectedRequestCount()
          ├─> TaskProgress (new)
          ├─> Scanner.scan()
          ├─> TaskScheduler.findOriginalResponse()
          │   └─> OriginalResponseCache.get()
          └─> TaskScheduler.logResult()
              ├─> LogModel.addLog()
              └─> ScanTaskListener.onTaskProgress()
```

### 4. OriginalResponseCache.java

**文件路径**: `src/main/java/com/xprobe/scanner/core/OriginalResponseCache.java`  
**类名**: `OriginalResponseCache`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `OriginalResponseCache(int)` | 构造函数，创建LRU缓存 | 被XProbe.initialize()调用 |
| `put(String, String, HttpResponse)` | 缓存响应 | 被RequestHandler调用 |
| `get(String, String)` | 获取缓存的响应 | 被TaskScheduler、UniversalScanner调用 |
| `size()` | 获取缓存大小 | 被外部调用 |
| `clear()` | 清空缓存 | 被外部调用 |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `generateKey(String, String)` | 生成缓存键 | 被put()和get()调用 |

### 5. GlobalFilter.java

**文件路径**: `src/main/java/com/xprobe/scanner/core/GlobalFilter.java`  
**类名**: `GlobalFilter`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `shouldProcessPassive(String)` | 检查URL是否应该被被动扫描处理 | 被RequestHandler、UniversalScanner调用 → 调用shouldProcess() |
| `shouldProcessActive(String)` | 检查URL是否应该被主动探测 | 被RealtimeScannerRefactored调用 → 调用shouldProcess() |
| `updateWhitelist(List<String>, boolean)` | 更新白名单 | 被XProbe、UnifiedConfigTab调用 → 调用compileWhitelistPatterns() |
| `updateBlacklist(List<String>, boolean)` | 更新黑名单 | 被XProbe、UnifiedConfigTab调用 → 调用compileBlacklistPatterns() |
| `addWhitelistItem(String)` | 添加白名单项 | 被UI调用 → 调用compileWhitelistPatterns() |
| `addBlacklistItem(String)` | 添加黑名单项 | 被UI调用 → 调用compileBlacklistPatterns() |
| `removeWhitelistItem(String)` | 移除白名单项 | 被UI调用 → 调用compileWhitelistPatterns() |
| `removeBlacklistItem(String)` | 移除黑名单项 | 被UI调用 → 调用compileBlacklistPatterns() |
| `clearWhitelist()` | 清空白名单 | 被UI调用 |
| `clearBlacklist()` | 清空黑名单 | 被UI调用 |
| `isWhitelistEnabled()` | 白名单是否启用 | Getter方法 |
| `isBlacklistEnabled()` | 黑名单是否启用 | Getter方法 |
| `isStaticResourceFilterEnabled()` | 静态资源过滤是否启用 | Getter方法 |
| `getWhitelist()` | 获取白名单 | Getter方法 |
| `getBlacklist()` | 获取黑名单 | Getter方法 |
| `setWhitelistEnabled(boolean)` | 设置白名单启用状态 | Setter方法 |
| `setBlacklistEnabled(boolean)` | 设置黑名单启用状态 | Setter方法 |
| `setStaticResourceFilterEnabled(boolean)` | 设置静态资源过滤启用状态 | Setter方法 |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `shouldProcess(String, String)` | 统一的处理逻辑 | 被shouldProcessPassive()和shouldProcessActive()调用 → 调用StaticResourceFilter.isStaticResource() |
| `compileWhitelistPatterns()` | 编译白名单正则表达式 | 被updateWhitelist()、addWhitelistItem()等调用 |
| `compileBlacklistPatterns()` | 编译黑名单正则表达式 | 被updateBlacklist()、addBlacklistItem()等调用 |

### 6. RequestFilter.java

**文件路径**: `src/main/java/com/xprobe/scanner/core/RequestFilter.java`  
**类名**: `RequestFilter`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `RequestFilter(MontoyaApi, GlobalFilter)` | 构造函数 | 被XProbe.initialize()调用 |
| `shouldScan(HttpRequestToBeSent)` | 检查请求是否应该扫描 | 被RequestHandler调用 → 调用GlobalFilter.shouldProcessActive() |
| `updateWhitelist(List<String>, boolean)` | 更新白名单 | 被UI调用 → 调用GlobalFilter.updateWhitelist() |
| `updateBlacklist(List<String>, boolean)` | 更新黑名单 | 被UI调用 → 调用GlobalFilter.updateBlacklist() |
| `clearProcessedRequests()` | 清空已处理请求 | 被UI调用 |

### 7. InjectionPointExecutor.java

**文件路径**: `src/main/java/com/xprobe/scanner/core/InjectionPointExecutor.java`  
**类名**: `InjectionPointExecutor`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `getOriginalValue(HttpRequest, Configuration.InjectionPoint)` | 获取注入点的原始值 | 被UniversalScanner调用 |
| `applySingleInjection(HttpRequest, Configuration.InjectionPoint, String)` | 应用单个注入 | 被UniversalScanner调用 → 调用私有注入方法 |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `getParameterValue(...)` | 获取参数值 | 被getOriginalValue()调用 |
| `getPathSegmentValue(...)` | 获取路径段值 | 被getOriginalValue()调用 |
| `getHeaderValue(...)` | 获取Header值 | 被getOriginalValue()调用 |
| `getCookieValue(...)` | 获取Cookie值 | 被getOriginalValue()调用 |
| `getQueryString(...)` | 获取查询字符串 | 被getOriginalValue()调用 |
| `injectParameterValue(...)` | 注入参数值 | 被applySingleInjection()调用 |
| `injectUrlPathSegment(...)` | 注入URL路径段 | 被applySingleInjection()调用 |
| `injectHeaderValue(...)` | 注入Header值 | 被applySingleInjection()调用 |
| `injectCookieValue(...)` | 注入Cookie值 | 被applySingleInjection()调用 |
| `injectQueryString(...)` | 注入查询字符串 | 被applySingleInjection()调用 |

### 8. PayloadVariableResolver.java

**文件路径**: `src/main/java/com/xprobe/scanner/core/PayloadVariableResolver.java`  
**类名**: `PayloadVariableResolver`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `PayloadVariableResolver(MontoyaApi)` | 构造函数 | 被UniversalScanner调用 |
| `resolvePayload(String)` | 解析Payload变量（无上下文） | 被UniversalScanner调用 → 调用resolvePayload(String, Map) |
| `resolvePayload(String, Map<String, String>)` | 解析Payload变量（有上下文） | 被UniversalScanner调用 → 调用resolveVariable() → CrossPairVariableExtractor.replaceVariables() |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `resolveVariable(String, Map<String, String>)` | 解析单个变量 | 被resolvePayload()调用 → 调用generateRandomString()、api.collaborator()、resolveEncoding() |
| `generateRandomString(int)` | 生成随机字符串 | 被resolveVariable()调用 |

#### 内部类

**PayloadContext**
- `PayloadContext(String, Map<String, String>)` - 构造函数
- `getResolvedPayload()` - 获取解析后的Payload
- `getGeneratedVariables()` - 获取生成的变量
- `getCollaboratorClient()` - 获取Collaborator客户端
- `getCollaboratorPayload()` - 获取Collaborator Payload
- `hasCollaborator()` - 是否使用了Collaborator
- `getCollaboratorInteractions()` - 获取Collaborator交互

### 9. CrossPairVariableExtractor.java

**文件路径**: `src/main/java/com/xprobe/scanner/core/CrossPairVariableExtractor.java`  
**类名**: `CrossPairVariableExtractor`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `extractVariables(HttpResponse, Map<String, String>)` | 从响应中提取变量 | 被UniversalScanner调用 → 调用extractFromText() |
| `replaceVariables(String, Map<String, String>)` | 替换文本中的变量占位符 | 被UniversalScanner、PayloadVariableResolver调用 → 使用PAIR_PATTERN和VAR_PATTERN |
| `containsVariablePlaceholder(String)` | 检查文本是否包含变量占位符 | 被外部调用 |
| `extractVariableNames(String)` | 提取文本中使用的所有变量名 | 被UniversalScanner调用 → 使用PAIR_PATTERN和VAR_PATTERN |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `extractFromText(String, String)` | 从文本中提取第一个匹配组的值 | 被extractVariables()调用 → 使用Pattern.compile() |
| `extractHeadersAsString(HttpResponse)` | 将响应头转换为字符串 | 被extractVariables()调用 |

### 10. UnifiedHttpEvaluator.java

**文件路径**: `src/main/java/com/xprobe/scanner/core/UnifiedHttpEvaluator.java`  
**类名**: `UnifiedHttpEvaluator`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `evaluate(HttpRequest, UnifiedHttpConfig)` | 评估HTTP请求是否匹配配置 | 被UniversalScanner调用 → 调用evaluateElement()或evaluateExpression() |
| `evaluateElement(HttpRequest, HttpElementConfig)` | 评估单个HTTP元素 | 被evaluate()调用 → 根据元素类型调用对应的评估方法 |
| `getMatchedParameterNames(HttpRequest, HttpElementConfig)` | 获取匹配的参数名列表 | 被UniversalScanner调用 |
| `getOriginalValue(HttpRequest, HttpElementConfig)` | 获取原始值 | 被UniversalScanner调用 |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `evaluateExpression(HttpRequest, UnifiedHttpConfig, String)` | 评估复杂表达式 | 被evaluate()调用 → 调用evaluateExpressionRecursive() |
| `evaluateExpressionRecursive(String, Map<Integer, Boolean>)` | 递归评估表达式 | 被evaluateExpression()调用 |
| `evaluateMethod(HttpRequest, HttpElementConfig)` | 评估HTTP方法 | 被evaluateElement()调用 → 调用matchValue() |
| `evaluateHost(HttpRequest, HttpElementConfig)` | 评估主机 | 被evaluateElement()调用 → 调用matchValue() |
| `evaluatePath(HttpRequest, HttpElementConfig)` | 评估URL路径 | 被evaluateElement()调用 → 调用matchValue() |
| `evaluateParameter(HttpRequest, HttpElementConfig)` | 评估参数 | 被evaluateElement()调用 → 调用matchValue() |
| `evaluateHeader(HttpRequest, HttpElementConfig)` | 评估Header | 被evaluateElement()调用 → 调用matchValue() |
| `evaluateCookie(HttpRequest, HttpElementConfig)` | 评估Cookie | 被evaluateElement()调用 → 调用matchValue() |
| `evaluateBody(HttpRequest, HttpElementConfig)` | 评估Body | 被evaluateElement()调用 → 调用matchValue() |
| `matchValue(String, ValueMatchConfig)` | 匹配值 | 被所有评估方法调用 |

### 11. UnifiedResponseEvaluator.java

**文件路径**: `src/main/java/com/xprobe/scanner/core/UnifiedResponseEvaluator.java`  
**类名**: `UnifiedResponseEvaluator`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `evaluate(HttpResponse, UnifiedResponseConfig, PayloadContext, long, Map<String, String>)` | 评估HTTP响应是否匹配配置 | 被UniversalScanner调用 → 调用evaluateElement() |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `evaluateElement(HttpResponse, ResponseElementConfig, PayloadContext, long, Map<String, String>)` | 评估单个响应元素 | 被evaluate()调用 → 根据元素类型调用对应的评估方法 |
| `evaluateStatusCode(HttpResponse, MatchConfig, PayloadContext, Map<String, String>)` | 评估状态码 | 被evaluateElement()调用 → 调用compareNumeric()或matchTextValues() |
| `evaluateHeaders(HttpResponse, MatchConfig, PayloadContext, Map<String, String>)` | 评估响应头 | 被evaluateElement()调用 → 调用matchTextValues() |
| `evaluateBody(HttpResponse, MatchConfig, PayloadContext, Map<String, String>)` | 评估响应体 | 被evaluateElement()调用 → 调用matchTextValues() |
| `evaluateTime(long, MatchConfig)` | 评估响应时间 | 被evaluateElement()调用 → 调用compareNumeric() |
| `evaluateLength(HttpResponse, MatchConfig)` | 评估响应长度 | 被evaluateElement()调用 → 调用compareNumeric() |
| `evaluateCollaborator(PayloadContext, MatchConfig)` | 评估Collaborator | 被evaluateElement()调用 → 调用PayloadContext.getCollaboratorInteractions() |
| `evaluateExpression(String, Map<Integer, Boolean>)` | 评估表达式 | 被evaluate()调用 |
| `matchTextValues(String, MatchConfig, PayloadContext, Map<String, String>)` | 匹配文本值 | 被多个评估方法调用 → 调用matchValue() |
| `matchValue(String, String, MatchType, boolean)` | 匹配单个值 | 被matchTextValues()调用 |
| `compareNumeric(long, MatchConfig)` | 比较数值 | 被多个评估方法调用 |

### 12. ResponseComparisonEngine.java

**文件路径**: `src/main/java/com/xprobe/scanner/core/ResponseComparisonEngine.java`  
**类名**: `ResponseComparisonEngine`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `hasSignificantDifference(HttpResponse, HttpResponse, ResponseComparisonConfig)` | 对比两个响应是否存在显著差异 | 被UniversalScanner调用 |
| `verifyTimeDelay(long, long, int)` | 验证时间延迟是否符合预期 | 被UniversalScanner调用 |
| `verifyLinearTimeProgression(List<Long>, List<Long>, int)` | 验证多个时间延迟是否呈线性关系 | 被UniversalScanner调用 |
| `hasReflection(HttpResponse, String)` | 检查响应中是否包含反射的Payload标记 | 被UniversalScanner调用 |
| `hasExploitableReflection(HttpResponse, String, List<String>)` | 检查反射的Payload是否处于可利用的上下文 | 被UniversalScanner调用 |
| `extractFeatureSummary(HttpResponse)` | 提取响应特征摘要 | 被UniversalScanner调用 |
| `verifyTimeDelayWithReference(long, String, Map<Integer, PairResponseFeatures>, int)` | 验证时间延迟（带引用） | 被UniversalScanner调用 → 调用resolveFeatureReference() |
| `hasSignificantDifferenceWithReference(HttpResponse, String, Map<Integer, PairResponseFeatures>, ResponseComparisonConfig)` | 对比响应差异（带引用） | 被UniversalScanner调用 → 调用resolveFeatureReference() |
| `checkFeatureCondition(long, String, Map<Integer, PairResponseFeatures>)` | 检查特征条件 | 被UniversalScanner调用 → 调用resolveFeatureReference() |
| `resolveFeatureReference(String, Map<Integer, PairResponseFeatures>)` | 解析特征引用 | 被多个方法调用 |
| `compareResponseBody(PairResponseFeatures, PairResponseFeatures, ResponseComparisonConfig)` | 对比响应体 | 被UniversalScanner调用 |
| `isBodyEqual(PairResponseFeatures, PairResponseFeatures)` | 检查响应体是否相等 | 被compareResponseBody()调用 |
| `evaluateCrossPairFeature(PairResponseFeatures, String, Map<Integer, PairResponseFeatures>, ResponseComparisonConfig)` | 评估跨Pair特征 | 被UniversalScanner调用 → 调用多个辅助方法 |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `cleanDynamicContent(String, ResponseComparisonConfig)` | 清理响应中的动态内容 | 被hasSignificantDifference()调用 |
| `calculateSimilarity(String, String)` | 计算内容相似度 | 被hasSignificantDifference()调用 |

### 13. ConditionExpression.java

**文件路径**: `src/main/java/com/xprobe/scanner/core/ConditionExpression.java`  
**类名**: `ConditionExpression` (抽象类)

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `evaluate(HttpRequest)` | 评估表达式（抽象方法） | 被子类实现 |
| `toExpressionString()` | 转换为字符串表示（抽象方法） | 被子类实现 |

#### 内部类

**ConditionNode**
- `ConditionNode(Configuration.RequestCondition, int)` - 构造函数
- `evaluate(HttpRequest)` - 评估条件节点 → 调用RequestConditionEvaluator.evaluateCondition()
- `toExpressionString()` - 转换为字符串
- `getCondition()` - 获取条件
- `getConditionIndex()` - 获取条件索引

**LogicalNode**
- `LogicalNode(Operator)` - 构造函数
- `addChild(ConditionExpression)` - 添加子节点
- `addChildren(List<ConditionExpression>)` - 添加多个子节点
- `evaluate(HttpRequest)` - 评估逻辑节点 → 递归调用子节点的evaluate()
- `toExpressionString()` - 转换为字符串
- `getOperator()` - 获取操作符
- `getChildren()` - 获取子节点列表

**Builder**
- `fromSimpleConditions(List<Configuration.RequestCondition>)` - 从条件列表创建表达式
- `and(ConditionExpression...)` - 创建AND表达式
- `or(ConditionExpression...)` - 创建OR表达式
- `not(ConditionExpression)` - 创建NOT表达式

### 14. RequestConditionEvaluator.java

**文件路径**: `src/main/java/com/xprobe/scanner/core/RequestConditionEvaluator.java`  
**类名**: `RequestConditionEvaluator`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `evaluateCondition(HttpRequest, Configuration.RequestCondition)` | 评估请求条件 | 被ConditionExpression.ConditionNode调用 |

### 15. DeduplicationKeyGenerator.java

**文件路径**: `src/main/java/com/xprobe/scanner/core/DeduplicationKeyGenerator.java`  
**类名**: `DeduplicationKeyGenerator`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `generateKey(String, String, String, String, Configuration, String, Integer)` | 生成去重键 | 被UniversalScanner调用 |
| `detectGranularity(Configuration)` | 自动检测去重颗粒度 | 被generateKey()调用 |

### 16. PairResponseFeatures.java

**文件路径**: `src/main/java/com/xprobe/scanner/core/PairResponseFeatures.java`  
**类名**: `PairResponseFeatures`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `fromResponse(int, HttpResponse, long, boolean)` | 从响应创建特征对象 | 被UniversalScanner调用 |
| `addVariable(String, String)` | 添加变量 | 被UniversalScanner调用 |
| `getFeatureValue(String)` | 获取特征值 | 被ResponseComparisonEngine调用 |
| `getFeatureNumericValue(String)` | 获取特征数值 | 被ResponseComparisonEngine调用 |
| `getPairId()` | 获取Pair ID | Getter方法 |
| `setPairId(int)` | 设置Pair ID | Setter方法 |
| `getResponseTime()` | 获取响应时间 | Getter方法 |
| `setResponseTime(long)` | 设置响应时间 | Setter方法 |
| `getResponseLength()` | 获取响应长度 | Getter方法 |
| `setResponseLength(int)` | 设置响应长度 | Setter方法 |
| `getStatusCode()` | 获取状态码 | Getter方法 |
| `setStatusCode(int)` | 设置状态码 | Setter方法 |
| `getHeaderCount()` | 获取Header数量 | Getter方法 |
| `setHeaderCount(int)` | 设置Header数量 | Setter方法 |
| `getResponse()` | 获取响应对象 | Getter方法 |
| `setResponse(HttpResponse)` | 设置响应对象 | Setter方法 |
| `getCustomVariables()` | 获取自定义变量 | Getter方法 |
| `setCustomVariables(Map<String, String>)` | 设置自定义变量 | Setter方法 |
| `getSummary()` | 获取摘要 | Getter方法 |
| `toString()` | 转换为字符串 | Getter方法 |
| `getResponseBodyHash()` | 获取响应体哈希 | Getter方法 |
| `setResponseBodyHash(String)` | 设置响应体哈希 | Setter方法 |
| `getResponseBodyContent()` | 获取响应体内容 | Getter方法 |
| `setResponseBodyContent(String)` | 设置响应体内容 | Setter方法 |
| `getResponseBodyClean()` | 获取清理后的响应体 | Getter方法 |
| `setResponseBodyClean(String)` | 设置清理后的响应体 | Setter方法 |

### 17. ScanTaskListener.java

**文件路径**: `src/main/java/com/xprobe/scanner/core/ScanTaskListener.java`  
**类名**: `ScanTaskListener` (接口)

#### 方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `onTaskProgress(TaskScheduler.TaskProgress)` | 任务进度更新回调 | 被TaskScheduler调用 |

---

## 扫描引擎模块

### 18. Scanner.java

**文件路径**: `src/main/java/com/xprobe/scanner/scanners/Scanner.java`  
**类名**: `Scanner` (接口)

#### 方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `getType()` | 获取扫描器类型 | 被ScannerFactory调用 |
| `getName()` | 获取扫描器名称 | 被UI调用 |
| `getDescription()` | 获取扫描器描述 | 被UI调用 |
| `canScan(ScanTask)` | 判断是否可以扫描 | 被TaskScheduler调用 |
| `calculateExpectedRequestCount(ScanTask)` | 计算预计请求数 | 被TaskScheduler调用 |
| `scan(ScanTask)` | 执行扫描 | 被TaskScheduler调用 |

### 19. AbstractScanner.java

**文件路径**: `src/main/java/com/xprobe/scanner/scanners/AbstractScanner.java`  
**类名**: `AbstractScanner` (抽象类)

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `AbstractScanner(MontoyaApi, RealtimeScannerRefactored)` | 构造函数 | 被子类调用 |
| `getType()` | 获取扫描器类型（抽象方法） | 被子类实现 |
| `getName()` | 获取扫描器名称（抽象方法） | 被子类实现 |
| `getDescription()` | 获取扫描器描述（抽象方法） | 被子类实现 |
| `canScan(ScanTask)` | 判断是否可以扫描（抽象方法） | 被子类实现 |
| `calculateExpectedRequestCount(ScanTask)` | 计算预计请求数（抽象方法） | 被子类实现 |
| `scan(ScanTask)` | 执行扫描（抽象方法） | 被子类实现 |

### 20. UniversalScanner.java

**文件路径**: `src/main/java/com/xprobe/scanner/scanners/UniversalScanner.java`  
**类名**: `UniversalScanner`  
**继承**: `AbstractScanner`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `UniversalScanner(MontoyaApi, RealtimeScannerRefactored, XProbeConfigManager, OriginalResponseCache)` | 构造函数 | 被ScannerFactory调用 |
| `getType()` | 获取扫描器类型 | 返回"UNIVERSAL_RULE_SCANNER" |
| `getName()` | 获取扫描器名称 | 返回"通用规则扫描器" |
| `getDescription()` | 获取扫描器描述 | 返回描述字符串 |
| `canScan(ScanTask)` | 判断是否可以扫描 | 被TaskScheduler调用 → 调用多个辅助方法 |
| `calculateExpectedRequestCount(ScanTask)` | 计算预计请求数 | 被TaskScheduler调用 → 调用simulateDeduplication() |
| `scan(ScanTask)` | 执行扫描 | 被TaskScheduler调用 → 调用evaluatePair() |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `areAllTargetsDeduplicated(HttpRequest, Configuration, List<RuleMatchPair>)` | 检查是否所有目标都已去重 | 被canScan()和scan()调用 → 调用collectInjectionTargets() → DeduplicationKeyGenerator.generateKey() |
| `simulateDeduplication(List<InjectionTarget>, Configuration, HttpRequest, Integer)` | 模拟去重逻辑 | 被calculateExpectedRequestCount()调用 → 调用DeduplicationKeyGenerator.generateKey() |
| `detectGranularityForCalculation(Configuration)` | 自动检测去重颗粒度 | 被simulateDeduplication()调用 |
| `evaluatePair(RuleMatchPair, HttpRequest, PayloadVariableResolver, Configuration, List<PairEvaluationResult>, Map<Integer, PairResponseFeatures>, Map<String, String>)` | 评估单个配对 | 被scan()调用 → 调用多个辅助方法 |
| `filterDuplicateTargets(List<InjectionTarget>, Configuration, HttpRequest, Integer)` | 去重过滤 | 被evaluatePair()调用 → 调用DeduplicationKeyGenerator.generateKey() → RealtimeScannerRefactored.isAlreadyProcessed() |
| `markTargetAsProcessed(InjectionTarget)` | 标记目标为已处理 | 被evaluateBatchMode()和evaluateIndividualMode()调用 → 调用RealtimeScannerRefactored.markAsProcessed() |
| `getGlobalInjectionMode()` | 获取全局注入模式 | 被多个方法调用 → 调用XProbeConfigManager.getGlobalInjectionMode() |
| `evaluateBatchMode(...)` | 批量模式评估 | 被evaluatePair()调用 → 调用多个辅助方法 |
| `evaluateIndividualMode(...)` | 逐个模式评估 | 被evaluatePair()调用 → 调用多个辅助方法 |
| `collectInjectionTargets(HttpRequest, HttpElementConfig)` | 收集注入目标 | 被多个方法调用 |
| `shouldMatchTarget(String, HttpElementConfig)` | 判断目标名称是否匹配 | 被collectInjectionTargets()调用 |
| `injectPayload(HttpRequest, HttpElementConfig, String)` | 注入Payload | 被evaluateBatchMode()调用 → 调用InjectionPointExecutor.applySingleInjection() |
| `injectPayloadToSingleTarget(HttpRequest, HttpElementConfig, String, InjectionTarget)` | 注入Payload到单个目标 | 被evaluateIndividualMode()调用 → 调用InjectionPointExecutor.applySingleInjection() |
| `generateCartesianProduct(List<List<T>>)` | 生成笛卡尔积 | 被evaluateBatchMode()和evaluateIndividualMode()调用 |
| `evaluateCrossPairComparison(RuleMatchPair, HttpResponse, long, Map<Integer, PairResponseFeatures>)` | 评估跨Pair对比 | 被evaluatePair()调用 → 调用ResponseComparisonEngine.evaluateCrossPairFeature() |
| `evaluatePairExpression(Map<Integer, Boolean>, String)` | 评估配对表达式 | 被scan()调用 → 调用ConditionExpression.evaluate() |
| `topologicalSortPairs(List<RuleMatchPair>)` | 拓扑排序配对 | 被scan()调用 → 调用CrossPairVariableExtractor.extractVariableNames() |
| `needsBodyComparison(RuleMatchPair)` | 判断是否需要保存响应体 | 被evaluatePair()调用 |
| `getCachedResponse(HttpRequest)` | 获取缓存的响应 | 被evaluatePair()调用 → 调用OriginalResponseCache.get() |
| `isRequestModified(HttpRequest, HttpRequest)` | 检查请求是否被修改 | 被evaluateBatchMode()和evaluateIndividualMode()调用 |
| `registerPayloadVariables(Integer, PayloadContext, Map<String, String>)` | 注册Payload变量 | 被evaluatePair()调用 |
| `registerExtractedVariables(Integer, Map<String, String>, Map<String, String>)` | 注册提取的变量 | 被evaluatePair()调用 |
| `matchesValue(String, String, MatchType, boolean)` | 匹配值 | 被shouldMatchTarget()调用 |

#### 内部类

**PairEvaluationResult**
- `PairEvaluationResult(boolean, HttpResponse, HttpRequest, long)` - 构造函数
- `PairEvaluationResult(boolean)` - 构造函数（简化版）

**PayloadCombination**
- `PayloadCombination(...)` - 构造函数

**InjectionTarget**
- 内部类，用于表示注入目标

### 21. ScannerFactory.java

**文件路径**: `src/main/java/com/xprobe/scanner/scanners/ScannerFactory.java`  
**类名**: `ScannerFactory`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ScannerFactory(MontoyaApi, RealtimeScannerRefactored, XProbeConfigManager, OriginalResponseCache)` | 构造函数 | 被XProbe.initialize()调用 → 调用initializeScanners() |
| `getScanner(String)` | 获取扫描器 | 被TaskScheduler调用 |
| `registerScanner(Scanner)` | 注册扫描器 | 被initializeScanners()调用 |
| `registerCustomScanner(Scanner)` | 注册自定义扫描器 | 被外部调用 |
| `getAvailableScannerTypes()` | 获取可用的扫描器类型 | 被外部调用 |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `initializeScanners()` | 初始化扫描器 | 被构造函数调用 → 调用registerScanner(UniversalScanner) |

---

## 主动探测模块

### 22. RealtimeScannerRefactored.java

**文件路径**: `src/main/java/com/xprobe/scanner/active/RealtimeScannerRefactored.java`  
**类名**: `RealtimeScannerRefactored`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `RealtimeScannerRefactored(MontoyaApi, ConfigurationManager, GlobalFilter, LogModel, XProbeConfig)` | 构造函数 | 被XProbe.initialize()调用 → 创建ArjunService、ParameterCollector、ParameterManager |
| `setTaskScheduler(TaskScheduler)` | 设置任务调度器 | 被XProbe.initialize()调用 |
| `setActiveProbeTab(ActiveProbeTab)` | 设置主动探测标签页 | 被XProbe.initialize()调用 |
| `processNewRequest(HttpRequest)` | 处理新请求 | 被RequestHandler调用 → 调用ParameterCollector.collectFromRequest() → checkAndAutoTriggerArjun() |
| `processResponse(HttpRequest, HttpResponseReceived)` | 处理响应 | 被RequestHandler调用 → 调用ParameterCollector.collectFromResponse() |
| `triggerManualArjunScan(HttpRequest, Set<String>, boolean)` | 手动触发Arjun扫描 | 被ActiveProbeTab调用 → 调用ArjunService.scan() |
| `triggerManualEndpointScan(String, boolean, boolean)` | 手动触发接口探测 | 被ActiveProbeTab调用 → 调用validateEndpointWithRandomPath() → ParameterCollector.collectFromRequest() |
| `triggerArjunForMainDomain(String)` | 为主域名触发Arjun | 被多个方法调用 → 调用ArjunService.scan() |
| `triggerArjunForHost(String, String)` | 为指定子域名触发Arjun | 被ActiveProbeTab调用 → 调用ArjunService.scan() |
| `triggerInterfaceDiscovery()` | 触发接口探测 | 被ActiveProbeTab调用 → 调用ParameterCollector.collectFromRequest() |
| `setCollectionMode(ParameterCollector.CollectionMode)` | 设置收集模式 | 被XProbe.initialize()调用 → 调用ParameterCollector.setCollectionMode() |
| `addGlobalCustomParameters(Set<String>)` | 添加全局自定义参数 | 被XProbe.initialize()调用 → 调用ArjunService.setUserCustomDictionary() |
| `setMinParameterThreshold(int)` | 设置最小参数阈值 | 被XProbe.initialize()调用 |
| `setCooldownSeconds(int)` | 设置冷却时间 | 被XProbe.initialize()调用 |
| `getParameterCollector()` | 获取参数收集器 | 被XProbe、DashboardTab调用 |
| `getArjunService()` | 获取Arjun服务 | 被XProbe、DashboardTab调用 |
| `getGlobalFilter()` | 获取全局过滤器 | 被UniversalScanner调用 |
| `isAlreadyProcessed(String)` | 检查是否已处理 | 被UniversalScanner调用 → 调用BoundedCache.containsKey() |
| `markAsProcessed(String)` | 标记为已处理 | 被UniversalScanner调用 → 调用BoundedCache.put() |
| `startRealtimeScanning()` | 启动实时扫描 | 被ActiveProbeTab调用 |
| `stopRealtimeScanning()` | 停止实时扫描 | 被ActiveProbeTab调用 |
| `shutdown()` | 关闭资源 | 被XProbe卸载处理器调用 → 调用ArjunService.shutdown() |
| `addArjunResultListener(ArjunResultListener)` | 添加Arjun结果监听器 | 被ActiveProbeTab调用 |
| `removeArjunResultListener(ArjunResultListener)` | 移除Arjun结果监听器 | 被ActiveProbeTab调用 |
| `notifyArjunResult(...)` | 通知Arjun结果 | 被内部调用 → 调用所有监听器 |
| `triggerVulnerabilityScan(HttpRequest, Set<String>)` | 触发漏洞扫描 | 被内部调用 → 调用TaskScheduler.scheduleScan() |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `checkAndAutoTriggerArjun(HttpRequest)` | 检查并自动触发Arjun | 被processNewRequest()调用 → 调用多个辅助方法 |
| `periodicArjunCheck()` | 定时检查Arjun | 被定时器调用 |
| `modifyRequestHost(HttpRequest, String)` | 修改请求的host | 被triggerArjunForHost()调用 |
| `extractMainDomain(HttpRequest)` | 提取主域名 | 被多个方法调用 |
| `validateEndpointWithRandomPath(String, String, String, String)` | 验证端点（随机路径） | 被triggerManualEndpointScan()调用 → 调用buildRandomPathBaseline() |
| `buildRandomPathBaseline(String, String, String, String)` | 建立随机路径基线 | 被validateEndpointWithRandomPath()调用 → 调用api.http().sendRequest() |
| `performIncrementalArjunScan(...)` | 执行增量Arjun扫描 | 被多个方法调用 → 调用ArjunService.scan() |
| `performIncrementalArjunScanFromCollectedData()` | 从收集的数据执行增量Arjun扫描 | 被定时器调用 → 调用ArjunService.scan() |
| `scanManualEndpoint(String, boolean, boolean)` | 扫描手动端点 | 被performIncrementalArjunScan()调用 |
| `groupRequestsByMainDomain(List<HttpRequestResponse>)` | 按主域名分组请求 | 被performIncrementalArjunScan()调用 |

#### 内部类

**RandomPathBaseline**
- 用于存储随机路径基线信息

**ArjunResultListener**
- 接口，用于监听Arjun结果

### 23. ParameterCollector.java

**文件路径**: `src/main/java/com/xprobe/scanner/active/ParameterCollector.java`  
**类名**: `ParameterCollector`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ParameterCollector(MontoyaApi)` | 构造函数 | 被RealtimeScannerRefactored调用 |
| `setCollectionMode(CollectionMode)` | 设置收集模式 | 被RealtimeScannerRefactored调用 |
| `getCollectionMode()` | 获取收集模式 | Getter方法 |
| `collectFromRequest(HttpRequest)` | 从请求中收集参数 | 被RealtimeScannerRefactored调用 → 调用多个辅助方法 |
| `collectFromResponse(HttpRequest, HttpResponse)` | 从响应中收集参数 | 被RealtimeScannerRefactored调用 → 调用多个辅助方法 |
| `getParametersForMainDomain(String)` | 获取主域名的所有参数 | 被多个方法调用 |
| `getParametersForHost(String)` | 获取特定host的参数 | 被外部调用 |
| `getHostsForMainDomain(String)` | 获取主域名的所有host | 被外部调用 |
| `getEndpointsForMainDomain(String)` | 获取主域名的所有接口 | 被外部调用 |
| `getEndpointKeysForMainDomain(String)` | 获取主域名的所有接口键 | 被多个方法调用 |
| `getEndpointTemplate(String, String)` | 获取接口的请求模板 | 被外部调用 |
| `getEndpointTemplate(String, EndpointKey)` | 获取接口的请求模板（通过键） | 被多个方法调用 |
| `getKeywordsForMainDomain(String)` | 获取主域名的关键词 | 被外部调用 |
| `getAllMainDomains()` | 获取所有主域名 | 被多个方法调用 |
| `getStatistics()` | 获取统计信息 | 被DashboardTab调用 |
| `getLastUpdateTimeForDomain(String)` | 获取域名的最后更新时间 | 被外部调用 |
| `clear()` | 清空所有数据 | 被外部调用 |
| `clearMainDomain(String)` | 清空指定主域名的数据 | 被外部调用 |
| `clearHost(String, String)` | 清空指定子域名的数据 | 被外部调用 |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `extractMainDomain(String)` | 提取主域名 | 被多个方法调用 |
| `extractParameters(HttpRequest)` | 提取参数 | 被collectFromRequest()调用 → 调用extractUrlParameters()、extractBodyParameters()、extractHeaderParameters() |
| `extractUrlParameters(HttpRequest)` | 提取URL参数 | 被extractParameters()调用 |
| `extractBodyParameters(HttpRequest)` | 提取Body参数 | 被extractParameters()调用 |
| `extractHeaderParameters(HttpRequest)` | 提取Header参数 | 被extractParameters()调用 |
| `extractParametersFromResponse(HttpResponse)` | 从响应中提取参数 | 被collectFromResponse()调用 → 使用正则表达式 |
| `extractKeywordsFromResponse(HttpResponse)` | 从响应中提取关键词 | 被collectFromResponse()调用 → 调用extractKeywords() |
| `extractKeywords(String)` | 提取关键词 | 被extractKeywordsFromResponse()调用 |
| `getContentType(HttpRequest)` | 获取Content-Type | 被多个方法调用 |
| `normalizeContentType(String)` | 规范化Content-Type | 被多个方法调用 |

#### 内部类

**CollectionMode**
- `PARAMETERS_ONLY` - 仅收集参数名
- `PARAMETERS_AND_KEYWORDS` - 收集参数名+关键词

**DomainData**
- 内部类，用于存储域名数据

**EndpointKey**
- 内部类，用于表示接口键

**CollectorStatistics**
- 内部类，用于存储统计信息

### 24. ParameterManager.java

**文件路径**: `src/main/java/com/xprobe/scanner/active/ParameterManager.java`  
**类名**: `ParameterManager`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ParameterManager()` | 构造函数 | 被RealtimeScannerRefactored调用 |
| `getIncrementalParameters(String, String, String, String, Set<String>)` | 获取增量参数 | 被RealtimeScannerRefactored调用 |
| `markParametersAsScanned(String, String, String, String, Set<String>)` | 标记参数为已扫描 | 被RealtimeScannerRefactored调用 |
| `clearScannedParameters(String, String, String, String)` | 清空已扫描参数 | 被外部调用 |
| `clearAllScannedParameters()` | 清空所有已扫描参数 | 被外部调用 |

---

## Arjun参数发现模块

### 25. ArjunService.java

**文件路径**: `src/main/java/com/xprobe/scanner/active/arjun/ArjunService.java`  
**类名**: `ArjunService`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ArjunService(MontoyaApi, LogModel)` | 构造函数（简化版） | 被RealtimeScannerRefactored调用 → 委托到完整构造函数 |
| `ArjunService(MontoyaApi, LogModel, ArjunConfig)` | 构造函数（标准版） | 被RealtimeScannerRefactored调用 → 委托到完整构造函数 |
| `ArjunService(MontoyaApi, LogModel, ArjunConfig, XProbeConfig)` | 构造函数（完整版） | 被RealtimeScannerRefactored调用 → 创建ParamDiscoveryEngine |
| `scan(HttpRequest, Set<String>)` | 扫描URL查找隐藏参数 | 被RealtimeScannerRefactored调用 → 调用ParamDiscoveryEngine.scan() |
| `getStatistics()` | 获取统计信息 | 被DashboardTab调用 |
| `getConfig()` | 获取配置 | Getter方法 |
| `setUserCustomDictionary(Set<String>)` | 设置用户自定义字典 | 被RealtimeScannerRefactored调用 |
| `getUserCustomDictionary()` | 获取用户自定义字典 | Getter方法 |
| `shutdown()` | 关闭服务 | 被RealtimeScannerRefactored.shutdown()调用 → 调用ParamDiscoveryEngine.shutdown() |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `logArjunStart(String, String, int)` | 记录Arjun扫描开始日志 | 被scan()调用 |
| `logArjunSuccess(String, String, Set<String>, long)` | 记录Arjun扫描成功日志 | 被scan()调用 |
| `logArjunFailure(String, String, String)` | 记录Arjun扫描失败日志 | 被scan()调用 |
| `convertToArjunResult(DiscoveryResult)` | 转换DiscoveryResult为ArjunResult | 被scan()调用 |

#### 内部类

**ArjunResult**
- `success(String, Set<String>, List<String>)` - 创建成功结果
- `error(String)` - 创建错误结果
- `isSuccess()` - 是否成功
- `getUrl()` - 获取URL
- `getFoundParameters()` - 获取发现的参数
- `getOutput()` - 获取输出
- `getErrorMessage()` - 获取错误消息

**ArjunStatistics**
- 构造函数和Getter方法

### 26. ParamDiscoveryEngine.java

**文件路径**: `src/main/java/com/xprobe/scanner/active/arjun/ParamDiscoveryEngine.java`  
**类名**: `ParamDiscoveryEngine`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ParamDiscoveryEngine(MontoyaApi)` | 构造函数（默认） | 被ArjunService调用 → 委托到完整构造函数 |
| `ParamDiscoveryEngine(MontoyaApi, int, int, boolean, int, int)` | 构造函数（标准版） | 被ArjunService调用 → 委托到完整构造函数 |
| `ParamDiscoveryEngine(MontoyaApi, int, int, boolean, int, int, Map<String, String>)` | 构造函数（完整版） | 被ArjunService调用 → 创建所有组件 |
| `scan(HttpRequest, Set<String>)` | 启动参数发现扫描 | 被ArjunService调用 → 调用initialize() → narrowDown() → verify() |
| `shutdown()` | 关闭引擎 | 被ArjunService.shutdown()调用 |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `initialize(HttpRequest, Set<String>)` | 初始化：稳定性探测+建立基线 | 被scan()调用 → 调用多个辅助方法 |
| `narrowDown(ScanContext)` | 分块爆破+递归缩小 | 被scan()调用 → 调用ChunkProcessor.createChunks() → ConcurrentProcessor.processChunks() |
| `verify(ScanContext, Set<ParamCandidate>)` | 最终验证 | 被scan()调用 → 调用ParamVerifier.verify() |
| `recursiveNarrow(ScanContext, Set<String>)` | 递归缩小 | 被narrowDown()调用 |
| `sendRequestWithRetry(HttpRequest, String, boolean)` | 发送请求（带重试） | 被initialize()调用 → 调用BurpHttpRequester.sendRequest() |
| `countRemainingFactors(BaselineFactors)` | 计算剩余因子数量 | 被initialize()调用 |
| `isUnhealthyStatusCode(int)` | 检查是否不健康状态码 | 被initialize()调用 |
| `generateRandomString(int)` | 生成随机字符串 | 被多个方法调用 |

### 27. BurpHttpRequester.java

**文件路径**: `src/main/java/com/xprobe/scanner/active/arjun/http/BurpHttpRequester.java`  
**类名**: `BurpHttpRequester`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `BurpHttpRequester(MontoyaApi, int, boolean, int, Map<String, String>)` | 构造函数 | 被ParamDiscoveryEngine调用 |
| `sendRequest(HttpRequest)` | 发送请求 | 被ParamDiscoveryEngine调用 → 调用RateLimiter.acquire() → api.http().sendRequest() |
| `buildTestRequest(HttpRequest, Map<String, String>)` | 构建测试请求 | 被ParamDiscoveryEngine调用 |

#### 内部类

**RequestResult**
- 用于表示请求结果

### 28. AnomalyDetector.java

**文件路径**: `src/main/java/com/xprobe/scanner/active/arjun/core/AnomalyDetector.java`  
**类名**: `AnomalyDetector`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `AnomalyDetector(MontoyaApi)` | 构造函数 | 被ParamDiscoveryEngine调用 |
| `compare(HttpResponse, BaselineFactors, Map<String, String>)` | 检测响应异常 | 被ParamDiscoveryEngine调用 → 调用多个辅助方法 |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `isParamReflected(String, String)` | 检查参数名是否被反射 | 被compare()调用 |
| `isValueReflected(String, String)` | 检查参数值是否被反射 | 被compare()调用 |
| `getHeaderKeys(HttpResponse)` | 获取响应头键列表 | 被compare()调用 |
| `getRedirectLocation(HttpResponse)` | 获取重定向位置 | 被compare()调用 |
| `countLines(String)` | 计算行数 | 被compare()调用 |
| `removeTags(String)` | 移除HTML标签 | 被compare()调用 |

### 29. ResponseBaseline.java

**文件路径**: `src/main/java/com/xprobe/scanner/active/arjun/core/ResponseBaseline.java`  
**类名**: `ResponseBaseline`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ResponseBaseline(MontoyaApi)` | 构造函数 | 被ParamDiscoveryEngine调用 |
| `define(HttpResponse, HttpResponse, String, String, Set<String>)` | 建立基线规则 | 被ParamDiscoveryEngine.initialize()调用 → 创建BaselineFactors |

### 30. ChunkProcessor.java

**文件路径**: `src/main/java/com/xprobe/scanner/active/arjun/core/ChunkProcessor.java`  
**类名**: `ChunkProcessor`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ChunkProcessor(int)` | 构造函数 | 被ParamDiscoveryEngine调用 |
| `createChunks(Set<String>, int)` | 创建参数分块 | 被ParamDiscoveryEngine调用 |
| `createChunks(Set<String>)` | 创建参数分块（使用默认大小） | 被ParamDiscoveryEngine调用 → 调用createChunks(Set, int) |
| `calculateOptimalChunkSize(int, int)` | 动态计算最优chunk大小 | 被外部调用 |

### 31. ParamVerifier.java

**文件路径**: `src/main/java/com/xprobe/scanner/active/arjun/core/ParamVerifier.java`  
**类名**: `ParamVerifier`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ParamVerifier(MontoyaApi, BurpHttpRequester)` | 构造函数 | 被ParamDiscoveryEngine调用 |
| `verify(ScanContext, Set<ParamCandidate>)` | 验证参数 | 被ParamDiscoveryEngine.verify()调用 → 调用BurpHttpRequester.sendRequest() → AnomalyDetector.compare() |

### 32. ConcurrentProcessor.java

**文件路径**: `src/main/java/com/xprobe/scanner/active/arjun/concurrent/ConcurrentProcessor.java`  
**类名**: `ConcurrentProcessor`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ConcurrentProcessor(MontoyaApi, int)` | 构造函数 | 被ParamDiscoveryEngine调用 |
| `processChunks(...)` | 并发处理chunks | 被ParamDiscoveryEngine.narrowDown()调用 → 使用线程池并发执行 |

### 33. ErrorHandler.java

**文件路径**: `src/main/java/com/xprobe/scanner/active/arjun/error/ErrorHandler.java`  
**类名**: `ErrorHandler`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ErrorHandler(MontoyaApi, int, int, boolean)` | 构造函数 | 被ParamDiscoveryEngine调用 |
| `shouldRetry(Exception, int)` | 判断是否应该重试 | 被RetryStrategy调用 |
| `handleError(Exception, int)` | 处理错误 | 被RetryStrategy调用 |
| `reset()` | 重置错误处理器 | 被ParamDiscoveryEngine.scan()调用 |

### 34. RetryStrategy.java

**文件路径**: `src/main/java/com/xprobe/scanner/active/arjun/error/RetryStrategy.java`  
**类名**: `RetryStrategy`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `RetryStrategy(MontoyaApi, int)` | 构造函数 | 被ParamDiscoveryEngine调用 |
| `executeWithRetry(Supplier<HttpResponse>, ErrorHandler, String)` | 执行带重试的操作 | 被ParamDiscoveryEngine.initialize()调用 → 调用ErrorHandler.shouldRetry() |

### 35. RateLimiter.java

**文件路径**: `src/main/java/com/xprobe/scanner/active/arjun/error/RateLimiter.java`  
**类名**: `RateLimiter`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `RateLimiter(int)` | 构造函数 | 被BurpHttpRequester调用 |
| `acquire()` | 获取许可 | 被BurpHttpRequester.sendRequest()调用 |

---

## 配置管理模块

### 36. XProbeConfigManager.java

**文件路径**: `src/main/java/com/xprobe/scanner/config/XProbeConfigManager.java`  
**类名**: `XProbeConfigManager`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `XProbeConfigManager(ConfigPersistence)` | 构造函数 | 被XProbe.initialize()调用 |
| `initialize()` | 初始化配置管理器 | 被XProbe.initialize()调用 → 调用ConfigPersistence.load() |
| `getConfig()` | 获取当前配置（深拷贝） | 被多个组件调用 → 调用XProbeConfig.copy() |
| `getConfigCopy()` | 获取配置副本（已废弃） | 被外部调用 → 调用getConfig() |
| `updateConfig(Consumer<XProbeConfig>)` | 事务式更新配置 | 被UI调用 → 调用saveConfig() |
| `saveConfig(XProbeConfig)` | 保存配置并通知订阅者 | 被多个方法调用 → 调用ConfigPersistence.save() → notifyListeners() |
| `reload()` | 重新加载配置 | 被外部调用 → 调用ConfigPersistence.forceReload() → notifyListeners() |
| `subscribe(Consumer<XProbeConfig>)` | 订阅配置变更事件 | 被DashboardTab、UnifiedConfigTab调用 |
| `unsubscribe(Consumer<XProbeConfig>)` | 取消订阅 | 被外部调用 |
| `isPassiveScanEnabled()` | 检查被动扫描是否启用 | 被RequestHandler调用 → 调用getConfigReference() |
| `getGlobalInjectionMode()` | 获取全局注入模式 | 被UniversalScanner调用 → 调用getConfigReference() |
| `getScanResultLogMode()` | 获取扫描结果日志模式 | 被TaskScheduler调用 → 调用getConfigReference() |
| `exportRules(File)` | 导出规则 | 被UI调用 → 调用RulePersistence.exportRules() |
| `importRules(File, boolean)` | 导入规则 | 被UI调用 → 调用RulePersistence.importRules() → validateImportedRules() → ConfigurationManager.saveConfigurations() |
| `validateImportedRules(List<Configuration>)` | 验证导入的规则 | 被importRules()调用 → 调用hasConflictingId() |
| `hasConflictingId(List<Configuration>, String)` | 检查是否有冲突的ID | 被validateImportedRules()调用 |
| `saveRulesToExternalFile()` | 保存规则到外部文件 | 被外部调用 → 调用RulePersistence.exportRules() |
| `loadRulesFromExternalFile()` | 从外部文件加载规则 | 被外部调用 → 调用RulePersistence.importRules() |
| `syncRules()` | 同步规则 | 被外部调用 → 调用saveRulesToExternalFile()或loadRulesFromExternalFile() |
| `getConfigFilePath()` | 获取配置文件路径 | 被外部调用 → 调用ConfigPersistence.getConfigFilePath() |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `getConfigReference()` | 获取配置引用（仅供内部使用） | 被便捷方法调用 |
| `notifyListeners(XProbeConfig)` | 通知所有订阅者 | 被saveConfig()和reload()调用 |

### 37. ConfigurationManager.java

**文件路径**: `src/main/java/com/xprobe/scanner/config/ConfigurationManager.java`  
**类名**: `ConfigurationManager`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ConfigurationManager()` | 构造函数 | 被XProbe.initialize()调用 |
| `addConfiguration(Configuration)` | 添加配置 | 被XProbe.initialize()、UI调用 |
| `removeConfiguration(String)` | 移除配置 | 被UI调用 |
| `getConfiguration(String)` | 获取配置 | 被多个组件调用 |
| `getAllConfigurations()` | 获取所有配置 | 被多个组件调用 |
| `getEnabledConfigurations()` | 获取启用的配置 | 被RequestHandler调用 |
| `saveConfigurations()` | 保存配置 | 被UI调用 → 调用ConfigPersistence.save() |
| `loadConfigurations()` | 加载配置 | 被UI调用 → 调用ConfigPersistence.load() |

### 38. ConfigPersistence.java

**文件路径**: `src/main/java/com/xprobe/scanner/config/ConfigPersistence.java`  
**类名**: `ConfigPersistence`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ConfigPersistence()` | 构造函数 | 被XProbeConfigManager调用 → 创建ObjectMapper |
| `save(XProbeConfig)` | 保存配置到磁盘（带备份和原子性写入） | 被XProbeConfigManager调用 → 调用backupConfig() → mapper.writeValue() |
| `load()` | 从磁盘加载配置（带缓存和备份恢复） | 被XProbeConfigManager.initialize()调用 → 调用mapper.readValue() |
| `forceReload()` | 强制重新加载配置（跳过缓存） | 被XProbeConfigManager.reload()调用 → 调用load() |
| `invalidateCache()` | 使缓存失效 | 被外部调用 |
| `getConfigFilePath()` | 获取配置文件路径 | 被XProbeConfigManager调用 |
| `configFileExists()` | 检查配置文件是否存在 | 被外部调用 |
| `deleteConfigFile()` | 删除配置文件 | 被外部调用 |
| `backupFileExists()` | 检查备份文件是否存在 | 被外部调用 |
| `getBackupFilePath()` | 获取备份文件路径 | 被外部调用 |
| `createManualBackup()` | 手动创建备份 | 被外部调用 → 调用Files.copy() |
| `loadFromBackup()` | 从备份恢复（不保存） | 被外部调用 → 调用mapper.readValue() |
| `deleteBackupFile()` | 删除备份文件 | 被外部调用 |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `createDefaultConfig()` | 创建默认配置 | 被load()调用 → 创建XProbeConfig对象 |

### 39. RulePersistence.java

**文件路径**: `src/main/java/com/xprobe/scanner/config/RulePersistence.java`  
**类名**: `RulePersistence`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `RulePersistence()` | 构造函数 | 被XProbeConfigManager调用 |
| `exportRules(File, List<Configuration>)` | 导出规则到JSON文件 | 被XProbeConfigManager.exportRules()调用 → 调用ObjectMapper.writeValue() |
| `importRules(File)` | 从JSON文件导入规则 | 被XProbeConfigManager.importRules()调用 → 调用ObjectMapper.readValue() |

### 40. ConfigValidator.java

**文件路径**: `src/main/java/com/xprobe/scanner/config/ConfigValidator.java`  
**类名**: `ConfigValidator`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `validate(Configuration)` | 验证配置有效性 | 被UI调用 |
| `validatePair(RuleMatchPair)` | 验证配对有效性 | 被UI调用 |

### 41. XProbeConfig.java

**文件路径**: `src/main/java/com/xprobe/scanner/config/XProbeConfig.java`  
**类名**: `XProbeConfig`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `XProbeConfig()` | 构造函数 | 被ConfigPersistence.createDefaultConfig()调用 |
| `copy()` | 创建配置的深拷贝 | 被XProbeConfigManager.getConfig()调用 |
| `isEnablePassiveScan()` | 检查被动扫描是否启用 | Getter方法 |
| `setEnablePassiveScan(boolean)` | 设置被动扫描启用状态 | Setter方法 |
| `getGlobalInjectionMode()` | 获取全局注入模式 | Getter方法 |
| `setGlobalInjectionMode(Configuration.InjectionMode)` | 设置全局注入模式 | Setter方法 |
| `getScanResultLogMode()` | 获取扫描结果日志模式 | Getter方法 |
| `setScanResultLogMode(String)` | 设置扫描结果日志模式 | Setter方法 |
| `getWhitelist()` | 获取白名单 | Getter方法 |
| `setWhitelist(List<String>)` | 设置白名单 | Setter方法 |
| `isWhitelistEnabled()` | 白名单是否启用 | Getter方法 |
| `setWhitelistEnabled(boolean)` | 设置白名单启用状态 | Setter方法 |
| `getBlacklist()` | 获取黑名单 | Getter方法 |
| `setBlacklist(List<String>)` | 设置黑名单 | Setter方法 |
| `isBlacklistEnabled()` | 黑名单是否启用 | Getter方法 |
| `setBlacklistEnabled(boolean)` | 设置黑名单启用状态 | Setter方法 |
| `getCollectionMode()` | 获取收集模式 | Getter方法 |
| `setCollectionMode(String)` | 设置收集模式 | Setter方法 |
| `getGlobalParameters()` | 获取全局参数 | Getter方法 |
| `setGlobalParameters(Set<String>)` | 设置全局参数 | Setter方法 |
| `getArjunRealtimeThreshold()` | 获取Arjun实时阈值 | Getter方法 |
| `setArjunRealtimeThreshold(int)` | 设置Arjun实时阈值 | Setter方法 |
| `getArjunRealtimeInterval()` | 获取Arjun实时间隔 | Getter方法 |
| `setArjunRealtimeInterval(int)` | 设置Arjun实时间隔 | Setter方法 |
| `getScannerCoreThreads()` | 获取扫描器核心线程数 | Getter方法 |
| `setScannerCoreThreads(int)` | 设置扫描器核心线程数 | Setter方法 |
| `getScannerMaxThreads()` | 获取扫描器最大线程数 | Getter方法 |
| `setScannerMaxThreads(int)` | 设置扫描器最大线程数 | Setter方法 |
| `getScannerQueueSize()` | 获取扫描器队列大小 | Getter方法 |
| `setScannerQueueSize(int)` | 设置扫描器队列大小 | Setter方法 |
| `getScannerKeepAliveSeconds()` | 获取扫描器空闲回收时间 | Getter方法 |
| `setScannerKeepAliveSeconds(long)` | 设置扫描器空闲回收时间 | Setter方法 |
| `getArjunRateLimit()` | 获取Arjun速率限制 | Getter方法 |
| `setArjunRateLimit(int)` | 设置Arjun速率限制 | Setter方法 |
| `isArjunStableMode()` | Arjun稳定模式是否启用 | Getter方法 |
| `setArjunStableMode(boolean)` | 设置Arjun稳定模式 | Setter方法 |
| `getArjunThreads()` | 获取Arjun线程数 | Getter方法 |
| `setArjunThreads(int)` | 设置Arjun线程数 | Setter方法 |
| `getArjunMaxRetries()` | 获取Arjun最大重试次数 | Getter方法 |
| `setArjunMaxRetries(int)` | 设置Arjun最大重试次数 | Setter方法 |
| `getArjunCustomHeaders()` | 获取Arjun自定义HTTP头 | Getter方法 |
| `setArjunCustomHeaders(Map<String, String>)` | 设置Arjun自定义HTTP头 | Setter方法 |
| `getScanConfigurations()` | 获取扫描配置列表 | Getter方法 |
| `setScanConfigurations(List<Configuration>)` | 设置扫描配置列表 | Setter方法 |

### 42. Configuration.java

**文件路径**: `src/main/java/com/xprobe/scanner/config/Configuration.java`  
**类名**: `Configuration`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `Configuration()` | 构造函数 | 被多个组件调用 |
| `getId()` | 获取配置ID | Getter方法 |
| `setId(String)` | 设置配置ID | Setter方法 |
| `getCustomLabel()` | 获取自定义标签 | Getter方法 |
| `setCustomLabel(String)` | 设置自定义标签 | Setter方法 |
| `isEnabled()` | 是否启用 | Getter方法 |
| `setEnabled(boolean)` | 设置启用状态 | Setter方法 |
| `getPairs()` | 获取配对列表 | Getter方法 |
| `setPairs(List<RuleMatchPair>)` | 设置配对列表 | Setter方法 |
| `getDeduplicationGranularity()` | 获取去重颗粒度 | Getter方法 |
| `setDeduplicationGranularity(DeduplicationGranularity)` | 设置去重颗粒度 | Setter方法 |
| `generateId()` | 生成唯一ID | 被ConfigurationManager调用 |

#### 内部枚举

**InjectionMode**
- `BATCH` - 批量模式
- `INDIVIDUAL` - 逐个模式

**DeduplicationGranularity**
- `GLOBAL` - 全局去重
- `HOST` - 主机级别去重
- `PATH` - 路径级别去重
- `REQUEST` - 请求级别去重
- `PARAMETER` - 参数级别去重
- `AUTO` - 自动检测

### 43. RuleMatchPair.java

**文件路径**: `src/main/java/com/xprobe/scanner/config/RuleMatchPair.java`  
**类名**: `RuleMatchPair`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `RuleMatchPair()` | 构造函数 | 被多个组件调用 |
| `getId()` | 获取配对ID | Getter方法 |
| `setId(Integer)` | 设置配对ID | Setter方法 |
| `getDisplayLabel()` | 获取显示标签 | Getter方法 |
| `setDisplayLabel(String)` | 设置显示标签 | Setter方法 |
| `getRequestConfig()` | 获取请求配置 | Getter方法 |
| `setRequestConfig(UnifiedHttpConfig)` | 设置请求配置 | Setter方法 |
| `getResponseConfig()` | 获取响应配置 | Getter方法 |
| `setResponseConfig(UnifiedResponseConfig)` | 设置响应配置 | Setter方法 |
| `getComparisonConfig()` | 获取对比配置 | Getter方法 |
| `setComparisonConfig(ResponseComparisonConfig)` | 设置对比配置 | Setter方法 |
| `getExtractVariables()` | 获取变量提取规则 | Getter方法 |
| `setExtractVariables(Map<String, String>)` | 设置变量提取规则 | Setter方法 |
| `getPairExpression()` | 获取配对表达式 | Getter方法 |
| `setPairExpression(String)` | 设置配对表达式 | Setter方法 |

### 44. UnifiedHttpConfig.java

**文件路径**: `src/main/java/com/xprobe/scanner/config/UnifiedHttpConfig.java`  
**类名**: `UnifiedHttpConfig`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `UnifiedHttpConfig()` | 构造函数 | 被多个组件调用 |
| `getElements()` | 获取元素配置列表 | Getter方法 |
| `setElements(List<HttpElementConfig>)` | 设置元素配置列表 | Setter方法 |
| `getConditionExpression()` | 获取条件表达式 | Getter方法 |
| `setConditionExpression(String)` | 设置条件表达式 | Setter方法 |

#### 内部类

**HttpElementConfig**
- 各种Getter/Setter方法
- `getType()` - 获取元素类型
- `setType(ElementType)` - 设置元素类型
- `isUseForMatch()` - 是否用于匹配
- `setUseForMatch(boolean)` - 设置是否用于匹配
- `isUseForInjection()` - 是否用于注入
- `setUseForInjection(boolean)` - 设置是否用于注入
- `getPayloads()` - 获取Payload列表
- `setPayloads(List<String>)` - 设置Payload列表

**ElementType**
- `METHOD` - HTTP方法
- `HOST` - 主机
- `PATH` - 路径
- `PARAMETER` - 参数
- `HEADER` - 请求头
- `COOKIE` - Cookie
- `BODY` - 请求体

### 45. UnifiedResponseConfig.java

**文件路径**: `src/main/java/com/xprobe/scanner/config/UnifiedResponseConfig.java`  
**类名**: `UnifiedResponseConfig`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `UnifiedResponseConfig()` | 构造函数 | 被多个组件调用 |
| `getElements()` | 获取响应元素配置列表 | Getter方法 |
| `setElements(List<ResponseElementConfig>)` | 设置响应元素配置列表 | Setter方法 |
| `getConditionExpression()` | 获取条件表达式 | Getter方法 |
| `setConditionExpression(String)` | 设置条件表达式 | Setter方法 |

#### 内部类

**ResponseElementConfig**
- 各种Getter/Setter方法
- `getType()` - 获取元素类型
- `setType(ElementType)` - 设置元素类型
- `getMatchConfig()` - 获取匹配配置
- `setMatchConfig(MatchConfig)` - 设置匹配配置

**ElementType**
- `STATUS_CODE` - 状态码
- `RESPONSE_HEADERS` - 响应头
- `RESPONSE_BODY` - 响应体
- `RESPONSE_TIME` - 响应时间
- `RESPONSE_LENGTH` - 响应长度
- `COLLABORATOR` - Collaborator交互

### 46. ResponseComparisonConfig.java

**文件路径**: `src/main/java/com/xprobe/scanner/config/ResponseComparisonConfig.java`  
**类名**: `ResponseComparisonConfig`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ResponseComparisonConfig()` | 构造函数 | 被多个组件调用 |
| `getBodyComparisonMode()` | 获取响应体对比模式 | Getter方法 |
| `setBodyComparisonMode(BodyComparisonMode)` | 设置响应体对比模式 | Setter方法 |
| `getReferencePairId()` | 获取引用配对ID | Getter方法 |
| `setReferencePairId(Integer)` | 设置引用配对ID | Setter方法 |
| `getReferenceFeatureType()` | 获取引用特征类型 | Getter方法 |
| `setReferenceFeatureType(String)` | 设置引用特征类型 | Setter方法 |
| `getBodyComparisonReferencePairId()` | 获取响应体对比引用配对ID | Getter方法 |
| `setBodyComparisonReferencePairId(Integer)` | 设置响应体对比引用配对ID | Setter方法 |
| `isCompareStatusCode()` | 是否对比状态码 | Getter方法 |
| `setCompareStatusCode(boolean)` | 设置是否对比状态码 | Setter方法 |
| `isCompareLength()` | 是否对比长度 | Getter方法 |
| `setCompareLength(boolean)` | 设置是否对比长度 | Setter方法 |
| `isCompareContent()` | 是否对比内容 | Getter方法 |
| `setCompareContent(boolean)` | 设置是否对比内容 | Setter方法 |
| `getLengthDifferenceThreshold()` | 获取长度差异阈值 | Getter方法 |
| `setLengthDifferenceThreshold(int)` | 设置长度差异阈值 | Setter方法 |
| `getContentSimilarityThreshold()` | 获取内容相似度阈值 | Getter方法 |
| `setContentSimilarityThreshold(double)` | 设置内容相似度阈值 | Setter方法 |
| `getContentDifferencePatterns()` | 获取内容差异模式 | Getter方法 |
| `setContentDifferencePatterns(List<String>)` | 设置内容差异模式 | Setter方法 |
| `getIgnoreDynamicPatterns()` | 获取忽略动态内容模式 | Getter方法 |
| `setIgnoreDynamicPatterns(List<String>)` | 设置忽略动态内容模式 | Setter方法 |

#### 内部枚举

**BodyComparisonMode**
- `NONE` - 不对比
- `EXACT` - 精确对比
- `SIMILARITY` - 相似度对比
- `DIFFERENCE` - 差异对比

---

## UI界面模块

### 47. DashboardTab.java

**文件路径**: `src/main/java/com/xprobe/scanner/ui/DashboardTab.java`  
**类名**: `DashboardTab`  
**实现接口**: `ScanTaskListener`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `DashboardTab(MontoyaApi, ConfigurationManager, RequestFilter, LogModel)` | 构造函数 | 被XProbe.constructMainTab()调用 → 调用initializeComponents() → setupLayout() → setupEventListeners() → startAutoRefresh() |
| `setParameterCollector(ParameterCollector)` | 设置参数收集器引用 | 被XProbe.constructMainTab()调用 |
| `setArjunService(ArjunService)` | 设置Arjun服务引用 | 被XProbe.constructMainTab()调用 |
| `setTaskScheduler(TaskScheduler)` | 设置任务调度器 | 被XProbe.constructMainTab()调用 → 调用TaskScheduler.setScanTaskListener() |
| `getComponent()` | 获取UI组件 | 被XProbe.constructMainTab()调用 |
| `onTaskProgress(TaskScheduler.TaskProgress)` | 任务进度更新回调 | 被TaskScheduler调用 → 调用updateProgress() |
| `cleanup()` | 清理资源 | 被XProbe卸载处理器调用 → 停止定时器 |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `initializeComponents()` | 初始化UI组件 | 被构造函数调用 |
| `setupLayout()` | 设置布局 | 被构造函数调用 |
| `setupEventListeners()` | 设置事件监听器 | 被构造函数调用 → 调用XProbeConfigManager.subscribe() |
| `startAutoRefresh()` | 启动自动刷新 | 被构造函数调用 → 创建定时器 |
| `updateStatistics()` | 更新统计信息 | 被多个方法调用 → 调用ParameterCollector.getStatistics() → ArjunService.getStatistics() → TaskScheduler.getProgressStatistics() |
| `updateProgress()` | 更新进度信息 | 被onTaskProgress()调用 → 调用TaskScheduler.getProgressStatistics() |
| `addActivityLog(String)` | 添加活动日志 | 被多个方法调用 |
| `createValueLabel(String)` | 创建值标签 | 被initializeComponents()调用 |
| `createStatCard(JLabel, Color)` | 创建统计卡片 | 被setupLayout()调用 |

### 48. ScanResultTab.java

**文件路径**: `src/main/java/com/xprobe/scanner/ui/ScanResultTab.java`  
**类名**: `ScanResultTab`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ScanResultTab(MontoyaApi, LogModel, RealtimeScannerRefactored, OriginalResponseCache)` | 构造函数 | 被XProbe.constructMainTab()调用 → 调用initializeComponents() → setupLayout() → setupEventListeners() |
| `getComponent()` | 获取UI组件 | 被XProbe.constructMainTab()调用 |
| `cleanup()` | 清理资源 | 被XProbe卸载处理器调用 |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `initializeComponents()` | 初始化UI组件 | 被构造函数调用 → 调用api.userInterface().createHttpRequestEditor() → api.userInterface().createHttpResponseEditor() |
| `setupLayout()` | 设置布局 | 被构造函数调用 |
| `setupEventListeners()` | 设置事件监听器 | 被构造函数调用 → 调用LogModel.addListener() |
| `updateStatistics()` | 更新统计信息 | 被多个方法调用 → 调用LogModel.getVulnerabilityCount() |
| `exportResults(File)` | 导出结果 | 被UI调用 → 调用LogModel导出方法 |
| `clearResults()` | 清空结果 | 被UI调用 → 调用LogModel.clear() → RealtimeScannerRefactored.clearScanCache() → OriginalResponseCache.clear() |
| `applyFilters()` | 应用过滤器 | 被事件监听器调用 → 调用TableRowSorter.setRowFilter() |

### 49. ActiveProbeTab.java

**文件路径**: `src/main/java/com/xprobe/scanner/ui/ActiveProbeTab.java`  
**类名**: `ActiveProbeTab`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ActiveProbeTab(MontoyaApi, ConfigurationManager, RealtimeScannerRefactored)` | 构造函数 | 被XProbe.constructMainTab()调用 → 调用initializeComponents() → setupLayout() → setupEventListeners() |
| `getComponent()` | 获取UI组件 | 被XProbe.constructMainTab()调用 |
| `getActiveScanner()` | 获取主动扫描器 | Getter方法 |
| `addInterfaceDiscoveryResult(...)` | 添加接口探测结果 | 被RealtimeScannerRefactored调用 |
| `onArjunResult(...)` | Arjun结果回调 | 被RealtimeScannerRefactored调用 → 调用addArjunResultToTable() |
| `cleanup()` | 清理资源 | 被XProbe卸载处理器调用 → 停止定时器 |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `initializeComponents()` | 初始化UI组件 | 被构造函数调用 |
| `setupLayout()` | 设置布局 | 被构造函数调用 |
| `setupEventListeners()` | 设置事件监听器 | 被构造函数调用 |
| `startArjunScan()` | 启动Arjun扫描 | 被UI调用 → 调用RealtimeScannerRefactored.triggerManualArjunScan() |
| `startInterfaceDiscovery()` | 启动接口探测 | 被UI调用 → 调用RealtimeScannerRefactored.triggerInterfaceDiscovery() |
| `refreshCollectedData()` | 刷新收集的数据 | 被定时器调用 → 调用ParameterCollector.getStatistics() |
| `clearResults()` | 清空结果 | 被UI调用 → 调用ParameterDataStorage.clear() |
| `clearArjunCache()` | 清空Arjun缓存 | 被UI调用 → 调用ParameterManager.clearAllScannedParameters() |
| `toggleMasterSwitch()` | 切换主开关 | 被UI调用 → 调用RealtimeScannerRefactored.setArjunEnabled() |
| `addArjunResultToTable(...)` | 添加Arjun结果到表格 | 被onArjunResult()调用 |
| `showContextMenu(int, int, int)` | 显示上下文菜单 | 被事件监听器调用 |
| `triggerInterfaceDiscoveryForMainDomain(String)` | 为主域名触发接口探测 | 被UI调用 → 调用RealtimeScannerRefactored.triggerInterfaceDiscoveryForHost() |
| `triggerInterfaceDiscoveryForHost(String, String)` | 为子域名触发接口探测 | 被UI调用 → 调用RealtimeScannerRefactored.triggerInterfaceDiscoveryForHost() |

### 50. PassiveScanConfigTab.java

**文件路径**: `src/main/java/com/xprobe/scanner/ui/PassiveScanConfigTab.java`  
**类名**: `PassiveScanConfigTab`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `PassiveScanConfigTab(MontoyaApi, ConfigurationManager, XProbeConfigManager)` | 构造函数 | 被XProbe.constructMainTab()调用 |
| `getComponent()` | 获取UI组件 | 被XProbe.constructMainTab()调用 |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `initializeComponents()` | 初始化UI组件 | 被构造函数调用 |
| `setupLayout()` | 设置布局 | 被构造函数调用 |
| `setupEventListeners()` | 设置事件监听器 | 被构造函数调用 |
| `loadConfigurations()` | 加载配置 | 被构造函数调用 → 调用ConfigurationManager.getAllConfigurations() |
| `saveConfiguration(Configuration)` | 保存配置 | 被UI调用 → 调用ConfigurationManager.addConfiguration() → ConfigurationManager.saveConfigurations() |
| `deleteConfiguration(String)` | 删除配置 | 被UI调用 → 调用ConfigurationManager.removeConfiguration() → ConfigurationManager.saveConfigurations() |

### 51. UnifiedConfigTab.java

**文件路径**: `src/main/java/com/xprobe/scanner/ui/UnifiedConfigTab.java`  
**类名**: `UnifiedConfigTab`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `UnifiedConfigTab(MontoyaApi, ConfigurationManager, GlobalFilter, RealtimeScannerRefactored, XProbeConfigManager)` | 构造函数 | 被XProbe.constructMainTab()调用 |
| `setArjunService(ArjunService)` | 设置Arjun服务 | 被XProbe.constructMainTab()调用 |
| `getComponent()` | 获取UI组件 | 被XProbe.constructMainTab()调用 |
| `cleanup()` | 清理资源 | 被XProbe卸载处理器调用 |

#### 私有方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `initializeComponents()` | 初始化UI组件 | 被构造函数调用 |
| `setupLayout()` | 设置布局 | 被构造函数调用 |
| `setupEventListeners()` | 设置事件监听器 | 被构造函数调用 → 调用XProbeConfigManager.subscribe() |
| `loadConfig()` | 加载配置 | 被构造函数调用 → 调用XProbeConfigManager.getConfig() |
| `saveConfig()` | 保存配置 | 被UI调用 → 调用XProbeConfigManager.updateConfig() |
| `updateWhitelist()` | 更新白名单 | 被UI调用 → 调用GlobalFilter.updateWhitelist() → XProbeConfigManager.updateConfig() |
| `updateBlacklist()` | 更新黑名单 | 被UI调用 → 调用GlobalFilter.updateBlacklist() → XProbeConfigManager.updateConfig() |

---

## 工具类模块

### 52. StaticResourceFilter.java

**文件路径**: `src/main/java/com/xprobe/scanner/utils/StaticResourceFilter.java`  
**类名**: `StaticResourceFilter`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `isStaticResource(String)` | 检查URL是否是静态资源 | 被GlobalFilter调用 |
| `shouldCollectParameters(String)` | 是否应该收集参数 | 被ParameterCollector调用 |
| `shouldScanWithArjun(String)` | 是否应该用Arjun扫描 | 被ArjunService调用 |

### 53. PayloadEncoder.java

**文件路径**: `src/main/java/com/xprobe/scanner/utils/PayloadEncoder.java`  
**类名**: `PayloadEncoder`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `encodeBase64(String)` | Base64编码 | 被PayloadVariableResolver调用 |
| `encodeUrl(String)` | URL编码 | 被PayloadVariableResolver调用 |

### 54. BoundedCache.java

**文件路径**: `src/main/java/com/xprobe/scanner/utils/BoundedCache.java`  
**类名**: `BoundedCache<K, V>`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `BoundedCache(int)` | 构造函数 | 被ParameterCollector、RealtimeScannerRefactored调用 |
| `put(K, V)` | 添加元素 | 被多个组件调用 |
| `get(K)` | 获取元素 | 被多个组件调用 |
| `containsKey(K)` | 检查是否包含键 | 被RealtimeScannerRefactored调用 |
| `size()` | 获取大小 | Getter方法 |
| `clear()` | 清空缓存 | 被外部调用 |

---

## 数据模型模块

### 55. ScanTask.java

**文件路径**: `src/main/java/com/xprobe/scanner/models/ScanTask.java`  
**类名**: `ScanTask`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ScanTask(String, Configuration, HttpRequest, RequestContext)` | 构造函数 | 被RequestHandler调用 |
| `getScanType()` | 获取扫描类型 | Getter方法 |
| `getConfiguration()` | 获取配置 | Getter方法 |
| `getRequest()` | 获取请求 | Getter方法 |
| `getContext()` | 获取上下文 | Getter方法 |

### 56. ScanResult.java

**文件路径**: `src/main/java/com/xprobe/scanner/models/ScanResult.java`  
**类名**: `ScanResult`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ScanResult(...)` | 构造函数 | 被UniversalScanner调用 |
| `isVulnerable()` | 是否发现漏洞 | Getter方法 |
| `getRuleName()` | 获取规则名称 | Getter方法 |
| `getRequest()` | 获取请求 | Getter方法 |
| `getResponse()` | 获取响应 | Getter方法 |

### 57. RequestContext.java

**文件路径**: `src/main/java/com/xprobe/scanner/models/RequestContext.java`  
**类名**: `RequestContext`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `RequestContext(String, String, String, int)` | 构造函数 | 被RequestHandler调用 |
| `getToolType()` | 获取工具类型 | Getter方法 |
| `getMethod()` | 获取HTTP方法 | Getter方法 |
| `getUrl()` | 获取URL | Getter方法 |
| `getHashCode()` | 获取哈希码 | Getter方法 |

---

## 日志模块

### 58. LogModel.java

**文件路径**: `src/main/java/com/xprobe/scanner/Logs/LogModel.java`  
**类名**: `LogModel`  
**继承**: `AbstractTableModel`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `LogModel()` | 构造函数 | 被XProbe.initialize()调用 |
| `add(int, String, String, String, HttpRequest, HttpResponse, int, int, long, HttpRequest, HttpResponse, String)` | 添加日志条目 | 被TaskScheduler.logResult()调用 |
| `get(int)` | 获取日志条目 | 被ScanResultTab调用 |
| `getVulnerabilityCount()` | 获取漏洞数量 | 被ScanResultTab调用 |
| `clear()` | 清空日志 | 被ScanResultTab调用 |
| `addListener(LogListener)` | 添加监听器 | 被ScanResultTab调用 |
| `removeListener(LogListener)` | 移除监听器 | 被ScanResultTab调用 |
| `setMaxEntries(int)` | 设置最大条目数 | 被外部调用 |
| `getMaxEntries()` | 获取最大条目数 | Getter方法 |
| `getRowCount()` | 获取行数 | 被JTable调用 |
| `getColumnCount()` | 获取列数 | 被JTable调用 |
| `getColumnName(int)` | 获取列名 | 被JTable调用 |
| `getColumnClass(int)` | 获取列类型 | 被JTable调用 |
| `getValueAt(int, int)` | 获取单元格值 | 被JTable调用 |

#### 内部类

**LogEntry**
- 存储单条日志的所有信息

**LogListener**
- 接口，用于监听日志变更

### 59. LogHandler.java

**文件路径**: `src/main/java/com/xprobe/scanner/Logs/LogHandler.java`  
**类名**: `LogHandler`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `LogHandler()` | 构造函数 | 被外部调用 |

### 60. LogTab.java

**文件路径**: `src/main/java/com/xprobe/scanner/Logs/LogTab.java`  
**类名**: `LogTab`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `LogTab()` | 构造函数 | 被外部调用 |

---

## 其他模块

### 61. ParameterDataStorage.java

**文件路径**: `src/main/java/com/xprobe/scanner/active/ParameterDataStorage.java`  
**类名**: `ParameterDataStorage`

#### 公共方法

| 方法签名 | 功能说明 | 调用关系 |
|---------|---------|---------|
| `ParameterDataStorage()` | 构造函数 | 被ActiveProbeTab调用 |
| `getDataModel()` | 获取数据模型 | 被ActiveProbeTab调用 |
| `clear()` | 清空数据 | 被ActiveProbeTab调用 |
| `save(File)` | 保存数据 | 被ActiveProbeTab调用 |
| `load(File)` | 加载数据 | 被ActiveProbeTab调用 |

---

## 调用关系总结

### 核心调用路径图

```
1. 插件初始化路径:
   Burp Suite
   → XProbe.initialize()
   → XProbeConfigManager.initialize()
   → ConfigPersistence.load()
   → [创建所有组件]
   → api.http().registerHttpHandler(RequestHandler)
   → api.userInterface().registerSuiteTab(...)

2. HTTP请求处理路径:
   Burp Suite HTTP Handler
   → RequestHandler.handleHttpRequestToBeSent()
   → RealtimeScannerRefactored.processNewRequest()
   → ParameterCollector.collectFromRequest()

3. HTTP响应处理路径:
   Burp Suite HTTP Handler
   → RequestHandler.handleHttpResponseReceived()
   → OriginalResponseCache.put()
   → RealtimeScannerRefactored.processResponse()
   → RequestHandler.collectScanTasks()
   → TaskScheduler.scheduleScan()
   → UniversalScanner.scan()

4. 扫描执行路径:
   TaskScheduler.executeScanTask()
   → ScannerFactory.getScanner()
   → UniversalScanner.canScan()
   → UniversalScanner.scan()
   → UniversalScanner.evaluatePair()
   → PayloadVariableResolver.resolvePayload()
   → InjectionPointExecutor.applySingleInjection()
   → api.http().sendRequest()
   → UnifiedResponseEvaluator.evaluate()
   → TaskScheduler.logResult()
   → LogModel.add()

5. Arjun参数发现路径:
   RealtimeScannerRefactored.triggerManualArjunScan()
   → ArjunService.scan()
   → ParamDiscoveryEngine.scan()
   → ParamDiscoveryEngine.initialize()
   → ParamDiscoveryEngine.narrowDown()
   → ParamDiscoveryEngine.verify()
   → ArjunService.convertToArjunResult()
   → RealtimeScannerRefactored.notifyArjunResult()
   → ActiveProbeTab.onArjunResult()
```

### 关键依赖关系

1. **配置依赖链**:
   - `XProbeConfigManager` → `ConfigPersistence`
   - `XProbeConfigManager` → `RulePersistence`
   - `ConfigurationManager` → `ConfigPersistence`

2. **扫描依赖链**:
   - `TaskScheduler` → `ScannerFactory` → `UniversalScanner`
   - `UniversalScanner` → `RealtimeScannerRefactored` (去重)
   - `UniversalScanner` → `OriginalResponseCache` (原始响应)
   - `UniversalScanner` → `XProbeConfigManager` (配置)

3. **Arjun依赖链**:
   - `RealtimeScannerRefactored` → `ArjunService` → `ParamDiscoveryEngine`
   - `ParamDiscoveryEngine` → `BurpHttpRequester`
   - `ParamDiscoveryEngine` → `AnomalyDetector`
   - `ParamDiscoveryEngine` → `ChunkProcessor`

4. **参数收集依赖链**:
   - `RealtimeScannerRefactored` → `ParameterCollector`
   - `RealtimeScannerRefactored` → `ParameterManager`

5. **UI依赖链**:
   - `DashboardTab` → `TaskScheduler` (进度监听)
   - `DashboardTab` → `ParameterCollector` (统计)
   - `DashboardTab` → `ArjunService` (统计)
   - `ScanResultTab` → `LogModel` (结果显示)
   - `ActiveProbeTab` → `RealtimeScannerRefactored` (主动探测)

---

**文档结束**

> 本文档涵盖了XProbe项目的所有87个Java文件，1993+个方法，详细说明了每个函数的功能和调用关系。文档按模块组织，便于查找和理解代码结构。