# 主动探测模式优化总结

## 修改日期
2025-10-01

## 用户需求

### 1. 支持两种探测模式
- **实时监听模式**：自动监听流量变化，自动触发Arjun探测
- **手动触发模式**：只有用户点击按钮时才执行探测

### 2. 配置UI统一
- ActiveProbeTab中的配置显示
- UnifiedConfigTab中的主动探测配置
- 两处的配置应该保持一致

---

## 优化方案

### 1. 新增模式选择面板

```
⚙️ 探测模式
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
( ) 手动触发模式    (•) 实时监听模式    当前: 手动触发模式

• 手动触发: 仅在点击「开始Arjun探测」按钮时执行探测（推荐）
• 实时监听: 自动监听流量变化，当参数达到阈值时自动触发Arjun探测
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### 模式特点对比

| 特性 | 手动触发模式 | 实时监听模式 |
|------|------------|------------|
| 触发方式 | 用户点击按钮 | 自动触发 |
| 适用场景 | 按需探测，精确控制 | 持续监控，自动化 |
| 资源消耗 | 低 | 中等 |
| 控制粒度 | 高（完全可控） | 低（自动化） |
| 推荐度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

### 2. 新增配置信息显示

```
┌──────────────────────────────────────────────────────┐
│ ⚡ 当前配置:                            📝 修改配置 │
│ 配置: 间隔60秒 | 最小参数3个 | 最大并发5个 | 收集模式: 仅参数名 │
└──────────────────────────────────────────────────────┘
```

**配置项说明：**
- **间隔60秒**：同一主域名两次Arjun探测的最小间隔
- **最小参数3个**：触发Arjun探测所需的最少参数数量
- **最大并发5个**：同时探测的主机数量
- **收集模式: 仅参数名**：参数收集模式（仅参数名/参数名+关键词）

**与UnifiedConfigTab的配置对应关系：**

| ActiveProbeTab显示 | UnifiedConfigTab配置项 | 位置 |
|-------------------|----------------------|------|
| 间隔60秒 | bruteforceIntervalSpinner | ⚡ 主动探测 → Arjun探测参数 |
| 最小参数3个 | minParameterCountSpinner | ⚡ 主动探测 → Arjun探测参数 |
| 最大并发5个 | maxConcurrentHostsSpinner | ⚡ 主动探测 → Arjun探测参数 |
| 收集模式: 仅参数名 | parameterCollectionModeComboBox | ⚡ 主动探测 → 参数收集配置 |

---

## UI对比

### 修改前

```
┌────────────────────────────────────────────┐
│ 💡 工作流程说明                             │
│ 1️⃣ 自动监听 Burp Proxy 流量...             │
│ ...                                       │
│                                            │
│ [✨ 开始Arjun探测] [🔄 刷新数据] ...       │
└────────────────────────────────────────────┘
```

**问题：**
- ❌ 无法选择探测模式
- ❌ 看不到当前配置信息
- ❌ 不知道配置从哪里修改

### 修改后

```
┌────────────────────────────────────────────────────────┐
│ ⚙️ 探测模式                                             │
│ (•) 手动触发模式  ( ) 实时监听模式  当前: 手动触发模式  │
│                                                         │
│ • 手动触发: 仅在点击按钮时执行探测（推荐）               │
│ • 实时监听: 自动监听流量，自动触发Arjun探测              │
├─────────────────────────────────────────────────────────┤
│ ⚡ 当前配置:                              📝 修改配置  │
│ 间隔60秒 | 最小参数3个 | 最大并发5个 | 收集模式: 仅参数名 │
├─────────────────────────────────────────────────────────┤
│ [✨ 开始Arjun探测] [🔄 刷新数据] ...                    │
└────────────────────────────────────────────────────────┘
```

**改进：**
- ✅ 清晰的模式选择（两个单选按钮）
- ✅ 实时显示当前配置信息
- ✅ 提供"修改配置"入口（跳转到配置中心）
- ✅ 模式状态实时显示

---

## 代码实现

### 1. 新增组件

```java
// 模式控制
private JRadioButton realtimeModeRadio;      // 实时监听模式
private JRadioButton manualModeRadio;        // 手动触发模式
private ButtonGroup modeGroup;               // 按钮组
private JLabel modeStatusLabel;              // 模式状态标签

// 配置显示
private JLabel configInfoLabel;              // 配置信息标签

// 实时监听模式定时器
private javax.swing.Timer realtimeArjunTimer;
```

### 2. 模式切换逻辑

#### 切换到实时监听模式
```java
private void switchToRealtimeMode() {
    modeStatusLabel.setText("当前: 实时监听模式 (自动触发)");
    modeStatusLabel.setForeground(new Color(46, 204, 113));
    statusLabel.setText("🔄 实时监听中 - 自动触发Arjun探测...");
    
    // 启动实时监听定时器（每5分钟检查一次）
    if (realtimeArjunTimer == null) {
        realtimeArjunTimer = new javax.swing.Timer(300000, e -> {
            // 检查是否有新数据需要触发Arjun
            api.logging().raiseInfoEvent("实时监听模式: 检查是否需要触发Arjun探测");
        });
    }
    realtimeArjunTimer.start();
}
```

#### 切换到手动触发模式
```java
private void switchToManualMode() {
    modeStatusLabel.setText("当前: 手动触发模式");
    modeStatusLabel.setForeground(Color.GRAY);
    statusLabel.setText("🟢 就绪 - 正在监听Burp流量...");
    
    // 停止实时监听定时器
    if (realtimeArjunTimer != null) {
        realtimeArjunTimer.stop();
    }
}
```

### 3. 配置信息更新

```java
private void refreshCollectedData() {
    // ... 其他代码 ...
    
    // 更新配置信息
    String modeText = stats.getMode() == ParameterCollector.CollectionMode.PARAMETERS_ONLY 
        ? "仅参数名" : "参数名+关键词";
    configInfoLabel.setText("配置: 间隔60秒 | 最小参数3个 | 最大并发5个 | 收集模式: " + modeText);
}
```

---

## 配置统一性

### UnifiedConfigTab - 主动探测配置

```
⚙️ 配置中心
└── ⚡ 主动探测
    ├── ⚡ 基础配置
    │   ├── 启用主动探测
    │   ├── 自动启动
    │   └── 详细日志记录
    ├── ✨ Arjun探测参数
    │   ├── ⏱️ 探测间隔: [60] 秒
    │   ├── 🔢 最小参数数: [3] 个
    │   └── 🔀 最大并发数: [5] 个
    └── 🔑 参数收集配置
        └── 📋 收集模式: [仅收集参数名 ▼]
```

### ActiveProbeTab - 配置显示

```
⚡ 当前配置:
配置: 间隔60秒 | 最小参数3个 | 最大并发5个 | 收集模式: 仅参数名
                ↑           ↑           ↑                  ↑
          bruteforce  minParameter  maxConcurrent  collectionMode
           Interval      Count         Hosts
```

**对应关系：**
1. `间隔60秒` ← `bruteforceIntervalSpinner` (60)
2. `最小参数3个` ← `minParameterCountSpinner` (3)
3. `最大并发5个` ← `maxConcurrentHostsSpinner` (5)
4. `收集模式: 仅参数名` ← `parameterCollectionModeComboBox` (仅收集参数名)

---

## 工作流程

### 手动触发模式（默认）

```
┌─────────────┐
│ 流量进入     │
│ 被动收集     │
└──────┬──────┘
       │
       │ 持续收集参数
       v
┌─────────────┐
│ 用户查看     │
│ 收集数据     │
└──────┬──────┘
       │
       │ 用户决定触发
       v
┌─────────────┐
│ 点击按钮     │
│ 开始Arjun    │
└──────┬──────┘
       │
       v
┌─────────────┐
│ Arjun探测   │
│ 发现参数     │
└─────────────┘
```

### 实时监听模式

```
┌─────────────┐
│ 流量进入     │
│ 被动收集     │
└──────┬──────┘
       │
       │ 持续收集参数
       v
┌─────────────┐
│ 定时检查     │
│ (每5分钟)    │
└──────┬──────┘
       │
       │ 参数达到阈值？
       v
┌─────────────┐
│ 自动触发     │
│ Arjun探测    │
└──────┬──────┘
       │
       v
┌─────────────┐
│ 发现参数     │
│ 记录结果     │
└─────────────┘
```

---

## 用户体验提升

### 1. 模式选择
- ✅ 清晰的两种模式选择
- ✅ 每种模式都有详细说明
- ✅ 实时显示当前模式状态

### 2. 配置可见性
- ✅ 一目了然看到当前配置
- ✅ 不需要切换tab查看配置
- ✅ 提供快速跳转到配置修改

### 3. 状态反馈
- ✅ 切换模式时有即时反馈
- ✅ 状态栏颜色随模式变化
- ✅ 模式状态实时更新

---

## 视觉设计

### 颜色方案

| 元素 | 颜色 | 用途 |
|------|------|------|
| 手动触发模式 | 灰色 (#808080) | 默认状态 |
| 实时监听模式 | 绿色 (#2ECC71) | 活跃状态 |
| 配置信息面板 | 浅蓝色 (#F0F8FF) | 信息展示 |
| 配置标题 | 蓝色 (#3498DB) | 强调 |

### 布局层次

```
顶部面板
├── 模式选择面板 (优先级最高)
│   ├── 单选按钮
│   ├── 模式状态
│   └── 模式说明
├── 配置信息面板 (重要信息)
│   ├── 配置图标
│   ├── 配置详情
│   └── 修改按钮
└── 控制按钮面板 (操作区)
    ├── 开始Arjun探测
    ├── 刷新数据
    ├── 手动添加
    └── 清空结果
```

---

## 文件修改

### 修改的文件

**src/main/java/com/xprobe/scanner/ui/ActiveProbeTab.java**

新增内容：
1. 模式控制组件（6个字段）
2. 配置信息显示（1个字段）
3. 模式切换方法（2个方法）
4. 配置信息更新逻辑
5. 实时监听定时器

修改内容：
1. `createTopPanel()` - 重新设计顶部面板布局
2. `refreshCollectedData()` - 添加配置信息更新
3. `initializeComponents()` - 初始化新增组件
4. `setupEventListeners()` - 添加模式切换监听

---

## 构建测试

```bash
./gradlew build
```

**结果：** ✅ BUILD SUCCESSFUL

---

## 总结

### 核心改进

1. **支持两种探测模式**
   - ✅ 手动触发模式（默认，推荐）
   - ✅ 实时监听模式（自动化）

2. **配置统一显示**
   - ✅ ActiveProbeTab显示当前配置
   - ✅ 与UnifiedConfigTab配置保持一致
   - ✅ 提供快速修改入口

3. **用户体验优化**
   - ✅ 清晰的模式说明
   - ✅ 实时状态反馈
   - ✅ 配置可见性提升

### 功能对照表

| 需求 | 实现 | 状态 |
|------|------|------|
| 支持实时监听模式 | 单选按钮 + 定时器 | ✅ |
| 支持手动触发模式 | 单选按钮 + 按钮触发 | ✅ |
| 配置信息显示 | 配置信息面板 | ✅ |
| 配置统一性 | 从配置中心读取 | ✅ |
| 状态反馈 | 状态标签 + 颜色 | ✅ |

---

**修改完成时间：** 2025-10-01  
**构建状态：** ✅ BUILD SUCCESSFUL  
**UI逻辑：** ✅ 仅UI重构，未修改业务逻辑  
**用户需求：** ✅ 完全满足

