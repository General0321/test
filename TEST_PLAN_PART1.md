# 🧪 XProbe 测试计划 - 第1部分

## 📋 测试范围
1. 配置管理器重构
2. 严重问题修复
3. 扫描结果显示修改后的流量
4. UI优化

---

## 🎯 测试环境准备

```bash
# 1. 确保JAR包是最新的
cd /Users/0x7llcf/Desktop/tools/burp插件/XProbe
./gradlew clean jar

# 2. 备份配置（可选）
cp ~/.config/xprobe/config.json ~/.config/xprobe/config.json.backup
```

---

## 测试组1：配置管理器基础功能

### ✅ 测试1.1：正常初始化
**步骤**：
1. 启动Burp，加载XProbe插件
2. 查看Burp日志

**预期**：
- `✅ 配置管理器初始化成功`
- `✅ XProbe 插件初始化完成`

---

### ✅ 测试1.2：配置文件不存在
**步骤**：
```bash
mv ~/.config/xprobe/config.json ~/.config/xprobe/config.json.bak
```
重启Burp

**预期**：
- `⚠️ 配置加载失败，使用默认配置`
- `✅ 默认配置已保存`
- 插件不崩溃

---

### ✅ 测试1.3：配置文件损坏
**步骤**：
```bash
echo "invalid" > ~/.config/xprobe/config.json
```
重启Burp

**预期**：
- 使用默认配置
- 配置文件被修复
- 插件正常工作

---

## 测试组2：配置保存和线程安全

### ✅ 测试2.1：修改被动扫描总开关
**步骤**：
1. 打开XProbe Tab → 被动扫描规则
2. 点击顶部"被动扫描总开关"按钮

**预期**：
- 按钮变色（绿↔红）
- 文字变化（启用↔禁用）
- 日志：`✅ 被动扫描已XXX`

---

### ✅ 测试2.2：修改全局注入模式
**步骤**：
1. 切换"全局注入模式"下拉框

**预期**：
- 选项改变
- 日志：`✅ 全局注入模式已设置为: XXX`

---

### ✅ 测试2.3：配置文件权限问题
**步骤**：
```bash
chmod 444 ~/.config/xprobe/config.json
```
尝试修改配置

**预期**：
- 显示错误对话框
- UI状态回滚
- 日志：`❌ 保存失败`

**清理**：
```bash
chmod 644 ~/.config/xprobe/config.json
```

---

## 测试组3：扫描功能

### ✅ 测试3.1：添加简单规则
**步骤**：
1. 点击"添加规则"
2. 填写：
   - 规则名称：`readfile`
   - 匹配：参数名 = `file`
   - 注入：参数值 = `58513`
   - 响应：响应体包含 `404`
3. 保存

**预期**：
- 规则出现在列表
- 注入点数：1
- Payload数：1

---

### ✅ 测试3.2：扫描结果显示修改后的URL ⭐重要
**步骤**：
1. 确保被动扫描启用
2. 设置为"记录所有流量"
3. 发送请求：`GET /2.php?file=1`
4. 查看扫描结果

**预期**：
- URL显示：`/2.php?file=58513` ✅ **不是file=1**
- 命中规则：`readfile`
- 响应码：404

---

### ✅ 测试3.3：仅记录命中模式
**步骤**：
1. 切换为"仅记录命中"
2. 发送两个请求：
   - `GET /2.php?file=1` （命中）
   - `GET /2.php?id=1` （不命中）

**预期**：
- 只显示第一个请求
- 命中规则：`readfile`

---

### ✅ 测试3.4：记录所有流量模式
**步骤**：
1. 切换为"记录所有流量"
2. 发送两个请求（同上）

**预期**：
- 显示两个请求
- 第一个有规则名
- 第二个规则名为空

---

## 测试组4：UI优化

### ✅ 测试4.1：顶部控制面板布局 ⭐重要
**步骤**：
1. 打开被动扫描规则Tab
2. 观察顶部面板

**预期效果**：
- ✅ 柔和的浅蓝色背景
- ✅ 淡蓝色边框
- ✅ 三个区域清晰分隔（竖线）
- ✅ 每个区域：标题（加粗）+ 控件 + 提示（小字）
- ✅ 间距合理，不拥挤

---

### ✅ 测试4.2：文字优化
**预期文字**：
- `全局注入模式`（无冒号）
- `各规则可单独设置`（灰色）
- `扫描结果记录`（无冒号）
- `仅命中可节省资源`（橙色）

---

## 📝 快速测试清单（5分钟）

**必测项目**：
- [ ] 插件正常加载
- [ ] 被动扫描开关能切换
- [ ] 能添加规则
- [ ] 扫描结果显示payload后的URL ⭐
- [ ] 顶部UI布局清晰美观 ⭐
- [ ] 配置能保存

**测试结果**：___

---

**测试人员**：___  
**测试日期**：___  
**版本**：XProbe 1.0.0


## 📋 测试范围
1. 配置管理器重构
2. 严重问题修复
3. 扫描结果显示修改后的流量
4. UI优化

---

## 🎯 测试环境准备

```bash
# 1. 确保JAR包是最新的
cd /Users/0x7llcf/Desktop/tools/burp插件/XProbe
./gradlew clean jar

# 2. 备份配置（可选）
cp ~/.config/xprobe/config.json ~/.config/xprobe/config.json.backup
```

---

## 测试组1：配置管理器基础功能

### ✅ 测试1.1：正常初始化
**步骤**：
1. 启动Burp，加载XProbe插件
2. 查看Burp日志

**预期**：
- `✅ 配置管理器初始化成功`
- `✅ XProbe 插件初始化完成`

---

### ✅ 测试1.2：配置文件不存在
**步骤**：
```bash
mv ~/.config/xprobe/config.json ~/.config/xprobe/config.json.bak
```
重启Burp

**预期**：
- `⚠️ 配置加载失败，使用默认配置`
- `✅ 默认配置已保存`
- 插件不崩溃

---

### ✅ 测试1.3：配置文件损坏
**步骤**：
```bash
echo "invalid" > ~/.config/xprobe/config.json
```
重启Burp

**预期**：
- 使用默认配置
- 配置文件被修复
- 插件正常工作

---

## 测试组2：配置保存和线程安全

### ✅ 测试2.1：修改被动扫描总开关
**步骤**：
1. 打开XProbe Tab → 被动扫描规则
2. 点击顶部"被动扫描总开关"按钮

**预期**：
- 按钮变色（绿↔红）
- 文字变化（启用↔禁用）
- 日志：`✅ 被动扫描已XXX`

---

### ✅ 测试2.2：修改全局注入模式
**步骤**：
1. 切换"全局注入模式"下拉框

**预期**：
- 选项改变
- 日志：`✅ 全局注入模式已设置为: XXX`

---

### ✅ 测试2.3：配置文件权限问题
**步骤**：
```bash
chmod 444 ~/.config/xprobe/config.json
```
尝试修改配置

**预期**：
- 显示错误对话框
- UI状态回滚
- 日志：`❌ 保存失败`

**清理**：
```bash
chmod 644 ~/.config/xprobe/config.json
```

---

## 测试组3：扫描功能

### ✅ 测试3.1：添加简单规则
**步骤**：
1. 点击"添加规则"
2. 填写：
   - 规则名称：`readfile`
   - 匹配：参数名 = `file`
   - 注入：参数值 = `58513`
   - 响应：响应体包含 `404`
3. 保存

**预期**：
- 规则出现在列表
- 注入点数：1
- Payload数：1

---

### ✅ 测试3.2：扫描结果显示修改后的URL ⭐重要
**步骤**：
1. 确保被动扫描启用
2. 设置为"记录所有流量"
3. 发送请求：`GET /2.php?file=1`
4. 查看扫描结果

**预期**：
- URL显示：`/2.php?file=58513` ✅ **不是file=1**
- 命中规则：`readfile`
- 响应码：404

---

### ✅ 测试3.3：仅记录命中模式
**步骤**：
1. 切换为"仅记录命中"
2. 发送两个请求：
   - `GET /2.php?file=1` （命中）
   - `GET /2.php?id=1` （不命中）

**预期**：
- 只显示第一个请求
- 命中规则：`readfile`

---

### ✅ 测试3.4：记录所有流量模式
**步骤**：
1. 切换为"记录所有流量"
2. 发送两个请求（同上）

**预期**：
- 显示两个请求
- 第一个有规则名
- 第二个规则名为空

---

## 测试组4：UI优化

### ✅ 测试4.1：顶部控制面板布局 ⭐重要
**步骤**：
1. 打开被动扫描规则Tab
2. 观察顶部面板

**预期效果**：
- ✅ 柔和的浅蓝色背景
- ✅ 淡蓝色边框
- ✅ 三个区域清晰分隔（竖线）
- ✅ 每个区域：标题（加粗）+ 控件 + 提示（小字）
- ✅ 间距合理，不拥挤

---

### ✅ 测试4.2：文字优化
**预期文字**：
- `全局注入模式`（无冒号）
- `各规则可单独设置`（灰色）
- `扫描结果记录`（无冒号）
- `仅命中可节省资源`（橙色）

---

## 📝 快速测试清单（5分钟）

**必测项目**：
- [ ] 插件正常加载
- [ ] 被动扫描开关能切换
- [ ] 能添加规则
- [ ] 扫描结果显示payload后的URL ⭐
- [ ] 顶部UI布局清晰美观 ⭐
- [ ] 配置能保存

**测试结果**：___

---

**测试人员**：___  
**测试日期**：___  
**版本**：XProbe 1.0.0

