主动探测部分分为
目标选择，可以自由选择一个或多个，同一主域下的或者不同子域下的

接口来源选择，可以自由选择接口来源，包括手动输入、自动采集、复用缓存
接口来源范围，按照选中的目标和接口来源来获取接口，根据目标选择的目标来进行获取，是只获取选择目标的，还是获取选中目标主域的接口，这里同时也要遵循接口来源。如果复用缓存就从缓存里面按照选中的范围来获取，如果是自动采集那么采集的时候也按照选中的范围来获取；如果是手动输入就没有接口来源范围。

参数来源选择，按照选中的目标来获取参数，是只获取选择目标的，还是获取选中目标主域的参数，按照配置来决定

目标选择
接口来源选择
接口来源范围
参数来源选择

可以理解为按照层级工作的，去每一层获取资源，自顶向下，但是整体的动作都是一样的

以选中目标为核心、逐层独立取资源



“以选中目标为核心、逐层独立取资源、AUTO 用 SiteMap”的设计，我已对实现与 UI 进行对齐和落地，整体流程如下，且已满足“层级独立、最终按策略执行”的要求。

实现确认（自顶向下分层）
1) 目标选择
- 支持选择多个主域/子域（可跨主域）。
- 后续所有层均仅在这些“选中目标”范围内工作（核心参考）。

2) 接口来源选择
- MANUAL（手动输入相对路径）
  - 对每个选中目标子域，将相对路径（如 /api/login、/admin）拼接为完整 URL：
    - scheme/port 优先从该子域已收集模板推断，否则回退 https:443。
  - 无“接口来源范围”这一项（UI已禁用）。
- AUTO（自动采集）
  - 仅从 SiteMap 获取（已按你要求确认）。
  - 获取时“按接口来源范围”过滤：
    - 仅选中子域接口：只接收在“选中子域集合”的请求/响应。
    - 主域所有子域接口：接收在“选中主域的所有子域”的请求/响应。
  - 将采集到的接口与参数写入缓存（ParameterCollector），与后续策略联动一致。
- COLLECTED（复用缓存）
  - 从缓存取接口，且“按接口来源范围”过滤（仅选中子域 / 主域所有子域）。

3) 接口来源范围（只影响“取接口”的范围）
- MANUAL：无此项（已禁用）。
- AUTO：已在 SiteMap 采集阶段使用所选范围过滤。
- COLLECTED：已在缓存接口过滤阶段使用所选范围。

4) 参数来源选择（只影响“取参数”的范围）
- 三来源下均生效（MANUAL/AUTO/COLLECTED）：
  - 仅选中子域参数：使用 getParametersForHost(host)。
  - 主域所有子域参数：使用 getParametersForMainDomain(mainDomain)，若收集模式为“参数+关键词”则合并关键词。
- 说明：在 MANUAL 来源时，之前参数范围未生效的问题已修复（现在手动相对路径也会按参数范围取参数）。

5) 最终策略（STEP 4）
- INTERFACE_ONLY：仅对“取到的接口”做存在性验证（含随机路径基线判断）。
- INTERFACE_THEN_ARJUN：先接口验证，后参数探测（参数按参数来源范围）。
- ARJUN_ONLY：直接参数探测（参数按参数来源范围）。
- 结果写表：
  - 接口探测：ActiveProbeTab.addInterfaceDiscoveryResult（类型=接口探测，状态=✅/❌）。
  - 参数探测：ActiveProbeTab.addArjunResultToTable（类型=参数探测，未发现参数也写“⚠️ 未发现参数”）。

本次关键修订（已落地）
- MANUAL + 参数范围：生效（此前未生效的问题已修复）。
- MANUAL 相对路径：对选中子域逐一拼接为完整 URL 后执行。
- AUTO 接口采集：新增从 SiteMap 获取并按“接口来源范围”筛选（满足“自动采集就是从 SiteMap/Proxy 中获取，范围即获取范围”的设计；历史 Proxy 专门列表不需额外支持，SiteMap 已覆盖）。
- UI 联动：
  - MANUAL：接口来源范围禁用，参数来源范围启用。
  - ARJUN_ONLY 默认选中“直接参数探测”。

你强调的“每层独立、取资源范围由目标+来源/范围决定”的行为，现已满足：
- 层独立：目标选择 → 接口来源/范围 → 参数来源 → 最终策略，彼此独立，范围只影响“取资源”的范围，不改变整体动作。
- 管道一致：最终都以“选中目标 + 取到的接口 + 取到的参数”为输入，执行策略并写表。

小提示/后续可选优化（非必须）
- 请求构造（克隆替代重建）：当前某些主动请求使用重建方式，可能丢会话上下文/手写 Content-Length 风险。建议后续改为“克隆原始请求仅替换 Host/Service + withBody 自动长度 + IPv6 [addr]:port 规范”，准确性更高。
- AUTO 只用 SiteMap 已符合你的要求；若未来希望对“实时 Proxy 历史”也跑一遍，可另加选项。

快速自测清单
- 勾选目标：两主域多个子域
- MANUAL:
  - 输入 /api/login、/admin → 参数范围切换（仅选中子域参数/主域所有子域参数），观察参数数与探测结果变化
  - 策略切换：仅接口/接口后参数/仅参数 → 表格应出现对应“接口探测/参数探测”行
- AUTO:
  - 接口范围切换：仅选中子域接口 vs 主域所有子域接口 → 观察“自动接口采集完成(范围=...)”日志与接口数变化
  - 参数范围切换：观察增量参数与结果行变化
- COLLECTED:
  - 与 AUTO 同步骤，但来源为缓存数据

如需，我可以顺手把“请求构造（克隆替代重建）”的修复也提上来，这会让对跨子域/保留会话的探测更稳定。