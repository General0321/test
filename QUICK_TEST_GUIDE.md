# ⚡ XProbe 快速测试指南

## 🚀 开始测试（10分钟）

### 步骤0：准备环境
```bash
cd /Users/0x7llcf/Desktop/tools/burp插件/XProbe
./gradlew clean jar
```
在Burp中加载 `build/libs/XProbe-1.0.0.jar`

---

## ✅ 核心测试（必做）

### 1️⃣ 配置初始化测试（1分钟）

**测试什么**：验证配置管理器能正常工作

**怎么测**：
1. 查看Burp日志

**看什么**：
- ✅ `配置管理器初始化成功`
- ✅ `XProbe 插件初始化完成`
- ❌ 无错误或异常

**如果失败**：检查 `CRITICAL_FIXES_COMPLETE.md` 第1个问题

---

### 2️⃣ UI优化测试（1分钟）⭐

**测试什么**：验证被动扫描规则Tab的UI改进

**怎么测**：
1. 打开XProbe Tab → 被动扫描规则
2. 观察顶部控制面板

**看什么**：
- ✅ 浅蓝色背景 + 边框
- ✅ 三个区域清晰分隔（竖线）
- ✅ 文字清晰：
  ```
  全局注入模式
  [批量模式（快速）] 各规则可单独设置
  ```
- ✅ 间距舒适，不拥挤

**截图对比**：
- 修复前：拥挤，一行挤满
- 修复后：分组明确，层次清晰

**如果失败**：检查 `UI_AND_LOG_IMPROVEMENTS.md`

---

### 3️⃣ 配置保存测试（2分钟）

**测试什么**：验证配置能正确保存和恢复

**怎么测**：
1. 点击被动扫描总开关（禁用）
2. 查看按钮变红色，文字变"已禁用"
3. 重启Burp
4. 查看设置是否保留

**看什么**：
- ✅ 按钮立即响应（变色）
- ✅ 日志：`✅ 被动扫描已禁用`
- ✅ 重启后状态保留

**如果失败**：检查 `CRITICAL_FIXES_COMPLETE.md` 第2个问题

---

### 4️⃣ 添加规则测试（2分钟）

**测试什么**：验证规则添加功能

**怎么测**：
1. 点击"添加规则"
2. 填写：
   - **规则名称**：`testfile`
   - **简单模式**
   - **请求配置**：参数名 = `file`（完全匹配）
   - **注入配置**：参数值，payload = `testpayload123`
   - **响应匹配**：响应码 = `404`
3. 保存

**看什么**：
- ✅ 规则出现在列表
- ✅ 注入点数：1
- ✅ Payload数：1
- ✅ 状态：启用

---

### 5️⃣ 扫描结果测试（3分钟）⭐⭐⭐

**测试什么**：验证扫描结果显示修改后的URL

**怎么测**：
1. 确保被动扫描启用
2. 设置"扫描结果记录"为"记录所有流量"
3. 使用Burp Proxy发送：
   ```
   GET /2.php?file=1 HTTP/1.1
   Host: 192.168.1.7:81
   ```
4. 打开XProbe Tab → 扫描结果
5. 查看表格中的URL

**看什么**：
- ✅ URL显示：`http://192.168.1.7:81/2.php?file=testpayload123`
- ❌ **不是** `file=1`（原始值）
- ✅ 命中规则列显示：`testfile`
- ✅ 响应码：404

**这是最重要的测试！！！**

**截图示例**：
```
| Method | URL                                    | 命中规则  |
|--------|----------------------------------------|----------|
| GET    | .../2.php?file=testpayload123         | testfile |
         ^^^^^^^^^^^^^^^^^^^^^^^^^ 
         ✅ 这里应该是payload，不是原始的file=1
```

**如果失败**：检查 `UI_AND_LOG_IMPROVEMENTS.md` 第1个问题

---

### 6️⃣ 过滤功能测试（1分钟）

**测试什么**：验证扫描结果过滤

**怎么测**：
1. 继续发送一个不匹配的请求：`GET /2.php?id=1`
2. 在扫描结果Tab中，找到"命中规则"下拉框
3. 选择"所有命中"

**看什么**：
- ✅ 只显示命中的记录（file=testpayload123）
- ✅ 不显示未命中的记录（id=1）

---

## 🐛 异常情况测试（可选）

### 测试A：配置文件不存在
```bash
mv ~/.config/xprobe/config.json ~/.config/xprobe/config.json.bak
```
重启Burp

**预期**：
- ✅ 插件不崩溃
- ✅ 自动创建默认配置
- ✅ 日志：`使用默认配置`

**恢复**：
```bash
mv ~/.config/xprobe/config.json.bak ~/.config/xprobe/config.json
```

---

### 测试B：配置保存失败
```bash
chmod 444 ~/.config/xprobe/config.json
```
尝试切换被动扫描开关

**预期**：
- ✅ 显示错误对话框
- ✅ UI状态回滚
- ✅ 日志：`保存失败`

**恢复**：
```bash
chmod 644 ~/.config/xprobe/config.json
```

---

## 📊 测试结果记录

### 核心功能测试
- [ ] 1️⃣ 配置初始化
- [ ] 2️⃣ UI优化
- [ ] 3️⃣ 配置保存
- [ ] 4️⃣ 添加规则
- [ ] 5️⃣ 扫描结果显示payload ⭐⭐⭐
- [ ] 6️⃣ 过滤功能

### 异常情况测试
- [ ] A. 配置文件不存在
- [ ] B. 配置保存失败

### 总体评价
- **通过率**：___/6
- **是否可用**：[ ] 是 [ ] 否
- **主要问题**：___

---

## 🎯 重点关注

### 最重要的两个测试
1. **测试5️⃣**：扫描结果显示payload后的URL
   - 这是本次更新的核心功能
   - 必须通过！

2. **测试2️⃣**：UI优化效果
   - 用户体验的直观改进
   - 应该明显优于之前

---

## 📸 预期效果截图说明

### UI优化前后对比
**修复前**：
```
[开关]|全局注入模式:[下拉](说明)|结果记录:[下拉](说明)
      拥挤，难以阅读
```

**修复后**：
```
┌────────────────────────────────────────────────┐
│ [开关] | 全局注入模式     | 扫描结果记录      │
│        | [下拉]          | [下拉]           │
│        | 提示文字        | 提示文字         │
└────────────────────────────────────────────────┘
清晰分组，层次分明
```

### 扫描结果前后对比
**修复前**：
```
URL: http://192.168.1.7:81/2.php?file=1
     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ 原始值
```

**修复后**：
```
URL: http://192.168.1.7:81/2.php?file=testpayload123
                                  ^^^^^^^^^^^^^^^^^ payload
```

---

## ❓ 常见问题

**Q1：插件加载失败？**
A：检查JAR包是否最新，重新执行 `./gradlew clean jar`

**Q2：扫描结果中URL还是原始值？**
A：检查规则是否正确添加，被动扫描是否启用

**Q3：UI没有变化？**
A：确认加载的是最新的JAR包，不是旧版本

**Q4：配置无法保存？**
A：检查配置文件权限：`ls -la ~/.config/xprobe/config.json`

---

## 📞 问题反馈

如果测试失败，请提供：
1. **失败的测试编号**（如：测试5️⃣）
2. **实际结果**（截图或描述）
3. **Burp日志**（相关错误信息）
4. **操作系统**：macOS / Windows / Linux

---

**预计测试时间**：10分钟  
**关键测试**：测试5️⃣（扫描结果）、测试2️⃣（UI优化）  
**测试愉快！🎉**


## 🚀 开始测试（10分钟）

### 步骤0：准备环境
```bash
cd /Users/0x7llcf/Desktop/tools/burp插件/XProbe
./gradlew clean jar
```
在Burp中加载 `build/libs/XProbe-1.0.0.jar`

---

## ✅ 核心测试（必做）

### 1️⃣ 配置初始化测试（1分钟）

**测试什么**：验证配置管理器能正常工作

**怎么测**：
1. 查看Burp日志

**看什么**：
- ✅ `配置管理器初始化成功`
- ✅ `XProbe 插件初始化完成`
- ❌ 无错误或异常

**如果失败**：检查 `CRITICAL_FIXES_COMPLETE.md` 第1个问题

---

### 2️⃣ UI优化测试（1分钟）⭐

**测试什么**：验证被动扫描规则Tab的UI改进

**怎么测**：
1. 打开XProbe Tab → 被动扫描规则
2. 观察顶部控制面板

**看什么**：
- ✅ 浅蓝色背景 + 边框
- ✅ 三个区域清晰分隔（竖线）
- ✅ 文字清晰：
  ```
  全局注入模式
  [批量模式（快速）] 各规则可单独设置
  ```
- ✅ 间距舒适，不拥挤

**截图对比**：
- 修复前：拥挤，一行挤满
- 修复后：分组明确，层次清晰

**如果失败**：检查 `UI_AND_LOG_IMPROVEMENTS.md`

---

### 3️⃣ 配置保存测试（2分钟）

**测试什么**：验证配置能正确保存和恢复

**怎么测**：
1. 点击被动扫描总开关（禁用）
2. 查看按钮变红色，文字变"已禁用"
3. 重启Burp
4. 查看设置是否保留

**看什么**：
- ✅ 按钮立即响应（变色）
- ✅ 日志：`✅ 被动扫描已禁用`
- ✅ 重启后状态保留

**如果失败**：检查 `CRITICAL_FIXES_COMPLETE.md` 第2个问题

---

### 4️⃣ 添加规则测试（2分钟）

**测试什么**：验证规则添加功能

**怎么测**：
1. 点击"添加规则"
2. 填写：
   - **规则名称**：`testfile`
   - **简单模式**
   - **请求配置**：参数名 = `file`（完全匹配）
   - **注入配置**：参数值，payload = `testpayload123`
   - **响应匹配**：响应码 = `404`
3. 保存

**看什么**：
- ✅ 规则出现在列表
- ✅ 注入点数：1
- ✅ Payload数：1
- ✅ 状态：启用

---

### 5️⃣ 扫描结果测试（3分钟）⭐⭐⭐

**测试什么**：验证扫描结果显示修改后的URL

**怎么测**：
1. 确保被动扫描启用
2. 设置"扫描结果记录"为"记录所有流量"
3. 使用Burp Proxy发送：
   ```
   GET /2.php?file=1 HTTP/1.1
   Host: 192.168.1.7:81
   ```
4. 打开XProbe Tab → 扫描结果
5. 查看表格中的URL

**看什么**：
- ✅ URL显示：`http://192.168.1.7:81/2.php?file=testpayload123`
- ❌ **不是** `file=1`（原始值）
- ✅ 命中规则列显示：`testfile`
- ✅ 响应码：404

**这是最重要的测试！！！**

**截图示例**：
```
| Method | URL                                    | 命中规则  |
|--------|----------------------------------------|----------|
| GET    | .../2.php?file=testpayload123         | testfile |
         ^^^^^^^^^^^^^^^^^^^^^^^^^ 
         ✅ 这里应该是payload，不是原始的file=1
```

**如果失败**：检查 `UI_AND_LOG_IMPROVEMENTS.md` 第1个问题

---

### 6️⃣ 过滤功能测试（1分钟）

**测试什么**：验证扫描结果过滤

**怎么测**：
1. 继续发送一个不匹配的请求：`GET /2.php?id=1`
2. 在扫描结果Tab中，找到"命中规则"下拉框
3. 选择"所有命中"

**看什么**：
- ✅ 只显示命中的记录（file=testpayload123）
- ✅ 不显示未命中的记录（id=1）

---

## 🐛 异常情况测试（可选）

### 测试A：配置文件不存在
```bash
mv ~/.config/xprobe/config.json ~/.config/xprobe/config.json.bak
```
重启Burp

**预期**：
- ✅ 插件不崩溃
- ✅ 自动创建默认配置
- ✅ 日志：`使用默认配置`

**恢复**：
```bash
mv ~/.config/xprobe/config.json.bak ~/.config/xprobe/config.json
```

---

### 测试B：配置保存失败
```bash
chmod 444 ~/.config/xprobe/config.json
```
尝试切换被动扫描开关

**预期**：
- ✅ 显示错误对话框
- ✅ UI状态回滚
- ✅ 日志：`保存失败`

**恢复**：
```bash
chmod 644 ~/.config/xprobe/config.json
```

---

## 📊 测试结果记录

### 核心功能测试
- [ ] 1️⃣ 配置初始化
- [ ] 2️⃣ UI优化
- [ ] 3️⃣ 配置保存
- [ ] 4️⃣ 添加规则
- [ ] 5️⃣ 扫描结果显示payload ⭐⭐⭐
- [ ] 6️⃣ 过滤功能

### 异常情况测试
- [ ] A. 配置文件不存在
- [ ] B. 配置保存失败

### 总体评价
- **通过率**：___/6
- **是否可用**：[ ] 是 [ ] 否
- **主要问题**：___

---

## 🎯 重点关注

### 最重要的两个测试
1. **测试5️⃣**：扫描结果显示payload后的URL
   - 这是本次更新的核心功能
   - 必须通过！

2. **测试2️⃣**：UI优化效果
   - 用户体验的直观改进
   - 应该明显优于之前

---

## 📸 预期效果截图说明

### UI优化前后对比
**修复前**：
```
[开关]|全局注入模式:[下拉](说明)|结果记录:[下拉](说明)
      拥挤，难以阅读
```

**修复后**：
```
┌────────────────────────────────────────────────┐
│ [开关] | 全局注入模式     | 扫描结果记录      │
│        | [下拉]          | [下拉]           │
│        | 提示文字        | 提示文字         │
└────────────────────────────────────────────────┘
清晰分组，层次分明
```

### 扫描结果前后对比
**修复前**：
```
URL: http://192.168.1.7:81/2.php?file=1
     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ 原始值
```

**修复后**：
```
URL: http://192.168.1.7:81/2.php?file=testpayload123
                                  ^^^^^^^^^^^^^^^^^ payload
```

---

## ❓ 常见问题

**Q1：插件加载失败？**
A：检查JAR包是否最新，重新执行 `./gradlew clean jar`

**Q2：扫描结果中URL还是原始值？**
A：检查规则是否正确添加，被动扫描是否启用

**Q3：UI没有变化？**
A：确认加载的是最新的JAR包，不是旧版本

**Q4：配置无法保存？**
A：检查配置文件权限：`ls -la ~/.config/xprobe/config.json`

---

## 📞 问题反馈

如果测试失败，请提供：
1. **失败的测试编号**（如：测试5️⃣）
2. **实际结果**（截图或描述）
3. **Burp日志**（相关错误信息）
4. **操作系统**：macOS / Windows / Linux

---

**预计测试时间**：10分钟  
**关键测试**：测试5️⃣（扫描结果）、测试2️⃣（UI优化）  
**测试愉快！🎉**

