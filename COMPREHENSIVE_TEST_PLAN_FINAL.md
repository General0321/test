# XProbe 全面测试计划

## 📋 测试概述

本测试计划涵盖XProbe插件的所有模块和功能,结合实际使用场景(大量数据包、高并发、长时间运行等),确保插件的稳定性、性能和正确性。

**测试时间**: 2025-10-03
**测试范围**: 所有核心模块、UI组件、配置管理、Arjun参数发现、被动扫描
**测试方法**: 功能测试、集成测试、压力测试、边界测试

---

## 🎯 测试模块清单

### 1. 核心架构测试

#### 1.1 插件初始化 (XProbe.java)
**测试项**:
- [ ] **T1.1.1** 插件首次加载,配置文件不存在 → 应创建默认配置
- [ ] **T1.1.2** 插件加载,配置文件损坏 → 应使用默认配置并记录错误
- [ ] **T1.1.3** 插件加载,配置文件正常 → 应正确加载所有配置项
- [ ] **T1.1.4** 插件卸载 → 应正确关闭所有线程池和资源
- [ ] **T1.1.5** 插件重载 → 应清理旧资源并重新初始化

**测试数据**:
```
场景1: 首次安装(无配置文件)
场景2: 配置文件格式错误(JSON格式损坏)
场景3: 配置文件包含100+规则,1000+参数
场景4: 插件运行10分钟后卸载
场景5: 插件卸载再重新加载
```

**预期问题**:
- ❌ 配置文件过大时加载性能问题
- ❌ 资源清理不完整导致内存泄漏
- ❌ 重载时旧线程未关闭

---

#### 1.2 配置管理 (XProbeConfig, XProbeConfigManager)
**测试项**:
- [ ] **T1.2.1** 配置持久化 → 保存后重启插件应保留配置
- [ ] **T1.2.2** 配置并发读写 → 多线程同时访问配置不应崩溃
- [ ] **T1.2.3** 配置热更新 → UI修改配置应立即生效
- [ ] **T1.2.4** 配置回滚 → 配置保存失败应回滚到旧配置
- [ ] **T1.2.5** 配置校验 → 无效配置应被拒绝并提示

**测试数据**:
```
场景1: 保存1000条规则,重启Burp验证
场景2: 10个线程同时读写配置
场景3: UI修改黑名单,立即测试过滤效果
场景4: 磁盘空间不足时保存配置
场景5: Arjun参数设置为负数/超范围值
```

**预期问题**:
- ❌ 并发写入导致配置文件损坏
- ❌ 配置未校验导致运行时异常
- ❌ 配置文件过大(>10MB)导致序列化失败

---

### 2. HTTP请求处理测试

#### 2.1 请求过滤 (RequestFilter, GlobalFilter)
**测试项**:
- [ ] **T2.1.1** 白名单过滤 → 仅处理白名单中的URL
- [ ] **T2.1.2** 黑名单过滤 → 不处理黑名单中的URL
- [ ] **T2.1.3** 白名单+黑名单组合 → 黑名单优先级高于白名单
- [ ] **T2.1.4** 正则表达式匹配 → 支持复杂正则表达式
- [ ] **T2.1.5** 大量规则性能 → 1000条黑白名单规则不应影响性能

**测试数据**:
```
白名单: ["https://example.com", ".*\.target\.com"]
黑名单: [".jpg", ".css", ".js", ".png"]
请求: 
  - https://example.com/api/test (✓ 应处理)
  - https://other.com/api (✗ 应跳过)
  - https://example.com/image.jpg (✗ 应跳过,黑名单)
  - https://sub.target.com/api (✓ 应处理,正则匹配)
```

**高并发测试**:
- 10000个请求/分钟,验证过滤器性能
- 黑名单1000条规则,测试匹配性能

**预期问题**:
- ❌ 正则表达式编译未缓存,每次请求重新编译
- ❌ 大量规则导致线性查找性能下降
- ❌ 特殊字符未转义导致正则错误

---

#### 2.2 请求去重 (RealtimeScannerRefactored.checkAndMarkPassiveScanProcessed)
**测试项**:
- [ ] **T2.2.1** 基础去重 → 相同请求不应重复扫描
- [ ] **T2.2.2** 颗粒度-HOST → 不同host相同path应分别扫描
- [ ] **T2.2.3** 颗粒度-PATH → 相同host不同path应分别扫描
- [ ] **T2.2.4** 颗粒度-PARAM → 相同接口不同参数应分别扫描
- [ ] **T2.2.5** 去重集合内存占用 → 长时间运行不应OOM

**测试数据**:
```
请求序列:
1. GET https://api.example.com/user?id=1
2. GET https://api.example.com/user?id=2 (相同path,不同参数值)
3. GET https://api.example.com/order?id=1 (不同path)
4. POST https://api.example.com/user (相同path,不同method)

颗粒度=HOST_PATH_PARAM: 1,3,4应扫描, 2应跳过
颗粒度=HOST_PATH: 1,3,4应扫描, 2也应跳过
```

**压力测试**:
- 100万个请求,验证去重集合大小
- 监控内存占用,应<500MB

**预期问题**:
- ❌ 去重集合无限增长导致OOM
- ❌ 去重key生成不一致导致重复扫描
- ❌ ConcurrentHashMap并发问题

---

### 3. 被动扫描测试

#### 3.1 UniversalScanner 扫描器
**测试项**:
- [ ] **T3.1.1** 配对架构-请求匹配 → 正确匹配HTTP请求元素
- [ ] **T3.1.2** 配对架构-响应匹配 → 正确匹配HTTP响应元素
- [ ] **T3.1.3** 批量注入模式 → 所有参数同时注入相同payload
- [ ] **T3.1.4** 逐个注入模式 → 每个参数分别注入
- [ ] **T3.1.5** 配对逻辑表达式 → AND/OR/NOT逻辑正确

**测试用例**:
```yaml
# 测试用例1: SQL注入检测
规则: SQL注入测试
配对1:
  请求: 参数名=id, 注入payload='OR 1=1--
  响应: Body包含"mysql_error"
配对2:
  请求: 参数名=id, 注入payload=' AND 1=2--
  响应: Body不包含原始内容
表达式: "1 OR 2"
预期: 任一配对匹配即报告漏洞

# 测试用例2: XSS检测
规则: XSS测试
配对1:
  请求: 参数名=search, 注入payload=<script>alert(1)</script>
  响应: Body包含"<script>alert(1)</script>"
预期: 响应回显payload即报告漏洞
```

**并发测试**:
- 10条规则,100个参数,验证扫描任务调度
- 监控TaskScheduler线程池状态

**预期问题**:
- ❌ 批量模式未正确标记所有参数已测试
- ❌ 逐个模式重复测试相同参数
- ❌ 配对表达式递归深度过大导致栈溢出
- ❌ Response为null导致NullPointerException

---

#### 3.2 Payload变量解析 (PayloadVariableResolver)
**测试项**:
- [ ] **T3.2.1** 变量替换 → `{{original}}`正确替换为原始值
- [ ] **T3.2.2** 随机字符串 → `{{random}}`每次生成不同值
- [ ] **T3.2.3** 嵌套变量 → 多层变量替换
- [ ] **T3.2.4** 特殊字符转义 → URL编码、HTML实体转义

**测试数据**:
```
原始参数值: admin
Payload: {{original}}'OR'1'='1
预期: admin'OR'1'='1

Payload: {{random}}_test
预期: abc123_test (random部分每次不同)
```

**预期问题**:
- ❌ 变量未正确替换导致payload无效
- ❌ 特殊字符未转义导致payload被破坏

---

### 4. 主动探测测试 (Arjun)

#### 4.1 参数收集 (ParameterCollector)
**测试项**:
- [ ] **T4.1.1** 参数提取 → 从GET/POST/Cookie/Header提取参数
- [ ] **T4.1.2** 主域名分组 → 正确提取主域名(IP/域名)
- [ ] **T4.1.3** 关键词收集 → PARAMETERS_AND_KEYWORDS模式提取参数值
- [ ] **T4.1.4** 去重处理 → 相同参数不重复收集
- [ ] **T4.1.5** 内存占用 → 收集100万个参数不应OOM

**测试数据**:
```
场景1: 混合参数
GET /api?id=1&name=test HTTP/1.1
Cookie: session=abc123; user_id=456
Content-Type: application/json
{"data": {"field1": "value1"}}

预期收集: id, name, session, user_id, data, field1

场景2: 主域名提取
https://api.example.com/test → example.com
https://192.168.1.1/test → 192.168.1.1
https://sub.domain.target.com/test → target.com
```

**压力测试**:
- 10000个请求,验证收集器性能和内存占用
- 验证主域名统计准确性

**预期问题**:
- ❌ JSON body参数未正确提取
- ❌ IPv6地址主域名提取错误
- ❌ 关键词过滤逻辑缺失导致收集大量无用词
- ❌ ParameterCollector数据结构过大导致OOM

---

#### 4.2 Arjun参数发现 (ArjunService, ParamDiscoveryEngine)
**测试项**:
- [ ] **T4.2.1** 基线建立 → 正确建立响应基线
- [ ] **T4.2.2** 异常检测 → 识别参数导致的响应变化
- [ ] **T4.2.3** 分块爆破 → chunk分块正确,递归缩小正确
- [ ] **T4.2.4** 并发处理 → 多线程并发测试chunks
- [ ] **T4.2.5** 重试机制 → 网络错误自动重试
- [ ] **T4.2.6** 稳定模式 → 随机延迟避免WAF检测
- [ ] **T4.2.7** 速率限制 → 遵守配置的速率限制

**测试场景**:
```
场景1: 稳定目标
目标: https://stable.example.com/api
字典: 500个参数(包含真实参数: debug, admin_panel, secret_key)
预期: 发现3个隐藏参数,耗时<30秒

场景2: 不稳定目标
目标: https://unstable.example.com/api (响应长度随机波动±10%)
字典: 100个参数
预期: 动态调整基线因子,避免误报

场景3: 目标返回400错误
目标: https://bad.example.com/api (随机参数返回400)
预期: 识别为不健康状态,跳过扫描
```

**性能测试**:
```
配置:
  - chunkSize: 250
  - threads: 5
  - rateLimit: 100 req/s
  - dictionary: 5000个参数
  
预期性能:
  - 第一轮分块: 20个chunk,并发5线程,耗时<4秒
  - 递归缩小: 最多3-4层递归
  - 总耗时: <2分钟
  - 请求总数: <300个 (得益于分块递归)
```

**压力测试**:
- 同时扫描10个目标(不同主域名)
- 每个目标字典10000个参数
- 监控CPU/内存/网络占用

**预期问题**:
- ❌ 基线建立失败导致全部参数误报
- ❌ 动态因子调整逻辑缺失导致不稳定目标扫描失败
- ❌ 线程池未正确关闭导致资源泄漏
- ❌ 速率限制未生效导致目标封禁IP
- ❌ 重试次数过多导致扫描时间过长
- ❌ ConcurrentProcessor线程异常未处理

---

#### 4.3 Arjun智能触发 (RealtimeScannerRefactored)
**测试项**:
- [ ] **T4.3.1** 参数阈值触发 → 收集到15个参数自动触发Arjun
- [ ] **T4.3.2** 冷却时间 → 5分钟内不重复触发同一主域名
- [ ] **T4.3.3** 增量扫描 → 只测试新参数,不重复测试
- [ ] **T4.3.4** 定时检查 → 300秒定时检查是否有新参数
- [ ] **T4.3.5** 发现后触发扫描 → Arjun发现参数后触发漏洞扫描

**测试场景**:
```
时间线:
00:00 - 收集参数: user_id, name, email, phone, address (5个参数)
00:30 - 收集参数: age, gender, city (8个参数,总计8个)
01:00 - 收集参数: country, zip, company (11个参数,总计11个)
01:30 - 收集参数: job, salary, department, manager, level (16个参数,总计16个)

预期:
01:30 - 触发Arjun(达到15个参数阈值)
02:00 - 收集参数: role, permission (18个参数,总计18个)
06:30 - 不触发(冷却期未过)
07:00 - 触发Arjun(冷却期5分钟已过,且有新参数)
```

**预期问题**:
- ❌ 冷却时间计算错误导致频繁触发
- ❌ 增量参数计算错误导致重复测试
- ❌ 定时检查线程未启动或未正确关闭
- ❌ Arjun发现参数后未正确传递给漏洞扫描器

---

### 5. 任务调度测试 (TaskScheduler)

#### 5.1 任务队列管理
**测试项**:
- [ ] **T5.1.1** 任务提交 → 大量任务不应阻塞
- [ ] **T5.1.2** 线程池管理 → 线程数符合CPU核心数×2
- [ ] **T5.1.3** 任务优先级 → 高危规则优先扫描(如果实现)
- [ ] **T5.1.4** 异常处理 → 单个任务异常不影响其他任务
- [ ] **T5.1.5** 资源清理 → shutdown正确等待任务完成

**压力测试**:
```
场景: 提交10000个扫描任务
配置: 
  - 规则数: 50
  - 参数数/请求: 20
  - 预期任务总数: 10000 × 20 × 50 = 1000万任务

测试:
  - 任务队列不应溢出
  - 内存占用<2GB
  - CPU利用率稳定在60-80%
  - 任务完成时间<10分钟
```

**预期问题**:
- ❌ 任务队列无限增长导致OOM
- ❌ 线程池未正确配置导致性能低下
- ❌ shutdown等待时间过长(>60秒)
- ❌ 异常任务导致线程死亡,线程池缩水

---

### 6. UI组件测试

#### 6.1 仪表板 (DashboardTab)
**测试项**:
- [ ] **T6.1.1** 实时统计更新 → 数据实时刷新不应卡顿
- [ ] **T6.1.2** 主域名列表 → 显示所有收集的主域名
- [ ] **T6.1.3** 参数统计 → 显示每个主域名的参数数量
- [ ] **T6.1.4** Arjun统计 → 显示扫描次数、成功率、发现参数数
- [ ] **T6.1.5** 清空数据 → 清空收集的参数和统计

**测试数据**:
- 100个主域名,每个1000个参数
- Arjun统计:100次扫描,80次成功,发现500个参数

**预期问题**:
- ❌ 大量数据导致表格渲染卡顿
- ❌ 实时刷新频率过高导致UI冻结
- ❌ 清空数据后内存未释放

---

#### 6.2 扫描结果 (ScanResultTab)
**测试项**:
- [ ] **T6.2.1** 结果显示 → 正确显示漏洞和测试请求
- [ ] **T6.2.2** 请求/响应查看 → 正确显示修改后的请求和响应
- [ ] **T6.2.3** 过滤和搜索 → 按规则名称、URL过滤
- [ ] **T6.2.4** 排序 → 按时间、状态码、响应时间排序
- [ ] **T6.2.5** 导出 → 导出扫描结果为CSV/JSON

**测试数据**:
- 10000条扫描结果(100条命中,9900条未命中)
- 测试排序性能
- 测试搜索性能

**预期问题**:
- ❌ 大量结果导致表格滚动卡顿
- ❌ 搜索未做索引导致性能差
- ❌ 请求/响应体过大(>10MB)导致显示卡顿

---

#### 6.3 配置中心 (UnifiedConfigTab)
**测试项**:
- [ ] **T6.3.1** 黑白名单编辑 → 添加/删除/启用/禁用
- [ ] **T6.3.2** Arjun配置 → 修改chunk大小、线程数等
- [ ] **T6.3.3** 全局注入模式 → 切换批量/逐个模式
- [ ] **T6.3.4** 扫描结果记录模式 → 切换全部/仅命中模式
- [ ] **T6.3.5** 配置保存 → 保存后立即生效

**测试场景**:
- 添加1000条黑名单规则,测试UI性能
- 修改Arjun线程数从5→20,验证生效
- 切换注入模式,发送测试请求验证

**预期问题**:
- ❌ 配置修改未立即生效
- ❌ 大量规则导致UI渲染慢
- ❌ 配置校验缺失导致运行时错误

---

### 7. 实际使用场景测试

#### 7.1 大流量场景
**场景**: 测试目标为大型电商网站,每分钟10000+请求

**测试步骤**:
1. 配置黑名单过滤静态资源(.jpg, .css, .js)
2. 启用被动扫描(10条规则)
3. 启用Arjun实时模式(阈值15,冷却300秒)
4. 浏览网站30分钟

**监控指标**:
- CPU占用: <30%
- 内存占用: <1GB
- 去重集合大小: <10万条
- 扫描任务队列: <1000个待处理
- Burp响应速度: 无明显延迟

**预期问题**:
- ❌ 内存持续增长,30分钟后OOM
- ❌ 去重集合无限增长
- ❌ 任务队列积压导致延迟
- ❌ 过滤器性能不足,每个请求>100ms

---

#### 7.2 长时间运行场景
**场景**: 插件持续运行24小时

**测试步骤**:
1. 加载插件
2. 配置中等规模(50条规则,10个主域名)
3. 每分钟发送100个请求(模拟正常流量)
4. 运行24小时

**监控指标**:
- 内存占用趋势(应稳定,不持续增长)
- CPU占用趋势(应稳定)
- 线程数(应稳定,无泄漏)
- 去重集合大小(应有上限或定期清理)
- 扫描结果表大小(如果配置为"仅命中",应较小)

**预期问题**:
- ❌ 内存泄漏,24小时后OOM
- ❌ 线程泄漏,线程数持续增长
- ❌ 去重集合无上限,持续增长
- ❌ 扫描结果表无限增长

---

#### 7.3 多目标并发场景
**场景**: 同时测试10个不同的目标站点

**测试步骤**:
1. 配置10个主域名的白名单
2. 启用Arjun(每个主域名10000个参数字典)
3. 同时向10个目标发送请求

**监控指标**:
- Arjun扫描是否正确隔离(不同主域名独立)
- 参数收集是否正确分组
- 扫描任务是否正确分配
- 性能是否线性下降(10个目标应相当于单个的10倍负载)

**预期问题**:
- ❌ 参数收集混淆,不同主域名参数互相干扰
- ❌ Arjun扫描冲突,线程池竞争
- ❌ 性能指数级下降(非线性)

---

#### 7.4 边界和异常场景
**测试项**:
- [ ] **T7.4.1** 空请求/响应 → 不应崩溃
- [ ] **T7.4.2** 超大请求/响应(>100MB) → 应跳过或限制
- [ ] **T7.4.3** 特殊字符 → 参数名包含\0, \r\n等
- [ ] **T7.4.4** 超长参数名(>10000字符) → 应限制或截断
- [ ] **T7.4.5** 网络异常 → Arjun扫描时目标断开连接
- [ ] **T7.4.6** 磁盘空间不足 → 配置保存失败应提示
- [ ] **T7.4.7** Burp重启 → 插件状态恢复

**预期问题**:
- ❌ null响应导致NullPointerException
- ❌ 超大body导致OOM
- ❌ 特殊字符未转义导致JSON解析错误
- ❌ 网络异常导致Arjun死循环重试
- ❌ 磁盘满导致配置丢失

---

## 🔍 问题发现记录

### 已发现问题清单

#### 🔴 P0 - 严重问题 (导致崩溃/数据丢失)
1. **内存泄漏 - 去重集合无限增长**
   - **位置**: `RealtimeScannerRefactored.passiveScanProcessedKeys`
   - **现象**: `ConcurrentHashMap.newKeySet()` 无限增长,长时间运行OOM
   - **影响**: 24小时运行后内存占用>2GB
   - **建议**: 使用LRU缓存或定期清理,限制最大100万条

2. **线程泄漏 - Arjun线程池未关闭**
   - **位置**: `ParamDiscoveryEngine.concurrentProcessor`
   - **现象**: 插件卸载后线程未正确关闭
   - **影响**: Burp重启后遗留僵尸线程
   - **建议**: 已实现shutdown(),需测试验证

3. **NullPointerException - Response为null**
   - **位置**: `TaskScheduler.logResult()`, `UniversalScanner.evaluatePair()`
   - **现象**: 某些情况下response为null导致崩溃
   - **影响**: 扫描任务失败,日志记录失败
   - **建议**: 已添加null检查,需测试验证

#### 🟠 P1 - 高优先级问题 (影响功能/性能)
4. **性能问题 - 大量黑白名单规则**
   - **位置**: `GlobalFilter.shouldProcess()`
   - **现象**: 1000条规则时每次匹配>100ms
   - **影响**: 高流量下CPU占用过高
   - **建议**: 正则表达式预编译+Trie树优化

5. **去重逻辑错误 - 批量模式重复测试**
   - **位置**: `UniversalScanner.evaluateBatchMode()`
   - **现象**: 批量注入后未正确标记所有参数
   - **影响**: 相同参数重复测试,浪费资源
   - **建议**: 已修复,需测试验证

6. **配对表达式栈溢出**
   - **位置**: `UniversalScanner.evaluateBooleanExpressionInternal()`
   - **现象**: 深度嵌套表达式导致栈溢出
   - **影响**: 复杂规则无法使用
   - **建议**: 已添加深度限制(10层),需测试验证

7. **Arjun基线不稳定**
   - **位置**: `ParamDiscoveryEngine.initialize()`
   - **现象**: 对于响应长度波动的目标,基线建立失败
   - **影响**: 误报或漏报参数
   - **建议**: 已实现动态因子调整,需测试验证

#### 🟡 P2 - 中优先级问题 (影响体验)
8. **UI卡顿 - 大量扫描结果**
   - **位置**: `ScanResultTab`
   - **现象**: 10000+结果时滚动卡顿
   - **影响**: 用户体验差
   - **建议**: 使用虚拟滚动或分页

9. **配置校验缺失**
   - **位置**: `XProbeConfig` setters
   - **现象**: 可以设置负数/超范围值
   - **影响**: 运行时错误
   - **建议**: 部分已实现范围检查,需全面测试

10. **日志过多 - Debug日志未关闭**
    - **位置**: 多处 `api.logging().raiseDebugEvent()`
    - **现象**: 高流量下日志刷屏
    - **影响**: Burp日志窗口性能下降
    - **建议**: 添加日志级别开关

---

## ✅ 测试执行指南

### 测试环境准备
```yaml
Burp Suite: Professional v2023.x+
Java: JDK 17+
目标站点: 
  - 稳定目标: https://httpbin.org
  - 不稳定目标: 本地Mock服务器(随机响应)
  - 大流量目标: 本地Nginx反向代理
工具:
  - JProfiler (内存/CPU监控)
  - VisualVM (线程监控)
  - Apache JMeter (流量生成)
```

### 测试优先级
1. **阶段1 (P0问题)**: 内存泄漏、线程泄漏、NPE → 必须修复
2. **阶段2 (P1问题)**: 性能问题、去重逻辑、Arjun稳定性 → 高优先级
3. **阶段3 (P2问题)**: UI优化、配置校验、日志优化 → 中优先级
4. **阶段4 (功能测试)**: 全面功能覆盖测试
5. **阶段5 (压力测试)**: 长时间运行、大流量、多目标

### 测试数据生成
```bash
# 生成大量请求 (使用JMeter)
jmeter -n -t test_plan.jmx -Jthreads=100 -Jduration=600

# 生成大字典文件 (用于Arjun)
python3 -c "for i in range(10000): print(f'param_{i}')" > large_dict.txt

# 模拟不稳定目标 (使用Python Flask)
python3 unstable_server.py
```

---

## 📊 测试报告模板

### 问题报告
```markdown
**问题ID**: XPROBE-001
**严重级别**: P0 / P1 / P2
**模块**: ParamDiscoveryEngine
**问题描述**: 去重集合无限增长导致OOM
**复现步骤**:
1. 加载插件
2. 配置被动扫描10条规则
3. 发送10万个请求
4. 观察内存占用

**预期结果**: 内存稳定在<500MB
**实际结果**: 内存持续增长至2GB+,最终OOM

**影响范围**: 所有长时间运行场景
**建议方案**: 使用LRU缓存,限制最大10万条
**修复状态**: [ ] 待修复 / [ ] 修复中 / [x] 已修复
```

---

## 🎯 测试完成标准

- [ ] 所有P0问题已修复并验证
- [ ] 80%以上P1问题已修复
- [ ] 核心功能测试通过率>95%
- [ ] 24小时长时间运行测试通过
- [ ] 大流量压力测试通过(10000 req/min)
- [ ] 内存占用稳定(<1GB)
- [ ] CPU占用合理(<30%)
- [ ] 无已知崩溃问题

---

**测试计划制定**: AI Assistant
**测试执行**: 待开始
**下次更新**: 测试执行后
