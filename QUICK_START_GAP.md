# XProbe 参数收集快速开始指南

## 🚀 核心特性

XProbe 现已集成 **GAP (Get All Parameters)** 的成熟参数收集逻辑，提供两种强大的收集模式：

### 模式 1️⃣：仅参数名
- **适用场景**：日常被动扫描、大流量目标
- **性能**：⚡ 快速、低内存
- **收集内容**：HTTP 请求中的所有参数名

### 模式 2️⃣：参数名 + 关键词
- **适用场景**：深度挖掘、重点目标
- **性能**：⚡ 中等、全面
- **收集内容**：参数名 + 参数值中的有意义单词

## 📖 5分钟上手

### 第一步：加载扩展

1. 打开 Burp Suite
2. Extensions → Installed → Add
3. 选择 `XProbe-1.0.0.jar`
4. 加载成功后，会出现 "XProbe" 选项卡

### 第二步：配置收集模式

1. 切换到 **XProbe** → **全局配置** 选项卡
2. 找到 **"参数收集模式"** 下拉框
3. 选择你需要的模式：

```
┌─────────────────────────────────┐
│ 参数收集模式: [下拉选择框]     │
│   • 仅收集参数名                │
│   • 参数名+关键词  ← 推荐      │
└─────────────────────────────────┘
```

### 第三步：开始使用

**被动收集（自动）**：
- 浏览目标网站，XProbe 会自动收集参数
- 无需任何操作，后台静默运行

**主动探测（手动触发）**：
1. 切换到 **主动扫描** 选项卡
2. 点击 **"触发 Arjun 扫描"** 按钮
3. 已收集的参数和关键词会自动传递给 Arjun

## 🎯 实战示例

### 示例 1：快速扫描电商网站

**场景**：测试 `https://shop.example.com`

**配置**：
```
模式: 仅参数名
全局配置 → 白名单: shop.example.com
```

**操作流程**：
1. 浏览网站（登录、搜索、添加购物车等）
2. XProbe 自动收集参数：`user_id`, `product_id`, `session_token` 等
3. 点击 "触发 Arjun 扫描"
4. Arjun 基于收集的参数进行深度探测

**预期结果**：
```
✅ 收集参数: 45个
✅ 发现接口: 23个
✅ Arjun 新发现: 12个隐藏参数
```

### 示例 2：深度测试 API

**场景**：测试 `https://api.example.com`

**配置**：
```
模式: 参数名+关键词  ← 重点
全局配置 → 白名单: api.example.com
```

**操作流程**：
1. 使用 Burp Proxy 拦截 API 请求
2. XProbe 收集：
   - 参数名：`token`, `user_id`, `api_key`
   - 关键词：`admin`, `dashboard`, `secret`, `config`
3. 触发 Arjun，使用参数 + 关键词组合探测

**预期结果**：
```
✅ 收集参数: 28个
✅ 收集关键词: 156个
✅ Arjun 扫描（参数+关键词）: 184个
✅ 发现隐藏接口: /api/admin/config
```

### 示例 3：大规模爬虫辅助

**场景**：辅助爬虫收集参数字典

**配置**：
```
模式: 参数名+关键词
无白名单限制（收集所有）
```

**操作流程**：
1. 使用 Spider 爬取整站
2. XProbe 在后台持续收集
3. 爬取完成后，查看统计：
   - 主动扫描 → Arjun 配置 → 查看统计

**预期结果**：
```
✅ 主域名: 15
✅ Host: 43  
✅ 接口: 892
✅ 参数: 1,247
✅ 关键词: 3,856
```

## 🔍 参数收集详解

### 参数名清理逻辑

**输入示例**：
```
user\\/name          → username
email%5b%5d          → email
param?value          → value
"user"id             → userid
```

**清理规则**（参考 GAP.py）：
- ✅ 移除 URL 编码的方括号
- ✅ 移除反斜杠和斜杠
- ✅ 移除引号和 HTML 实体
- ✅ 处理问号分隔符
- ✅ 严格验证：`^[A-Za-z0-9_.~\-\[\]]+$`

### 关键词提取逻辑

**输入示例**：
```
URL: /api/admin?token=admin_dashboard_2024_secret_key
```

**提取流程**：
1. 从参数值提取：`admin_dashboard_2024_secret_key`
2. 正则分词：`admin`, `dashboard`, `2024`, `secret`, `key`
3. 过滤规则：
   - ❌ `2024` (纯数字)
   - ✅ `admin`, `dashboard`, `secret`, `key` (有效关键词)

**最终输出**：
```
参数名: token
关键词: admin, dashboard, secret, key
```

### 停用词过滤

**停用词列表**（参考 GAP.py）：
```
基础词: true, false, null, undefined, nan
连接词: the, and, for, but, not
代词: you, her, his, she
常用词: all, can, get, has, new, old
```

**示例**：
```
输入: "the user can access the admin panel"
输出: user, access, admin, panel
      ↑ 过滤了 the, can, the
```

## 📊 统计信息查看

### 实时统计

**位置**：主动扫描 → 统计信息

**显示内容**：
```
┌────────────────────────────────────────┐
│ 参数收集统计                           │
├────────────────────────────────────────┤
│ 主域名: 5                              │
│ Host: 12                               │
│ 接口: 87                               │
│ 参数: 156                              │
│ 关键词: 423  [模式: 参数+关键词]      │
└────────────────────────────────────────┘
```

### 按主域名查看

**位置**：主动扫描配置 → Host 统计

**显示内容**：
```
┌─────────────────────────────────────────────────────┐
│ 主域名              | 接口 | 参数 | 最后更新         │
├─────────────────────────────────────────────────────┤
│ example.com (3 hosts) | 45   | 89   | 2025-10-01    │
│ api.example.com (2)   | 23   | 34   | 2025-10-01    │
└─────────────────────────────────────────────────────┘
```

## ⚙️ 高级配置

### 白名单/黑名单

**位置**：全局配置 → 黑白名单

**使用场景**：
```
白名单: 只收集特定域名
example.com
*.api.example.com

黑名单: 排除某些路径
*/logout
*/static/*
*.jpg
```

### Arjun 配置

**位置**：全局配置 → 代理池配置（如需）

**可配置项**：
- Arjun 路径
- 超时时间
- 并发线程数
- 代理设置

## 🎓 最佳实践

### 1. 根据场景选择模式

| 场景 | 推荐模式 | 理由 |
|------|----------|------|
| 日常被动扫描 | 仅参数名 | 性能优先 |
| 重点目标深度测试 | 参数名+关键词 | 发现率优先 |
| 大流量网站 | 仅参数名 | 避免内存占用 |
| API 接口测试 | 参数名+关键词 | API 通常参数少但关键词价值高 |

### 2. 定期清理数据

**操作**：主动扫描配置 → 清空全局字典

**建议**：
- 每次测试新目标前清空
- 避免不同项目参数混淆

### 3. 配合 Arjun 使用

**工作流**：
```
1. 开启被动收集
2. 浏览/爬取目标网站
3. 等待收集完成（查看统计）
4. 触发 Arjun 扫描
5. 查看 Arjun 发现的新参数
```

### 4. 利用主域名分组

**优势**：
- 同一主域名下的所有子域参数自动合并
- Arjun 扫描时自动使用合并后的字典
- 提升隐藏参数发现率

**示例**：
```
收集到:
  - api.example.com: user_id, token
  - admin.example.com: admin_id, session

触发 Arjun 时:
  ✅ 自动合并: user_id, token, admin_id, session
  ✅ 传递给 Arjun: 4个参数作为字典
```

## 🐛 常见问题

### Q1: 为什么关键词模式收集的数据比较多？

**A**: 关键词模式会从参数值中提取单词，数据量确实会增加。但有严格的过滤：
- 最小长度 3 字符
- 最大长度 50 字符
- 过滤纯数字
- 过滤停用词

### Q2: 参数收集会影响性能吗？

**A**: 影响很小：
- 使用预编译正则表达式
- 异步处理，不阻塞主流程
- 自动去重，避免重复处理

### Q3: 如何判断参数收集是否工作？

**A**: 查看统计信息：
1. 主动扫描 → 统计信息
2. 如果参数数 > 0，说明正在工作
3. 浏览目标后，数字应该增加

### Q4: Arjun 没有发现新参数？

**A**: 可能原因：
1. 收集的参数已经很全面
2. 目标网站没有隐藏参数
3. Arjun 配置问题（检查路径、权限）

**解决方案**：
- 尝试手动添加参数到全局字典
- 检查 Arjun 日志
- 更换扫描目标测试

## 📚 参考资源

### 相关文档
- `GAP_REFERENCE_IMPLEMENTATION.md` - GAP 集成详解
- `PARAMETER_COLLECTION_MODE_UPDATE.md` - 功能说明
- `FINAL_GAP_INTEGRATION_SUMMARY.md` - 完整总结

### 外部资源
- **GAP 项目**: https://github.com/xnl-h4ck3r/GAP-Burp-Extension
- **Arjun 项目**: https://github.com/s0md3v/Arjun

### 技术支持
- 遇到问题：查看 Burp Suite 扩展输出
- 报告 Bug：记录复现步骤和日志
- 功能建议：欢迎提出改进意见

## ✨ 更新日志

### v1.1.0 (2025-10-01)
- ✅ 集成 GAP 参数收集逻辑
- ✅ 新增双模式支持
- ✅ 优化关键词提取
- ✅ 完善停用词过滤
- ✅ 改进主域名分组

---

**享受高效的参数发现之旅！** 🚀

如果 XProbe 帮助你发现了漏洞，欢迎分享你的成功案例！

