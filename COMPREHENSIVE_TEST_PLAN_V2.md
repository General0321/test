# XProbe 插件全面测试计划 v2.0

> **文档创建时间**: 2025-10-02  
> **项目名称**: XProbe - Burp Suite 被动安全扫描器  
> **测试目标**: 确保插件在真实环境下稳定、高效、准确地运行

---

## 📋 目录

1. [测试概述](#1-测试概述)
2. [环境准备](#2-环境准备)
3. [模块测试计划](#3-模块测试计划)
4. [集成测试计划](#4-集成测试计划)
5. [性能与压力测试](#5-性能与压力测试)
6. [真实场景测试](#6-真实场景测试)
7. [已知问题与风险](#7-已知问题与风险)
8. [测试检查清单](#8-测试检查清单)

---

## 1. 测试概述

### 1.1 测试目标

- ✅ **功能完整性**: 确保所有功能模块正常工作
- ✅ **性能稳定性**: 确保插件在高负载下不崩溃、不泄漏内存
- ✅ **准确性验证**: 确保扫描结果准确、去重有效
- ✅ **用户体验**: 确保UI响应流畅、日志清晰
- ✅ **跨平台兼容**: 确保在 Windows/macOS/Linux 上都能正常运行

### 1.2 测试优先级

| 优先级 | 模块 | 原因 |
|-------|------|------|
| **P0** | 配置管理、请求处理、去重机制 | 核心功能，影响插件稳定性 |
| **P1** | 扫描器、Arjun集成、日志系统 | 主要功能，影响用户体验 |
| **P2** | UI界面、统计信息、参数导入导出 | 辅助功能，影响易用性 |
| **P3** | 边缘场景、异常处理 | 健壮性，影响可靠性 |

### 1.3 测试环境

#### 必需环境
- **Burp Suite Professional** v2023.1+ 或 Community Edition
- **Java Runtime** 11+
- **操作系统**: Windows 10/11, macOS 12+, Ubuntu 20.04+

#### 可选环境（主动探测功能）
- **Python** 3.8+
- **Arjun** 2.x (安装：`pip install arjun`)

---

## 2. 环境准备

### 2.1 测试数据准备

#### 2.1.1 测试目标站点

准备以下类型的测试目标（建议使用本地搭建的测试环境）：

1. **DVWA (Damn Vulnerable Web Application)**
   - URL: `http://localhost/dvwa`
   - 包含: SQL注入、XSS、命令注入等多种漏洞
   - 用于测试: 被动扫描规则、配对架构

2. **OWASP WebGoat**
   - URL: `http://localhost:8080/WebGoat`
   - 包含: 各种OWASP Top 10漏洞
   - 用于测试: 复杂场景、参数收集

3. **自建测试API**
   - 创建包含以下特性的测试接口：
     ```
     GET  /api/user?id=1&name=test
     POST /api/user (JSON: {"id":1,"name":"test"})
     POST /api/user (Form: id=1&name=test)
     PUT  /api/user/1
     DELETE /api/user/1
     ```
   - 用于测试: 不同HTTP方法、不同Content-Type

4. **高流量站点（生产环境模拟）**
   - 使用 `JMeter` 或 `wrk` 工具模拟高并发流量
   - 目标: 100-1000 req/s
   - 用于测试: 性能、内存泄漏

#### 2.1.2 测试规则库

创建以下扫描规则（存放在 `test-rules/` 目录）：

**规则1: SQL注入检测（旧架构）**
```json
{
  "ruleId": "test-sqli-001",
  "customLabel": "SQL注入检测-旧架构",
  "enabled": true,
  "parameterNames": ["id", "user_id", "product_id"],
  "parameterNameType": "String Match",
  "parameterValues": [
    "' OR '1'='1",
    "1' AND '1'='1",
    "1' OR 1=1--"
  ],
  "matchRules": [{
    "location": "Response Body",
    "matchType": "Contains",
    "rule": "SQL syntax error",
    "operator": "OR"
  }]
}
```

**规则2: XSS检测（配对架构）**
```json
{
  "ruleId": "test-xss-001",
  "customLabel": "XSS检测-配对架构",
  "enabled": true,
  "pairs": [{
    "id": 1,
    "displayLabel": "参数注入XSS",
    "requestConfig": {
      "elements": [{
        "type": "PARAMETER",
        "nameMatchConfig": {
          "matchType": "ANY",
          "values": []
        },
        "useForInjection": true,
        "injectionTarget": "VALUE",
        "payloads": [
          "<script>alert(1)</script>",
          "<img src=x onerror=alert(1)>"
        ]
      }]
    },
    "responseConfig": {
      "elements": [{
        "type": "BODY",
        "matchType": "CONTAINS",
        "values": ["<script>alert(1)</script>", "<img src=x"]
      }]
    }
  }],
  "pairExpression": "1"
}
```

**规则3: 路径遍历（被动检测）**
```json
{
  "ruleId": "test-lfi-001",
  "customLabel": "路径遍历-被动",
  "enabled": true,
  "pairs": [{
    "id": 1,
    "displayLabel": "检测敏感路径",
    "requestConfig": {
      "elements": [{
        "type": "PATH",
        "matchType": "CONTAINS",
        "values": ["../", "..\\", "/etc/"]
      }]
    },
    "responseConfig": {
      "elements": [{
        "type": "BODY",
        "matchType": "REGEX",
        "values": ["root:x:0:0"]
      }]
    }
  }]
}
```

### 2.2 Burp Suite 配置

1. **代理设置**: 127.0.0.1:8080
2. **目标作用域**: 添加测试站点到作用域
3. **扫描器设置**: 禁用内置扫描器（避免干扰）
4. **日志级别**: INFO（生产环境）/ DEBUG（开发测试）

### 2.3 测试工具准备

| 工具 | 用途 | 安装命令 |
|------|------|----------|
| **JMeter** | 性能测试 | `brew install jmeter` |
| **VisualVM** | 内存分析 | [下载链接](https://visualvm.github.io/) |
| **Arjun** | 参数爆破 | `pip install arjun` |
| **curl** | 手动HTTP测试 | 系统自带 |

---

## 3. 模块测试计划

### 3.1 配置管理模块 (P0)

**测试文件**: `XProbeConfigManager`, `ConfigPersistence`

#### 3.1.1 配置加载与保存

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| CFG-001 | 首次启动，无配置文件 | 创建默认配置，插件正常启动 | ⬜ |
| CFG-002 | 配置文件存在，正常加载 | 成功加载配置，应用到插件 | ⬜ |
| CFG-003 | 配置文件格式错误 | 使用默认配置，记录错误日志 | ⬜ |
| CFG-004 | 配置文件权限不足 | 使用默认配置，记录权限错误 | ⬜ |
| CFG-005 | 配置文件过大（>10MB） | 拒绝加载，记录错误 | ⬜ |
| CFG-006 | 修改配置后保存 | 配置持久化到磁盘 | ⬜ |
| CFG-007 | 配置保存失败（磁盘满） | 显示错误，保留内存配置 | ⬜ |

**测试步骤**:
```bash
# CFG-001: 删除配置文件测试
rm ~/.xprobe/config.json
# 启动Burp，观察日志

# CFG-003: 损坏配置文件测试
echo "invalid json" > ~/.xprobe/config.json
# 启动Burp，观察日志

# CFG-004: 权限测试
chmod 000 ~/.xprobe/config.json
# 启动Burp，观察日志
```

#### 3.1.2 配置热更新

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| CFG-010 | UI修改黑名单 | 立即生效，不需重启 | ⬜ |
| CFG-011 | UI修改扫描规则 | 立即生效，新流量使用新规则 | ⬜ |
| CFG-012 | UI修改全局注入模式 | 立即生效 | ⬜ |
| CFG-013 | 并发修改配置 | 最后一次修改生效 | ⬜ |

**测试步骤**:
1. 启动插件，发送测试流量
2. 在UI中修改配置（不重启）
3. 继续发送流量，观察行为是否改变

#### 3.1.3 配置迁移与兼容性

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| CFG-020 | 加载旧版配置（v1.0） | 自动迁移到新格式 | ⬜ |
| CFG-021 | 配置字段缺失 | 使用默认值填充 | ⬜ |
| CFG-022 | 配置字段类型错误 | 使用默认值，记录警告 | ⬜ |

---

### 3.2 请求处理与过滤模块 (P0)

**测试文件**: `RequestHandler`, `GlobalFilter`, `RequestFilter`

#### 3.2.1 黑白名单过滤

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| FLT-001 | 白名单启用，匹配URL | 请求被处理 | ⬜ |
| FLT-002 | 白名单启用，不匹配URL | 请求被跳过 | ⬜ |
| FLT-003 | 黑名单启用，匹配URL | 请求被跳过 | ⬜ |
| FLT-004 | 黑名单启用，不匹配URL | 请求被处理 | ⬜ |
| FLT-005 | 白名单+黑名单同时启用 | 白名单优先，黑名单次之 | ⬜ |
| FLT-006 | 正则表达式白名单 | 正确匹配正则模式 | ⬜ |
| FLT-007 | 无效正则表达式 | 忽略该规则，记录警告 | ⬜ |
| FLT-008 | 空白名单/黑名单 | 处理所有请求 | ⬜ |

**测试数据**:
```
白名单: ["example.com", ".*\\.api\\..*"]
黑名单: ["/static/", "/images/", ".*\\.jpg$"]

测试URL:
✓ http://example.com/api/user     -> 处理（白名单匹配）
✗ http://other.com/api/user        -> 跳过（白名单不匹配）
✓ http://sub.api.example.com/test  -> 处理（白名单正则匹配）
✗ http://example.com/static/css    -> 跳过（黑名单匹配）
✗ http://example.com/logo.jpg      -> 跳过（黑名单正则匹配）
```

#### 3.2.2 请求处理流程

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| REQ-001 | 普通GET请求 | 正常处理，生成扫描任务 | ⬜ |
| REQ-002 | POST请求（JSON） | 正常处理，识别JSON参数 | ⬜ |
| REQ-003 | POST请求（Form） | 正常处理，识别Form参数 | ⬜ |
| REQ-004 | PUT/DELETE请求 | 正常处理 | ⬜ |
| REQ-005 | 无参数请求 | 跳过扫描（无可注入点） | ⬜ |
| REQ-006 | 超大请求体（>10MB） | 正常处理或跳过（配置） | ⬜ |
| REQ-007 | 包含特殊字符的URL | 正确解析 | ⬜ |
| REQ-008 | 高并发请求（100 req/s） | 不阻塞、不丢失 | ⬜ |

**测试步骤**:
```bash
# 使用curl发送各种类型的请求
curl -X GET "http://localhost/api/user?id=1&name=test"
curl -X POST "http://localhost/api/user" -H "Content-Type: application/json" -d '{"id":1,"name":"test"}'
curl -X POST "http://localhost/api/user" -d "id=1&name=test"

# 高并发测试
ab -n 1000 -c 10 "http://localhost/api/user?id=1"
```

---

### 3.3 去重机制模块 (P0)

**测试文件**: `DeduplicationKeyGenerator`, `RealtimeScannerRefactored`

#### 3.3.1 去重颗粒度测试

| 去重级别 | 测试场景 | 预期结果 | 实际结果 |
|---------|---------|---------|---------|
| **GLOBAL** | 同一规则，不同URL | 只扫描一次 | ⬜ |
| **HOST** | 同一规则，不同host | 每个host扫描一次 | ⬜ |
| **PATH** | 同一规则，不同path | 每个path扫描一次 | ⬜ |
| **REQUEST** | 同一规则，不同method/contentType | 每个请求类型扫描一次 | ⬜ |
| **PARAMETER** | 同一规则，不同参数 | 每个参数扫描一次 | ⬜ |
| **NONE** | Fuzzing模式 | 每次都扫描 | ⬜ |
| **AUTO** | 自动检测 | 根据规则类型自动选择 | ⬜ |

**测试数据**:
```
规则: SQL注入检测 (id=test-sqli-001)
去重级别: PARAMETER

测试请求:
1. GET /api/user?id=1      -> 扫描（首次）
2. GET /api/user?id=2      -> 跳过（参数id已测试）
3. GET /api/user?name=test -> 扫描（新参数name）
4. POST /api/user (id=1)   -> 跳过（参数id已测试，虽然method不同）
```

#### 3.3.2 去重key生成

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| DUP-001 | 相同请求生成相同key | key相同 | ⬜ |
| DUP-002 | 不同参数生成不同key | key不同 | ⬜ |
| DUP-003 | URL查询参数顺序不同 | 根据颗粒度判断 | ⬜ |
| DUP-004 | 路径末尾斜杠差异 | key相同 | ⬜ |
| DUP-005 | Content-Type大小写 | key相同（标准化） | ⬜ |
| DUP-006 | 超长路径（>1000字符） | 正常生成key | ⬜ |

**测试代码**:
```java
// 单元测试示例
String key1 = DeduplicationKeyGenerator.generateKey("GET", "example.com", "/api/user", "application/json", config, "id");
String key2 = DeduplicationKeyGenerator.generateKey("GET", "example.com", "/api/user", "application/json", config, "id");
assert key1.equals(key2);

String key3 = DeduplicationKeyGenerator.generateKey("GET", "example.com", "/api/user", "application/json", config, "name");
assert !key1.equals(key3);
```

---

### 3.4 扫描器模块 (P1)

**测试文件**: `UniversalScanner`, `AbstractScanner`

#### 3.4.1 旧架构扫描器（参数匹配）

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| SCN-001 | 字符串匹配参数名 | 匹配成功，注入payload | ⬜ |
| SCN-002 | 正则匹配参数名 | 匹配成功，注入payload | ⬜ |
| SCN-003 | 参数名不匹配 | 跳过扫描 | ⬜ |
| SCN-004 | 多个payload测试 | 依次注入每个payload | ⬜ |
| SCN-005 | 响应匹配规则（Contains） | 正确检测漏洞 | ⬜ |
| SCN-006 | 响应匹配规则（Regex） | 正确检测漏洞 | ⬜ |
| SCN-007 | 多个匹配规则（AND） | 所有规则都匹配才报告 | ⬜ |
| SCN-008 | 多个匹配规则（OR） | 任一规则匹配即报告 | ⬜ |

#### 3.4.2 新架构扫描器（配对架构）

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| SCN-020 | 单个配对，请求+响应匹配 | 检测漏洞 | ⬜ |
| SCN-021 | 多个配对，AND逻辑 | 所有配对匹配才报告 | ⬜ |
| SCN-022 | 多个配对，OR逻辑 | 任一配对匹配即报告 | ⬜ |
| SCN-023 | 复杂表达式（A AND (B OR C)） | 正确计算表达式 | ⬜ |
| SCN-024 | 被动检测（无注入点） | 只检查原始响应 | ⬜ |
| SCN-025 | 主动注入（参数值） | 注入payload，检查响应 | ⬜ |
| SCN-026 | 主动注入（Header值） | 注入到Header | ⬜ |
| SCN-027 | 主动注入（Path） | 注入到路径 | ⬜ |
| SCN-028 | 主动注入（Body） | 注入到请求体 | ⬜ |

#### 3.4.3 注入模式测试

**批量模式 (BATCH)**

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| INJ-001 | 匹配2个参数，2个payload | 发送2个请求（所有参数同时注入） | ⬜ |
| INJ-002 | 批量模式标记去重 | 一次标记所有参数为已测试 | ⬜ |

**测试数据**:
```
请求: GET /api?id=1&uid=2
配置: 参数名匹配 [id, uid], Payload [p1, p2]

批量模式发送:
Request 1: GET /api?id=p1&uid=p1
Request 2: GET /api?id=p2&uid=p2
```

**逐个模式 (INDIVIDUAL)**

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| INJ-010 | 匹配2个参数，2个payload | 发送4个请求（每个参数单独注入） | ⬜ |
| INJ-011 | 逐个模式标记去重 | 每个参数单独标记 | ⬜ |

**测试数据**:
```
请求: GET /api?id=1&uid=2
配置: 参数名匹配 [id, uid], Payload [p1, p2]

逐个模式发送:
Request 1: GET /api?id=p1&uid=2
Request 2: GET /api?id=p2&uid=2
Request 3: GET /api?id=1&uid=p1
Request 4: GET /api?id=1&uid=p2
```

#### 3.4.4 Payload变量解析

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| PAY-001 | {{ORIGINAL}} | 替换为原始值 | ⬜ |
| PAY-002 | {{ORIGINAL_URL_ENCODED}} | 替换为URL编码值 | ⬜ |
| PAY-003 | {{ORIGINAL_BASE64}} | 替换为Base64编码值 | ⬜ |
| PAY-004 | {{RANDOM_STRING}} | 生成随机字符串 | ⬜ |
| PAY-005 | {{RANDOM_NUMBER}} | 生成随机数字 | ⬜ |
| PAY-006 | 组合变量 | 正确解析多个变量 | ⬜ |
| PAY-007 | 无效变量语法 | 保持原样或报错 | ⬜ |

**测试数据**:
```
原始值: test123
Payload: {{ORIGINAL}}' OR '1'='1
期望结果: test123' OR '1'='1

Payload: admin_{{RANDOM_STRING:8}}
期望结果: admin_a8fG9xZ2
```

---

### 3.5 Arjun集成模块 (P1)

**测试文件**: `ArjunIntegration`, `RealtimeScannerRefactored`

#### 3.5.1 Arjun环境检测

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| ARJ-001 | Arjun已安装（PATH） | 自动检测到路径 | ⬜ |
| ARJ-002 | Arjun未安装 | 显示错误，提供安装指引 | ⬜ |
| ARJ-003 | Python脚本路径（绝对） | 正确执行 | ⬜ |
| ARJ-004 | Python模块模式 | `python -m arjun` 正确执行 | ⬜ |
| ARJ-005 | macOS安全限制 | 使用wrapper脚本绕过 | ⬜ |
| ARJ-006 | Windows环境 | 正确找到python.exe | ⬜ |

**测试步骤**:
```bash
# ARJ-001: 测试自动检测
which arjun  # 确认已安装

# ARJ-002: 测试未安装情况
mv $(which arjun) ~/arjun.bak
# 在UI中点击"测试连接"

# ARJ-005: macOS测试
# 在UI中配置Arjun路径为 /usr/local/bin/arjun
# 点击"测试连接"
```

#### 3.5.2 参数收集

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| COL-001 | 从GET参数收集 | 正确提取参数名 | ⬜ |
| COL-002 | 从POST参数收集 | 正确提取参数名 | ⬜ |
| COL-003 | 从JSON body收集 | 正确提取字段名 | ⬜ |
| COL-004 | 参数去重 | 重复参数只记录一次 | ⬜ |
| COL-005 | 按主域名分组 | 正确分组到主域名 | ⬜ |
| COL-006 | 参数名清理 | 移除无效字符 | ⬜ |
| COL-007 | 关键词收集模式 | 从参数值提取关键词 | ⬜ |
| COL-008 | 停用词过滤 | 过滤常见无意义词 | ⬜ |

**测试数据**:
```
请求1: GET /api/user?id=1&name=test&email=user@example.com
请求2: GET /api/post?id=2&title=hello&content=world

收集结果（仅参数名模式）:
example.com -> [id, name, email, title, content]

收集结果（参数名+关键词模式）:
example.com -> [id, name, email, title, content, test, hello, world, user, example]
```

#### 3.5.3 Arjun扫描

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| ARJ-020 | 手动触发扫描 | 正确调用Arjun | ⬜ |
| ARJ-021 | 增量扫描（仅新参数） | 只扫描未测试的参数 | ⬜ |
| ARJ-022 | 扫描成功，发现参数 | 正确解析结果 | ⬜ |
| ARJ-023 | 扫描失败（超时） | 标记为失败，不重复尝试 | ⬜ |
| ARJ-024 | 扫描失败（网络错误） | 标记为失败，记录错误 | ⬜ |
| ARJ-025 | 并发扫描多个接口 | 不冲突，结果正确 | ⬜ |
| ARJ-026 | 自定义字典 | 使用自定义字典扫描 | ⬜ |
| ARJ-027 | 保留原始参数（--include） | Arjun保留原有参数 | ⬜ |

**测试步骤**:
```bash
# 准备测试环境
# 1. 使用Burp代理访问测试站点，生成流量
# 2. 确保参数收集器已收集到参数
# 3. 在"主动探测"标签页点击"触发Arjun扫描"
# 4. 观察日志输出和扫描结果
```

---

### 3.6 日志与结果模块 (P1)

**测试文件**: `LogModel`, `TaskScheduler`

#### 3.6.1 日志记录

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| LOG-001 | 记录扫描流量（ALL_REQUESTS） | 所有请求都记录 | ⬜ |
| LOG-002 | 记录扫描流量（MATCHED_ONLY） | 只记录命中的请求 | ⬜ |
| LOG-003 | 日志滚动窗口（7000条） | 超过7000自动删除旧记录 | ⬜ |
| LOG-004 | 日志条目包含完整信息 | URL、method、状态码、响应时间、规则名 | ⬜ |
| LOG-005 | 高并发写入日志 | 不丢失、不错乱 | ⬜ |
| LOG-006 | UI排序功能 | 点击列头正确排序 | ⬜ |
| LOG-007 | UI过滤功能 | 搜索框正确过滤 | ⬜ |

**测试步骤**:
```bash
# LOG-003: 滚动窗口测试
# 1. 设置日志模式为ALL_REQUESTS
# 2. 使用JMeter发送10000个请求
# 3. 检查日志表格，应该只有7000条

# LOG-005: 并发测试
ab -n 10000 -c 100 "http://localhost/api/user?id=1"
# 观察日志是否有丢失或重复
```

#### 3.6.2 扫描结果

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| RES-001 | 检测到漏洞 | 显示在结果表格 | ⬜ |
| RES-002 | 结果包含证据 | 显示匹配的内容 | ⬜ |
| RES-003 | 结果包含原始/修改请求 | 可查看对比 | ⬜ |
| RES-004 | 结果包含响应 | 可查看响应内容 | ⬜ |
| RES-005 | 导出结果到文件 | 正确导出JSON/CSV | ⬜ |
| RES-006 | 清空结果 | 一键清空 | ⬜ |

---

### 3.7 UI界面模块 (P2)

**测试文件**: `DashboardTab`, `PassiveScanConfigTab`, `ActiveProbeTab`, `UnifiedConfigTab`

#### 3.7.1 仪表板

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| UI-001 | 统计信息实时更新 | 数字正确、实时刷新 | ⬜ |
| UI-002 | 统计图表显示 | 图表正确渲染 | ⬜ |
| UI-003 | 参数收集信息 | 显示主域名、参数数量 | ⬜ |
| UI-004 | 性能指标 | 显示扫描速率、响应时间 | ⬜ |

#### 3.7.2 被动扫描配置

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| UI-010 | 添加新规则 | 规则出现在列表 | ⬜ |
| UI-011 | 编辑规则 | 修改生效 | ⬜ |
| UI-012 | 删除规则 | 规则被移除 | ⬜ |
| UI-013 | 启用/禁用规则 | 开关生效 | ⬜ |
| UI-014 | 导入规则（JSON） | 正确导入 | ⬜ |
| UI-015 | 导出规则（JSON） | 正确导出 | ⬜ |
| UI-016 | 规则验证 | 无效规则无法保存 | ⬜ |
| UI-017 | 规则模板 | 预设模板可用 | ⬜ |

#### 3.7.3 主动探测

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| UI-020 | 参数收集统计 | 显示收集到的参数 | ⬜ |
| UI-021 | 触发Arjun扫描 | 按钮可点击，扫描启动 | ⬜ |
| UI-022 | 查看扫描结果 | 显示发现的参数 | ⬜ |
| UI-023 | 导出参数字典 | 导出为文本文件 | ⬜ |
| UI-024 | 导入参数字典 | 正确导入 | ⬜ |
| UI-025 | 全局参数管理 | 添加/删除全局参数 | ⬜ |

#### 3.7.4 配置中心

| 测试ID | 测试场景 | 预期结果 | 实际结果 |
|--------|---------|---------|---------|
| UI-030 | 黑白名单配置 | 正确保存 | ⬜ |
| UI-031 | Arjun工具配置 | 正确保存 | ⬜ |
| UI-032 | 全局注入模式 | 切换生效 | ⬜ |
| UI-033 | 被动扫描开关 | 开关生效 | ⬜ |
| UI-034 | 日志记录模式 | 切换生效 | ⬜ |

---

## 4. 集成测试计划

### 4.1 端到端流程测试

#### 场景1: SQL注入检测流程

**测试步骤**:
1. 配置SQL注入检测规则
2. 启用被动扫描
3. 通过Burp代理访问DVWA SQL注入页面
4. 观察日志和扫描结果

**预期结果**:
- ✅ 请求被正确拦截
- ✅ 参数被识别并注入payload
- ✅ 响应中检测到SQL错误信息
- ✅ 结果表格显示漏洞
- ✅ 日志记录完整的请求/响应

#### 场景2: XSS检测流程（配对架构）

**测试步骤**:
1. 配置XSS检测规则（配对架构）
2. 访问包含输入框的页面
3. 提交表单

**预期结果**:
- ✅ 所有输入参数都被测试
- ✅ XSS payload被正确注入
- ✅ 响应中检测到payload回显
- ✅ 结果标记为漏洞

#### 场景3: Arjun参数挖掘流程

**测试步骤**:
1. 通过Burp代理访问API接口
2. 确认参数被收集
3. 触发Arjun扫描
4. 查看扫描结果

**预期结果**:
- ✅ 参数正确收集到主域名下
- ✅ Arjun正确调用
- ✅ 发现的参数添加到字典
- ✅ 下次扫描使用新参数

### 4.2 跨模块交互测试

| 测试ID | 测试场景 | 涉及模块 | 预期结果 |
|--------|---------|---------|---------|
| INT-001 | 配置修改后立即生效 | 配置管理 + 请求处理 | 新流量使用新配置 | ⬜ |
| INT-002 | 去重标记跨扫描器生效 | 去重机制 + 扫描器 | 不重复扫描 | ⬜ |
| INT-003 | 日志滚动不影响扫描 | 日志系统 + 扫描器 | 持续正常扫描 | ⬜ |
| INT-004 | 黑名单过滤后不收集参数 | 过滤器 + 参数收集 | 黑名单URL不收集 | ⬜ |
| INT-005 | Arjun发现的参数用于扫描 | Arjun + 扫描器 | 新参数被测试 | ⬜ |

---

## 5. 性能与压力测试

### 5.1 性能基准测试

| 指标 | 目标值 | 实际值 | 测试方法 |
|------|--------|--------|---------|
| 启动时间 | < 5秒 | ⬜ | 记录插件加载时间 |
| 内存占用（空闲） | < 100MB | ⬜ | VisualVM监控 |
| 内存占用（高负载） | < 500MB | ⬜ | 1小时高流量后 |
| 请求处理延迟 | < 50ms | ⬜ | 平均每个请求处理时间 |
| 最大吞吐量 | > 100 req/s | ⬜ | JMeter压测 |

### 5.2 压力测试场景

#### 场景1: 高并发请求

**测试配置**:
- 请求速率: 100 req/s
- 持续时间: 10分钟
- 请求类型: 混合（GET/POST/JSON）

**监控指标**:
- CPU使用率
- 内存使用率
- 请求处理延迟
- 日志丢失率

**预期结果**:
- CPU < 50%
- 内存稳定（无泄漏）
- 延迟 < 100ms
- 无请求丢失

**测试命令**:
```bash
# 使用wrk进行压测
wrk -t12 -c400 -d10m --script=test-mixed.lua http://localhost/api
```

#### 场景2: 大量规则

**测试配置**:
- 扫描规则数量: 100条
- 每条规则5个payload
- 请求速率: 50 req/s

**预期结果**:
- 规则匹配性能不下降
- 内存占用线性增长
- 无死锁或卡顿

#### 场景3: 超大响应

**测试配置**:
- 响应大小: 10MB
- 请求数量: 100个

**预期结果**:
- 正常处理
- 内存及时释放
- 不影响其他请求

### 5.3 内存泄漏测试

**测试步骤**:
1. 使用VisualVM连接到Burp进程
2. 运行高负载测试（1小时）
3. 观察内存曲线
4. 手动触发GC，观察是否回收

**判断标准**:
- ✅ 内存曲线锯齿状（有起有落）
- ❌ 内存持续上升不回落（内存泄漏）

---

## 6. 真实场景测试

### 6.1 真实漏洞检测

使用公开的漏洞靶场测试检测准确性：

| 靶场 | 漏洞类型 | 测试URL | 预期结果 |
|------|---------|---------|---------|
| DVWA | SQL注入 | `http://localhost/dvwa/vulnerabilities/sqli/` | 检测到 |
| DVWA | XSS | `http://localhost/dvwa/vulnerabilities/xss_r/` | 检测到 |
| WebGoat | 命令注入 | `http://localhost:8080/WebGoat/start.mvc#lesson/CommandInjection` | 检测到 |
| Juice Shop | XSS | `http://localhost:3000/#/search?q=<script>` | 检测到 |

### 6.2 误报测试

使用正常应用测试误报率：

| 应用 | 测试页面 | 预期结果 |
|------|---------|---------|
| GitHub | `https://github.com/search?q=test` | 无误报 |
| Google | `https://www.google.com/search?q=test` | 无误报 |
| 本地博客 | 正常文章页面 | 无误报 |

### 6.3 兼容性测试

| 环境 | 测试项 | 预期结果 | 实际结果 |
|------|--------|---------|---------|
| **Windows 10** | 插件加载 | 正常启动 | ⬜ |
| | 配置保存 | 正常保存 | ⬜ |
| | Arjun调用 | 正常执行 | ⬜ |
| | UI显示 | 无乱码 | ⬜ |
| **macOS 13** | 插件加载 | 正常启动 | ⬜ |
| | 配置保存 | 正常保存 | ⬜ |
| | Arjun调用 | 绕过安全限制 | ⬜ |
| | UI显示 | 正常 | ⬜ |
| **Ubuntu 22.04** | 插件加载 | 正常启动 | ⬜ |
| | 配置保存 | 正常保存 | ⬜ |
| | Arjun调用 | 正常执行 | ⬜ |
| | UI显示 | 正常 | ⬜ |

---

## 7. 已知问题与风险

### 7.1 代码层面的问题

#### 🔴 P0 - 严重问题

| 问题ID | 模块 | 问题描述 | 影响 | 建议修复 |
|--------|------|---------|------|---------|
| ISS-001 | `ArjunIntegration` | 进程流资源未正确关闭 | 长时间运行可能导致句柄泄漏 | 已使用try-with-resources修复，需验证 |
| ISS-002 | `TaskScheduler` | 线程池关闭逻辑不完善 | 插件卸载时可能线程未终止 | 已添加超时等待逻辑，需测试 |
| ISS-003 | `LogModel` | 高并发写入日志可能死锁 | UI卡顿 | 已优化锁粒度，需压测验证 |
| ISS-004 | `UniversalScanner` | 响应为null时未检查 | 可能抛出NullPointerException | 已添加安全检查，需回归测试 |

#### 🟡 P1 - 重要问题

| 问题ID | 模块 | 问题描述 | 影响 | 建议修复 |
|--------|------|---------|------|---------|
| ISS-010 | `DeduplicationKeyGenerator` | 超长URL可能导致内存占用过高 | 性能下降 | 添加URL长度限制（<2000字符） |
| ISS-011 | `ParameterCollector` | 参数名清理逻辑可能过滤掉合法参数 | 参数收集不完整 | 优化清理规则，增加白名单 |
| ISS-012 | `GlobalFilter` | 正则表达式性能问题 | 高流量下CPU占用高 | 缓存编译后的Pattern对象（已实现） |
| ISS-013 | `XProbeConfigManager` | 配置文件损坏时回退逻辑不完善 | 用户配置丢失 | 添加配置备份机制 |

#### 🟢 P2 - 一般问题

| 问题ID | 模块 | 问题描述 | 影响 | 建议修复 |
|--------|------|---------|------|---------|
| ISS-020 | UI界面 | 大量日志时UI渲染卡顿 | 用户体验差 | 实现虚拟滚动 |
| ISS-021 | UI界面 | 规则编辑器输入验证不足 | 可能保存无效规则 | 添加实时验证 |
| ISS-022 | 日志系统 | 日志级别控制不够精细 | 调试困难 | 添加按模块的日志级别控制 |

### 7.2 架构层面的风险

| 风险ID | 风险描述 | 影响 | 缓解措施 |
|--------|---------|------|---------|
| RISK-001 | 扫描器串行执行，吞吐量受限 | 高流量下扫描延迟增加 | 使用线程池并行扫描（已实现） |
| RISK-002 | 配置文件格式变更导致兼容性问题 | 升级后配置丢失 | 实现版本迁移逻辑 |
| RISK-003 | Arjun外部依赖不稳定 | Arjun失败影响主功能 | 添加熔断机制，失败后自动禁用 |
| RISK-004 | 去重集合无限增长 | 长时间运行内存占用高 | 实现LRU缓存或定期清理 |

### 7.3 实际使用中的潜在问题

#### 场景1: 扫描高流量站点

**问题**: 内存占用持续增长，最终OOM

**原因**:
- 日志表格无限增长
- 去重Set无限增长
- 配置对象重复创建

**建议**:
- 限制日志最大条数（已实现7000条滚动窗口）
- 定期清理去重Set（超过1小时的key）
- 使用对象池复用配置对象

#### 场景2: 规则配置错误

**问题**: 正则表达式编写错误导致扫描失败

**原因**:
- 用户编写复杂正则时没有验证
- 正则表达式语法错误

**建议**:
- UI添加正则表达式测试功能
- 保存前验证正则语法
- 提供常用正则模板

#### 场景3: Arjun集成失败

**问题**: macOS/Windows用户无法使用Arjun

**原因**:
- Python环境配置复杂
- PATH设置不正确
- 安全限制

**建议**:
- 提供一键自动检测和配置向导
- 显示详细的错误信息和解决方案
- 提供fallback方案（内置字典爆破）

#### 场景4: 误报问题

**问题**: 正常请求被误判为漏洞

**原因**:
- 匹配规则过于宽松
- 响应特征不明确

**建议**:
- 提供规则评分和置信度
- 支持自定义白名单
- 提供"标记为误报"功能

---

## 8. 测试检查清单

### 8.1 功能测试检查清单

- [ ] 所有P0功能测试通过
- [ ] 所有P1功能测试通过
- [ ] 至少90%的测试用例通过
- [ ] 无P0严重bug
- [ ] P1 bug数量 < 5

### 8.2 性能测试检查清单

- [ ] 启动时间 < 5秒
- [ ] 空闲内存 < 100MB
- [ ] 高负载内存 < 500MB
- [ ] 请求处理延迟 < 50ms
- [ ] 无内存泄漏
- [ ] 无死锁
- [ ] 支持100 req/s吞吐量

### 8.3 兼容性测试检查清单

- [ ] Windows 10/11 测试通过
- [ ] macOS 12+ 测试通过
- [ ] Ubuntu 20.04+ 测试通过
- [ ] Burp Suite Pro 2023.1+ 兼容
- [ ] Burp Suite Community 兼容

### 8.4 安全测试检查清单

- [ ] 无SQL注入漏洞
- [ ] 无路径遍历漏洞
- [ ] 无命令注入漏洞
- [ ] 配置文件权限检查
- [ ] 日志不包含敏感信息

### 8.5 文档检查清单

- [ ] README完整
- [ ] API文档完整
- [ ] 用户手册完整
- [ ] 配置示例完整
- [ ] 常见问题FAQ完整

---

## 9. 测试报告模板

### 9.1 测试执行记录

| 日期 | 测试人员 | 测试模块 | 通过/失败 | 备注 |
|------|---------|---------|----------|------|
| 2025-10-02 | | | | |

### 9.2 Bug报告模板

```
Bug ID: BUG-XXX
优先级: P0/P1/P2/P3
模块: 
标题: 
复现步骤:
1. 
2. 
3. 

预期结果:

实际结果:

环境信息:
- OS: 
- Burp版本: 
- 插件版本: 

附件:
- 截图
- 日志
- 配置文件
```

---

## 10. 测试总结与建议

### 10.1 核心测试重点

根据代码审查，以下是最应该重点测试的模块：

1. **配置管理** (P0)
   - 配置加载/保存的健壮性
   - 配置热更新的正确性
   - 配置文件损坏的恢复

2. **去重机制** (P0)
   - 各种颗粒度的正确性
   - 并发场景下的一致性
   - 长时间运行的内存占用

3. **扫描器核心** (P0)
   - 批量/逐个模式的正确性
   - 配对表达式的计算准确性
   - Payload变量解析的正确性

4. **Arjun集成** (P1)
   - 跨平台兼容性
   - 异常处理的健壮性
   - 进程资源的正确释放

### 10.2 建议的测试优先级

**第一阶段（1-2天）**: 核心功能
- 配置管理
- 请求处理
- 扫描器基础功能
- 去重机制

**第二阶段（2-3天）**: 高级功能
- 配对架构
- Arjun集成
- 参数收集
- 日志系统

**第三阶段（2-3天）**: 性能与压力
- 高并发测试
- 内存泄漏检测
- 长时间稳定性测试

**第四阶段（1-2天）**: 真实场景
- 真实漏洞靶场测试
- 误报测试
- 跨平台测试

### 10.3 测试自动化建议

**可自动化的测试**:
- 单元测试（JUnit）
- 配置加载/保存测试
- 去重key生成测试
- Payload变量解析测试
- 正则表达式匹配测试

**需要手动测试的**:
- UI交互测试
- Arjun集成测试（依赖外部环境）
- 真实漏洞检测测试
- 跨平台兼容性测试

### 10.4 持续改进建议

1. **添加单元测试**: 对核心逻辑模块编写JUnit测试
2. **集成测试框架**: 使用TestNG或JUnit 5编写集成测试
3. **性能基准**: 建立性能基准，每次release都进行回归
4. **自动化CI**: 使用GitHub Actions自动运行测试
5. **代码覆盖率**: 目标达到70%以上的代码覆盖率

---

## 附录

### A. 测试环境搭建脚本

```bash
#!/bin/bash
# 测试环境一键搭建脚本

# 1. 安装DVWA
docker run -d -p 80:80 vulnerables/web-dvwa

# 2. 安装WebGoat
docker run -d -p 8080:8080 webgoat/webgoat-8.0

# 3. 安装Arjun
pip install arjun

# 4. 下载测试规则库
git clone https://github.com/xprobe/test-rules.git

echo "测试环境搭建完成！"
echo "DVWA: http://localhost"
echo "WebGoat: http://localhost:8080/WebGoat"
```

### B. 性能测试脚本

```bash
#!/bin/bash
# 性能压测脚本

# JMeter压测（需要先安装JMeter）
jmeter -n -t test-plan.jmx -l results.jtl -e -o reports/

# wrk压测
wrk -t12 -c400 -d10m --latency http://localhost/api/user?id=1

# 内存监控
jmap -heap <burp-pid>
```

### C. 常用测试命令

```bash
# 构建插件
./gradlew build

# 运行单元测试
./gradlew test

# 生成测试报告
./gradlew test jacocoTestReport

# 清理构建
./gradlew clean
```

---

**文档版本**: v2.0  
**最后更新**: 2025-10-02  
**维护者**: XProbe Team

