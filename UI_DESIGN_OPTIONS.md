# 接口探测和Arjun探测 UI 设计方案

## 当前实现的问题
- 当接口来源选择"手动添加"时，点击按钮会先弹出手动添加目标的对话框
- 当接口来源选择"无"时，点击按钮会弹出选择子域名/主域名的对话框
- 用户体验不够直观，需要多次点击和选择

## 方案对比

### 方案1：表格选择 + 按钮操作（推荐 ⭐）
**实现方式：**
- 在"已收集的流量数据"表格中，用户可以选择一行或多行
- 点击"接口探测"或"Arjun探测"按钮时：
  - 如果表格中有选中的行：对选中的子域名进行探测
  - 如果没有选中行：弹出选择对话框（或提示用户先选择）
- 支持多选：可以同时选择多个子域名进行探测

**优点：**
- ✅ 直观：直接在表格中选择，所见即所得
- ✅ 高效：不需要弹出额外对话框
- ✅ 支持批量操作：可以同时选择多个子域名
- ✅ 符合常见UI模式（类似文件管理器）

**缺点：**
- ⚠️ 需要先收集数据才能选择（但这是合理的）

**UI示例：**
```
[已收集的流量数据表格]
子域名         主域名      接口数  参数数  ...
✓ mbd.baidu.com baidu.com   1      15    ...  ← 选中
  www.baidu.com baidu.com   2      16    ...
  api.baidu.com baidu.com   3      20    ...

[接口探测] [开始 Arjun 探测]  ← 点击后对选中的行进行探测
```

---

### 方案2：按钮下拉菜单
**实现方式：**
- "接口探测"和"Arjun探测"按钮改为下拉按钮（带下拉箭头）
- 下拉菜单包含：
  - "探测选中的子域名"
  - "探测选中的主域名"（如果选中了子域名，自动识别主域名）
  - "探测所有"
  - "手动添加目标..."
  - "从已有数据选择..."

**优点：**
- ✅ 功能集中，一目了然
- ✅ 支持多种探测方式
- ✅ 不需要额外的对话框

**缺点：**
- ⚠️ 需要点击下拉菜单，多一步操作
- ⚠️ 如果表格中没有选中行，"探测选中的子域名"选项需要禁用

**UI示例：**
```
[接口探测 ▼]
  ├─ 探测选中的子域名
  ├─ 探测选中的主域名（包含所有子域名）
  ├─ 探测所有
  ├─ 手动添加目标...
  └─ 从已有数据选择...
```

---

### 方案3：表格操作列
**实现方式：**
- 在表格中添加"操作"列
- 每行有"接口探测"和"Arjun探测"按钮（小图标按钮）
- 点击对应行的按钮，直接对该子域名进行探测

**优点：**
- ✅ 最直观：每行直接有操作按钮
- ✅ 不需要选择，直接点击即可
- ✅ 支持快速操作多个子域名

**缺点：**
- ⚠️ 表格列数增加，可能显得拥挤
- ⚠️ 如果子域名很多，按钮会很多

**UI示例：**
```
子域名         主域名      接口数  参数数  ...  操作
mbd.baidu.com baidu.com   1      15    ...  [🔍] [✨]
www.baidu.com baidu.com   2      16    ...  [🔍] [✨]
api.baidu.com baidu.com   3      20    ...  [🔍] [✨]
```

---

### 方案4：右键菜单增强（当前已有，可优化）
**实现方式：**
- 在右键菜单中添加"接口探测"和"Arjun探测"选项
- 点击后直接对选中的子域名进行探测
- 支持多选：右键点击后，对选中的所有子域名进行探测

**优点：**
- ✅ 不需要额外的按钮操作
- ✅ 符合右键菜单的使用习惯
- ✅ 当前已实现按子域名探测，只需添加接口探测选项

**缺点：**
- ⚠️ 需要右键操作，可能不如直接点击按钮方便

**UI示例：**
```
右键菜单：
├─ 🔍 对此子域名进行参数探测（使用主域名下所有接口和参数）
├─ 🔍 对此子域名进行接口探测  ← 新增
├─ 🔍 对此主域名进行接口探测  ← 新增
├─ 🔍 对此主域名进行参数探测  ← 新增
├─ 🗑️ 删除此子域名数据
└─ 🗑️ 删除主域名数据
```

---

### 方案5：选择模式 + 批量操作
**实现方式：**
- 添加一个"选择模式"复选框或开关
- 开启后，表格支持多选（复选框列）
- 点击"接口探测"或"Arjun探测"时，对选中的所有行进行探测
- 可以添加"全选"和"反选"按钮

**优点：**
- ✅ 支持批量操作
- ✅ 可以选择多个子域名或主域名同时探测
- ✅ 灵活：可以选择部分目标进行探测

**缺点：**
- ⚠️ 需要额外的UI元素（复选框列）
- ⚠️ 可能增加UI复杂度

**UI示例：**
```
[✓ 选择模式] [全选] [反选]

[✓] 子域名         主域名      接口数  ...
[✓] mbd.baidu.com baidu.com   1      ...
[ ] www.baidu.com baidu.com   2      ...
[✓] api.baidu.com baidu.com   3      ...

[接口探测] [开始 Arjun 探测]  ← 对选中的3个进行探测
```

---

### 方案6：组合方案（推荐 ⭐⭐）
**结合方案1 + 方案4：**
- **主要方式**：表格选择 + 按钮操作（方案1）
  - 在表格中选择行，点击按钮进行探测
  - 支持单选和多选
- **辅助方式**：右键菜单增强（方案4）
  - 右键菜单中添加"接口探测"和"Arjun探测"选项
  - 方便快速操作
- **特殊场景**：按钮下拉菜单（方案2）
  - 当没有选中行时，按钮显示下拉菜单
  - 提供"手动添加"、"探测所有"等选项

**优点：**
- ✅ 兼顾直观性和灵活性
- ✅ 支持多种操作方式
- ✅ 适应不同用户习惯

---

## 推荐方案

### 首选：方案6（组合方案）
结合表格选择、右键菜单和下拉菜单，提供最灵活的操作方式。

### 备选：方案1（表格选择）
如果追求简洁，只实现表格选择 + 按钮操作即可。

## 实现建议

1. **表格选择功能**：
   - 表格已支持多选（`setSelectionMode(ListSelectionModel.MULTIPLE_INTERVAL_SELECTION)`）
   - 需要修改按钮点击逻辑，检查是否有选中行

2. **右键菜单增强**：
   - 当前已有右键菜单，只需添加"接口探测"选项
   - 可以添加"按主域名探测"选项

3. **按钮逻辑优化**：
   - 有选中行：对选中的行进行探测
   - 无选中行：根据接口来源决定（手动添加/自动采集/选择对话框）

